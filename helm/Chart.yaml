apiVersion: v2
name: coriolis-operator
description: Controller skeleton for CoriolisAppliance resources
type: application
# CIXpress owns future synchronized release updates.
version: 0.3.0
appVersion: "0.3.0"
