# Status

Released `RuntimeNotImplemented` records below remain historical evidence. Release `0.5.37` added runtime readiness but its bounded POC was not accepted: source `03da71f851cf11e3feecc6f2941787699918b957` passed CIXpress Default `03n7l6` (`2026-08-26 18:29:15Z`-`18:30:20Z`) with top-level and `git-clone`/`kaniko-build`/`helm-update`/`cleanup` all `SUCCEEDED`; CI release `89b818095ac31ec5a42ea179679d4098cf04178a` published chart/app/operator `0.5.37`, chart digest `sha256:dc329077d03588b5f76c41b84c26f18c7e0695e462b4e713e07d01ce2cdfde00`, and operator digest `sha256:a8f35a7f168e7fdb35aee415095e8bcdd3e4ecadea56867bc3a7b1188d3715ea`.

The isolated POC used context `virt-infra-dev-buc-hq`, Argo project `default`, namespace/Application `coriolis-readiness-validation-20260826`, chart `0.5.37` with `skipCrds`, CR `readiness-validation`, and host `coriolis.app.cloudbase.wiki`. The node was healthy twice; the target/PVs/host were absent before mutation; the isolated exact-digest operator was `1/1` with zero restarts. The first disposable Application used a duplicated repo path and failed comparison with `401` before workloads; correcting it to established `cr.virtomat.io/virtomat` was a harness correction. Pull Secrets `regcred` and external read-only `coriolis-appliance-registry` were copied value-silently in memory with create/no-last-applied annotation; no payload was printed or written to workspace/tool output, and the target copies were removed with the namespace. The CR created at `19:05:45Z`, reached `Accepted=True`, `acceptedVersion=2603.4`, and `Reconciled=True`, then from `19:07:31Z` remained `Ready=False/RuntimeHealthCheckFailed` and `Degraded=True`; no `RuntimeReady` occurred during the bounded 20-minute observation. Bootstrap, PVCs, StatefulSets, ten runtime Deployments, six Services, and all runtime Pods met the structural gate. Fixed, value-safe in-Pod checkpoints verified web/config `200`, valid bounded config and first-launch, and all seven required logical-origin pairs, but `servicesUrls` included one unrelated upstream key (8 versus 7). Production equality rejected it before Keystone/API checks.

Frozen upstream config and the accepted web validator already tolerate unrelated keys. The local, uncommitted predicate fix now requires a Mapping with all seven required pairs while ignoring extras; missing or wrong required values still fail before auth, and no logs, bodies, headers, tokens, or errors leak. Validation passed 705 tests in 21.98s, 17 focused `runtime_health_checks` tests, Ruff lint, 70-file formatting, strict mypy over 18 source files, Helm lint (1 chart/0 failures) and template, `git diff --check`, and local Docker image `5f4ce77f38d4`, which was removed; no MkDocs ran. The fix is unpublished and unreleased, with no developer version, Chart, app, image, RBAC, CRD, or Helm change. Cleanup was normal: CR 23s, Application 9s, namespace 55s, and both Delete-policy PVs absent within 34s; no force/finalizer/owner/individual child/PVC/PV/Ingress deletion. Host and target are absent; shared `0.5.37` is `Synced/Healthy`, operator `1/1` zero restart, and node healthy. Next gates are review, explicit commit/publish, a corrected CI release, and a fresh clean POC; scale-zero/recovery/recreation acceptance remains unexecuted. `Ready=True` remains unaccepted and does not establish production readiness, HA, storage, backup/restore, external routing, or workflow acceptance.

On 2026-08-26, logical-origin Ingress reconciliation was released and POC-accepted as `0.5.35`. Source `016a25744a7c946a02fa6d89e38fa39e24e32146` (`Implement logical-origin Ingress reconciliation`) was published by CIXpress Default `4srgi9` from `12:30:18Z` to `12:31:50Z`; top-level and `git-clone`/`kaniko-build`/`helm-update`/`cleanup` all `SUCCEEDED`. CI release `a9a594bcdcdc5532e7b676067d55edb9566d81a4` published chart/app/image `0.5.35`; released operator digest `sha256:43e5787aa6bb96bbffc895fc792727afd85be5aa4aeae7fbcde0cb2386257a4f`. The operator owns three owner-referenced `networking.k8s.io/v1` Ingresses named `<appliance>-coriolis-web`, `<appliance>-keystone`, and `<appliance>-coriolis-api`. They share the resolved host, `ingressClassName`, TLS host/Secret, HTTPS redirect, and exact-origin CORS `https://<host>` with no wildcard; CORS permits `POST`, `GET`, `OPTIONS`, `DELETE`, `PUT`, and `PATCH`, includes `X-Auth-Token` and `X-Subject-Token`, and exposes `X-Subject-Token`. The primary web Ingress is the only cert-manager-annotated resource in `certManager` mode and routes `/` (`Prefix`) without rewrite to its same-name Service TCP `3000`; Keystone routes `/identity(/|$)(.*)` to same-name Service `5000` with `/v3/$2`; API routes `/coriolis(/|$)(.*)` to same-name Service `7667` with `/v1/$2`. The secondary Ingresses carry no cert-manager issuer annotation; `existingSecret` references an external same-namespace TLS Secret and no Ingress has a cert-manager annotation.

Pure ingress-settings validation precedes API creation. All three Ingresses are pre-read, classified, and preflighted before mutation; any collision is all-or-nothing and mutation-free. Managed repair uses resourceVersion-guarded SSA. After the API and web Service/Deployment, apply order is web, Keystone, API, then the marker last; sanitized failures skip later resources and the marker. The `spec.ingress` change handler exists. RBAC adds only Ingress `get`/`create`/`patch`, with no list/watch/delete/finalizer. No dedicated backend route targets `/log-stream`, Barbican, logger, licensing, or Metal Hub while their Services are absent; unmatched paths fall through to the web `/` `Prefix`/SPA until a more-precedent route is implemented. A future dedicated `/log-stream` logger route requires WebSocket support. The operator neither reads nor writes TLS certificate Secret material and does not install ingress-nginx or cert-manager. Final conditions remain `Ready=False/RuntimeNotImplemented`.

The local gate passed 640 tests in 17.84s; Ruff; 70-file format check; strict mypy over 18 source files; Helm lint/template including the exact Ingress Role rule; `git diff --check`; and local Docker image `bada188f716e`. The accepted isolated POC used context `virt-infra-dev-buc-hq`, project `default`, namespace/Application `coriolis-ingress-validation-20260826`, chart `0.5.35`, `skipCrds`, and host `coriolis.app.cloudbase.wiki`; final target scope is fully absent. Separate web, Keystone, and API deterministic Ingress collisions held stable and mutation-free for 223s, 195s, and at least 130s, with the expected complete `ResourceCollision` condition set, absent acceptedVersion, and no managed/retained/marker/PVC/PV writes; deleting only the API fixture auto-recovered without a CR edit or operator restart. Trusted production LetsEncrypt TLS, public DNS `89.34.101.238`, exact HTTP `308` HTTPS redirect, web `200`, Keystone logical paths `200`, Coriolis API logical paths unauthenticated `401`, and exact-origin CORS preflight `204` with credentials and Authorization allowed were verified; a foreign origin was not reflected. All workloads/bootstrap remained healthy, while successful CR status truthfully stayed `Ready=False/RuntimeNotImplemented`.

Deliberate managed-Ingress drift did not repair over 195s because the 60s timer is intentionally collision-only; normal operator Pod replacement/Kopf resume repaired the same Ingress UID, preserved foreign metadata, and the test metadata was explicitly removed. This is not broad periodic self-healing. Normal CR recreation reused retained ownerless Secrets/PVCs/PVs and TLS Secret metadata exactly, moved owned children/certificate to the new owner, and recovered routes. The intended live backend/Ingress/marker `ADDED` order is not claimed because two watch harnesses failed; source and unit tests establish the intended order. TLS Secret exact metadata survived CR/Ingress/Certificate GC and disappeared only with namespace deletion. Normal CR/Application/namespace cleanup and Delete-policy PV cleanup used no force, finalizer/owner edits, or individual retained/PV deletion; shared Argo remained `0.5.35` healthy, the node was healthy, and post-cleanup HTTP was `404`. A second diagnostic Secret metadata command emitted a last-applied annotation containing payload material into an internal tool stream; values were not reproduced, target copies were removed with the namespace, and the user waived rotation and accepted residual risk. Host uniqueness remains external/list-free and multi-CR unvalidated. Browser login, valid-credential/authenticated provider or write workflows, HA, production storage/backup/restore, periodic drift self-heal, fine-grained live ordering, and `Ready=True` acceptance remain excluded.

On 2026-08-26, the Coriolis web slice was released and POC-accepted as `0.5.33`. Source `942557a0914b7455af6dbeac6ae5966417bd1223` (`Implement Coriolis web reconciliation`) was published by CIXpress Default `opfrnr` from `08:39:32Z` to `08:40:58Z`; the exact source was the pipeline head and top-level plus `git-clone`, `kaniko-build`, `helm-update`, and `cleanup` all `SUCCEEDED`. CI commit `9f7151af10e2275e15718a325a12e850601ec5f3` published tag/chart/app/operator `0.5.33`, chart digest `sha256:0e0452229c22c2a4067c55df25c6e09c1ffeefcc8e92eb3fba77ab477713e27e`, and operator digest `sha256:8f3e80f3c6ea2a79c83feff5078ca45ef8d8615b060ae91b1fae235428a31273`. The web Deployment remains pinned to `cr.virtomat.io/virtomat/coriolis/coriolis-web:2603.4@sha256:32ebc391ac46fe627185694b3fd252afd7587b152f526dff38ae0a5b887c0db1`.

The accepted isolated POC used context `virt-infra-dev-buc-hq`, project `default`, namespace/Application `coriolis-web-validation-20260826`, exact OCI chart `0.5.33`, and `skipCrds`. Node preflight was healthy twice. Service-only and Deployment-only collision fixtures stayed exact for 175s and 273s respectively with stable `ResourceCollision`, absent acceptedVersion, and zero writes; deleting only the Deployment fixture auto-recovered without a CR edit or operator restart. The live CR-owned web Service TCP `3000`, ready EndpointSlice, and one-replica `Recreate` Deployment/Pod matched the exact digest and contract with zero restarts and no Ingress. The fresh fixed-output Service-DNS verifier emitted exactly `coriolis-web-poc-ok`, and the reset verifier emitted exactly `coriolis-web-reset-ok` after normal web Pod replacement. Controlled selector/replica drift was repaired in place by normal operator reconciliation. CR recreation preserved retained Secret/PVC metadata and PVs, moved 28 direct resources to the new owner, and reconverged with the exact web digest; normal CR/Application/namespace cleanup removed the target and its Delete-policy PVs without force, finalizer/owner edits, or individual PV operations. Two prior verifier programs failed before mutation due to test-harness defects, and one recreation command failed before creating a Pod; the fresh final manifest passed. Shared `argocd/coriolis` remained `0.5.33` `Synced/Healthy`, operator `1/1` zero restarts, no CR, and node healthy.

A delegated broad cleanup inventory command accidentally emitted Secret payload data into internal tool output. No Secret was modified and target copies were deleted with the namespace; no claim that credentials represented by `coriolis/regcred` and `coriolis/coriolis-appliance-registry` were not exposed is valid. The user explicitly waived credential rotation for this work and accepted the residual risk, allowing logical-origin Ingress to proceed. Accepted scope excludes Ingress/TLS/external routing, browser login, provider or write workflows, persistence, Step CA/web-proxy, HA, production storage, backup/restore, and overall readiness; `Ready=False/RuntimeNotImplemented` remains mandatory.

On 2026-08-26, the Coriolis worker slice was released and POC-accepted as `0.5.31`. Source commit `d1f45c7297ffa925bd9412a26dedd50d1a95e50f` (`Implement Coriolis worker reconciliation`) was published by CIXpress Default `c9jv45` (2026-08-26 `04:42:50Z`-`04:44:20Z`) at the exact source SHA, with top-level and `git-clone`, `kaniko-build`, `helm-update`, and `cleanup` all `SUCCEEDED`. CI release commit `1b7835146242e945d40660cd6462486a860c7f70` synchronized chart/app/operator `0.5.31`; released operator digest `sha256:52762fa5741ca65b8b60df976f7253db72dac650f8ba1bb7b0ee03fe1dbdf7e9` and chart digest `sha256:8e2578b401a7261c0556ac65605fec6b1aa8ef88987ec6c33d08bafac9e5b2e5`.

The controller owns one owner-referenced, one-replica `Recreate` worker Deployment at exact `cr.virtomat.io/virtomat/coriolis/coriolis-worker:2603.4@sha256:ff30999d6e43709411f197b1b6b80dbce1d7e5498a27f869df93a061626ab2c9`, applied after deployer-manager and before the API, using direct `/usr/local/bin/coriolis-worker --worker-process-count 1 --config-file=/etc/coriolis/coriolis.conf`, a 30-second grace period, and `imagePullPolicy: Always`. The Pod sets a deterministic label-safe `hostname` equal to the runtime name for stable `(host,binary,topic)` registration. It is explicitly privileged: root `runAsUser/Group 0`, `privileged: true`, `allowPrivilegeEscalation: true`, read-only root filesystem, and `Unconfined` seccomp, with automount token and service links disabled, a read-only config Secret, memory-backed `/tmp`, `emptyDir` logs/export, and `hostPath` `/dev` (read-write) and `/lib/modules` (read-only). There is no Service, port, probe, PVC, lock, init container, service account, resource block, `envFrom`, host networking, PID/IPC, mount propagation, or VDDK/Hyper-V/libvirt/logger/profiling/other mount. Reconciliation completes collision-safe full pre-reads/preflight before mutation, uses guarded SSA, gates apply on a succeeded common bootstrap, sanitizes apply failures, and writes the marker last; `Ready=False/RuntimeNotImplemented` remains mandatory.

The accepted POC used context `virt-infra-dev-buc-hq`, Argo project `default`, namespace/Application `coriolis-worker-validation-20260826`, OCI chart `coriolis/helm/coriolis-operator:0.5.31`, and `skipCrds`; preflight node was Ready/schedulable/no pressure twice. An initial schema-valid but runtime-incomplete CR produced stable mutation-free `InvalidRuntimeConfiguration`; it was normally deleted while no child or retained resources existed, then recreated with source-backed `local-path` MariaDB 10Gi/RabbitMQ 1Gi and 250m/512Mi requests, 1 CPU/1Gi limits (no schema defaulting is implied). Ownerless zero-replica worker collision Deployment UID `2858a477-d6e9-420f-99bf-8e61ab9fe7a9`, RV `14245705`, hash prefix `8d6a1cb0`; RS UID `13e33901-24db-420b-83f7-fbe3140822ce`, RV `14245704`, hash prefix `309fe1d5`; valid collision CR UID `b6082e05-b4bb-4b28-ae3a-6de46965c539`, RV `14247963`. Three snapshots spanning 204s held exact fixture/CR identity, stable `ResourceCollision`, absent acceptedVersion, and no CR-owned/retained/marker/PVC/PV writes or operator/node change. Deleting only the collision at `05:16:01Z` produced first child `05:16:46Z`, bootstrap completion `05:18:09Z`, managed worker Ready `05:18:13Z`, and final reconcile `05:18:16Z`, without CR edit or operator restart (first observations, not guaranteed exact latency).

The live worker exact contract passed: worker digest `sha256:ff30999d6e43709411f197b1b6b80dbce1d7e5498a27f869df93a061626ab2c9`, direct command/count/config, one Recreate replica, appliance-specific hostname, root/privileged/Unconfined/read-only root, actual `/dev` RW and `/lib/modules` RO hostPath mounts, config Secret RO, memory tmp/emptyDir logs/export, all documented omissions, no Service/EndpointSlice, zero restarts, and truthful final conditions. A warning-suppressed fixed-output registration probe emitted exactly `coriolis-worker-ok`; direct Worker RPC plus scheduler showed exactly one enabled/UP worker; the hashed DB service identity `6c0f0800ab89c2cf30d5bc01eb22e6040a261bb9ab707681a1231ae30d18d983` stayed identical through stability, worker replacement, RabbitMQ recovery, and CR recreation. Initial 20s stability passed. Normal worker Pod deletion requested `05:37:05Z`; replacement UID `ca6512f3-24bc-4819-88a5-f8b611ca6398` Ready `05:37:22Z`, zero restarts, exact image/node, unrelated identities unchanged. Normal RabbitMQ deletion requested `05:37:49Z`; replacement UID `ea8693bd-b493-4987-a33d-2d0da8e044bd` Ready `05:38:47Z`, zero restarts/exact image/node, worker unchanged; Rabbit PVC UID `48b92e12-0364-4bec-a8b8-a16ecee8e425`, RV `14251081`, volume `pvc-48b92e12-0364-4bec-a8b8-a16ecee8e425` exact.

Normal CR lifecycle: old UID `b6082e05-b4bb-4b28-ae3a-6de46965c539` delete requested `05:43:15Z`, old resources absent `05:44:12Z`; new UID `4a701a89-ceec-47ac-b8c4-5c17593ec877` created `05:44:27Z`; all 26 direct resources converged by about `05:45:30Z` (6 ConfigMaps, 9 Deployments, 1 Job, 3 Secrets, 5 Services, 2 StatefulSets), with a transient retry/apply status self-resolving before bootstrap. Five retained ownerless credential Secrets preserved exact UID/RV; MariaDB PVC UID `7e21a834-ac78-446c-89c2-0fcfeb4da979` RV `14251094` and the Rabbit PVC above preserved exact UID/RV/volume/PV/Bound with no retained owner refs added; the marker and all 26 direct resources moved only to the new UID. Probe/hash and 20s stability passed after recreation. Cleanup: CR delete requested `05:52:29Z`, absent `05:52:32Z`, all children GC'd; retained/external/PVC/PV identities exact before namespace deletion. Application deleted `05:53:56Z`-`05:54:10Z`; namespace deleted `05:54:12Z`-`05:55:35Z`; both Delete-policy PVs absent by `05:55:55Z`, with no force/grace-zero/finalizer/owner edits or individual child/retained/PVC/PV deletion; normal Argo Events not individually deleted. Final target CR/Application/namespace/PVs are absent; shared `argocd/coriolis` is `0.5.31` `Synced/Healthy`, shared operator 1/1 zero restarts, no shared CR, node Ready/schedulable/no pressure. Accepted boundaries: released worker reconciliation, exact privileged host mounts working enough for Pod/registration lifecycle, registration-only RPC/scheduler/DB identity, and collision/recovery/replacement/retention/cleanup. Excluded: provider imports/classes beyond `find_spec`, any provider/migration/task/write/Keystone-trust/VDDK/Hyper-V/libvirt/iSCSI/logger workflow, HA/cross-node/storage-persistence production claims, backup/restore, and overall readiness. `Ready=False/RuntimeNotImplemented` remains mandatory; remaining workloads and logical-origin Ingress follow. The full local gate remains 589 tests/13.88s, Ruff check and 43-file format check, strict mypy over 17 source files, Helm lint/template, local operator `d4e637d378ad`, and exact validator `SUMMARY runtime passed 478.013`, with clean review/cleanup.

On 2026-08-25, deployer-manager was released and POC-accepted as `0.5.29`. Source `4382c000f18cc323bacebc0e1df78c8b2f052d20` was published by CIXpress Default `wk1zo8` (`07:43:50Z`-`07:45:23Z`), with top-level, `git-clone`, `kaniko-build`, `helm-update`, and `cleanup` all `SUCCEEDED`; CI release/tag `c35afc7032de19ec43f3870b6300e2f4f02370aa` published chart/app/operator `0.5.29` at operator digest `sha256:37634ac8afd4ce7e7445aeb5f5786867cdd009d0f41b13b9baf95b1e92be4b08`.

The accepted POC used context `virt-infra-dev-buc-hq`, project `default`, namespace/Application `coriolis-deployer-manager-validation-20260825`, OCI chart `coriolis/helm/coriolis-operator:0.5.29`, and `skipCrds`. After the node recovered from preflight `Ready=False` to two consecutive `Ready=True` observations before mutation, it remained Ready. Ownerless zero-replica Deployment UID `866472b2-adf6-415a-8395-2421a079ab0e`, RV `13913626`, ReplicaSet UID `94388c5f-24f8-4c59-93b6-45d30716762c`, and spec hash `831ed01de99bb0854032749f2ede0e613e5ad437620d5f105d9948b2c482a095` held exact with initial CR UID `bb144cd3-a3ae-45e6-8eb3-69b00b6eb220`, RV `13913723`, absent acceptedVersion, stable `ResourceCollision`, zero CR-owner refs, deterministic managed/retained/marker writes, PVC/PV claims, or restarts across snapshots spanning 143s. Deleting only the collision at `08:08:04Z` produced first children in 54s, bootstrap in 135s, the managed deployer in 140s, final reconcile in about 141s, and Available in 143s without CR edit or operator restart.

The live owner-referenced one-replica `Recreate` Deployment pins deployer digest `sha256:a2a7091daf8e172b96fa0b48d19ffad285d7bfaad42fc7e8cd44a688f06f36aa`, direct command, config Secret only, UID/GID/fsGroup `42434`, restricted security, writable memory `/tmp` plus `/var/log/coriolis` only, and every documented omitted surface. Its initial Pod UID `e998c567-7edf-4ef4-a41d-0428be6ef464` was Ready with zero restarts; final conditions remain `Ready=False/RuntimeNotImplemented` and `Degraded=False/NotDegraded`. Initial 22s stability passed. Two normal deployer Pod deletions produced distinct Ready, zero-restart, exact-digest replacements (`aed98ed1-ce5b-40d6-b58e-ae309d9f6724`, then `669e570f-1101-4148-b820-531743a6e88d`) and 20s stability; exact deployer replacement latency is deliberately unclaimed because two local monitor assertions failed after deletion. RabbitMQ replacement (`cd8cb7bf-c1eb-4640-b3c9-bc5e8e6969cd` to `9c6a86fb-9034-4139-a9ed-990067457d83`) was requested at `08:28:30Z`, created at `08:28:33Z`, Ready at `08:29:01Z`, and zero-restart; deployer stayed unchanged/zero-restart and Rabbit PVC UID/RV/volume remained `d8d80ddd-824d-4f90-b46e-6d151a0679e7`/`13916615`/`pvc-d8d80ddd-824d-4f90-b46e-6d151a0679e7`.

CR recreation removed the old UID and created UID `ddfea8e8-baeb-4dc4-84cf-4c0e8cf04a24` at `08:39:39Z`, with final transition `08:40:48.942Z`. Five ownerless retained Secrets and two Bound PVCs preserved exact metadata/RV/volume; 25 direct ephemeral resources transferred (6 ConfigMaps, 8 Deployments, 1 Job, 3 Secrets, 5 Services, and 2 StatefulSets). A duplicate local `kubectl create` returned `AlreadyExists` after the new CR existed, so exact recreation-command timing/latency is deliberately unclaimed. Normal cleanup removed the CR in 23s, Application in 6s, namespace in 73s, and both Delete-policy PVs by `08:50:09Z`, with no force, grace-zero, finalizer patches, owner edits, or individual child/retained/PVC/PV deletion. Normal Argo Events remain in `argocd` and were not individually deleted; the exact CR/Application/namespace/PVs are absent. Shared `argocd/coriolis` is `0.5.29` `Synced/Healthy`, the shared operator is exact digest/zero-restart with no shared CR, and the node is Ready/schedulable. Deployer RPC, auto-deployment, Keystone trust, provider/migration/nonempty/write workflows, HA, production storage, backup/restore, and overall readiness remain unqualified; privileged worker and remaining workloads are next.

On 2026-08-25, minion-manager was released and POC-accepted as `0.5.28`. Source `5294df9a50e6c8a15c6cad374c66c3a9ab95b077` passed 558 unit tests, Ruff, the 61-file format check, strict mypy over 15 source files, Helm lint/template, operator build `690bdd8663a8`, exact-image runtime validation (`SUMMARY runtime passed 255.112` on the publication rerun), and diff check. CIXpress Default pipeline `f3pgh0` ran `05:18:39Z`-`05:19:50Z`; top-level and `git-clone`, `kaniko-build`, `helm-update`, and `cleanup` all `SUCCEEDED`. CI commit `69c60fdcaec2ec86df99d5cf91520d7051000691` published tag/chart/app/operator `0.5.28`; released operator imageID is `sha256:4e1a90f2bb883f80437ca048aa6645a9e94d87b80f2f82ad1a4964993e88886b`.

The accepted POC used context `virt-infra-dev-buc-hq`, project `default`, namespace/Application `coriolis-minion-manager-validation-20260825`, OCI chart `coriolis/helm/coriolis-operator:0.5.28`, and `skipCrds`. Ownerless zero-replica minion-manager Deployment UID `403307cd-c06b-4b83-ac0d-83f42221bc27`, RV `13873650`, and spec digest `070cb7df...8d74` held exact across three complete observations spanning 295s. Initial CR UID `13c491c4-6ced-4e0a-8495-459615491336` held absent acceptedVersion and stable `ResourceCollision` with zero managed/retained/marker writes; operator UID `04bb480b-573f-4fc7-bbfc-ec08c1057fbd` remained zero-restart. Deleting only the collision at `05:42:21Z` produced first children in 55s, bootstrap completion in 127s, and managed reconciliation/availability in 158s without CR edit or operator restart.

The live owner-referenced one-replica `Recreate` Deployment pins `sha256:1ea016dd...e1fb9`, direct minion-manager command, UID/GID/fsGroup `42434`, restricted security, projected configuration, and writable tmp/log paths, with no Service, `envFrom`, ports, probes, locks, PVC, init container, service account, or resources. Fixed-output diagnostics and empty `get_minion_pools` passed before and after 20s stability; minion replacement took 7s and RabbitMQ replacement 31s, preserving exact images, zero restarts, unrelated identities, and RabbitMQ PVC identity. CR recreation changed UID to `454dd96c-e204-4512-9f27-6c633d880dd3`, retained five ownerless state Secrets and two Bound PVCs at exact UID/RV, moved 24 ephemeral resources to the new owner, converged in 65s, and passed RPC again. Final conditions retain `Ready=False/RuntimeNotImplemented`. Normal cleanup removed the CR, Application, namespace, and both Delete-policy PVs without force, finalizer changes, or individual retained-resource deletion; shared Argo/operator and node stayed healthy. Minion provisioning, nonempty pools, refresh, provider actions, Keystone trust, writes, HA, production storage, and overall readiness remain unqualified.

On 2026-08-24, transfer-cron was released and POC-accepted as `0.5.27`. Implementation source `293500a0a64f6e10c776d674c141ae1ea3ba05e4` (`Implement Coriolis transfer-cron reconciliation`) passed 548 unit tests, Ruff, the 59-file format check, strict mypy over 14 source files, Helm lint/template, operator build image ID prefix `03163df34d52`, exact runtime validator `SUMMARY runtime passed 320.708`, and `git diff --check` with zero validator leftovers. CIXpress Default pipeline `ermyhd` ran `2026-08-24T19:58:15Z`-`19:59:44Z`; top-level and `git-clone` (19:58:15-19:58:26), `kaniko-build` (19:58:26-19:59:04), `helm-update` (19:59:30-19:59:36), and `cleanup` (19:59:36-19:59:44) all `SUCCEEDED`. CI commit `3f3074e68d9b786986e688649646f08bcd6fbd63` published tag/chart/app/operator `0.5.27`; released operator imageID is `sha256:7c70f0e1c54559440d02d5595b054c48c7b0d586f9f61b86aeaf717364b104cf`.

The accepted POC used context `virt-infra-dev-buc-hq`, project `default`, namespace/Application `coriolis-transfer-cron-validation-20260824`, exact chart `0.5.27` with `skipCrds`, and operator UID `04e546c6-91da-4c2a-9d14-ed1949894eb6` at zero restarts. An unmanaged ownerless zero-replica transfer Deployment (UID `daed6df8-8b2d-4fff-86fc-3d57edc58c64`, RV `13728367`, spec digest `0646ae84...d2f64`) and its ReplicaSet (UID `21b1cedc-202e-4336-8d79-35fc47339591`) held exact at t=0/+65/+130 with initial CR UID `f8699078-0ebd-4870-9cdb-fa9b688867a5`, generation/observed `1`, absent acceptedVersion, stable `ResourceCollision`, zero CR-owned resources, retained writes, marker, or operator restart. Deleting only the collision at `20:24:07Z` started children at `20:24:25Z`; bootstrap completed `20:25:48Z`, managed transfer converged `20:25:52Z`, and the transfer was Available `20:25:55Z` (108s), without CR edit or operator restart.

The live owner-referenced one-replica `Recreate` Deployment used exact transfer digest `sha256:3a44d3b...2b3ea9c`, UID/GID/fsGroup `42434`, restricted security, safe environment, projected configuration, and writable tmp/log paths, with no Service, `envFrom`, ports, probes, locks, PVC, init container, service account, or resources. Pod Ready/zero restarts and fixed messaging probes twice 20 seconds apart passed. Conductor, transfer, and RabbitMQ replacement completed in 45s, 7s, and 35s; transfer retained exact digest/zero restarts and empty post-fork schedule-load diagnostics.

CR recreation deleted old UID `f869...` at `20:48:54Z`, confirmed it absent in 1s and old-owned resources absent in 2s, then created UID `bff7959a-37f3-4450-8593-7ea8c1ec00ac` at `20:48:57Z`. Five ownerless state Secrets and two Bound PVCs retained exact identity, metadata, volume association, and no writes; 20 ephemeral resources moved to the new UID. Bootstrap completed in 51s and final convergence in 77s. Final conditions were `Accepted=True`, `Progressing=False/RuntimeNotImplemented`, `Reconciled=True`, `Ready=False/RuntimeNotImplemented`, `Degraded=False`, and `Upgradeable=False`. A failed local jq polling expression did not affect the accepted 7s transfer result, which uses Kubernetes creation/Ready timestamps. An immediate aggregate messaging probe after recreation was rejected; unchanged Pods then passed independent diagnostics, and corrected warning-suppressed combined probes returned only `CORIOLIS_MESSAGING_OK` at `20:57:48Z` and `20:58:11Z`. This was tooling-only evidence correction, not a workload change.

Final CR deletion began `21:00:13Z` and was absent `21:00:45Z`; metadata-only per-kind checks proved no old owner refs before Application deletion, though exact GC completion is unclaimed after one broad metadata query failed. Application deletion/absence was `21:05:35Z`/`21:05:41Z`; namespace deletion/absence `21:05:47Z`/`21:06:33Z`; both Delete-policy local-path PVs were absent `21:06:41Z`, without force, finalizer/patch, or individual deletion. Final scope is absent; shared `argocd/coriolis` is `0.5.27` `Synced/Healthy`, shared operator `1/1` exact imageID/zero restarts, zero CRs, node Ready/schedulable. This proves the exact live Deployment, collision recovery, empty schedule diagnostics, replacements, retained no-write recreation, and normal cleanup; it does not prove nonempty schedules, firing/due transfers, Keystone trust, writes/migrations/providers, in-memory cron persistence, HA, cross-node/production storage, backup/restore, or overall readiness. `Ready=False/RuntimeNotImplemented` and no transfer Service/Ingress remain mandatory; remaining workloads follow, then logical Ingress.

On 2026-08-24, the Coriolis scheduler slice was released and POC-accepted as `0.5.26`. Implementation source `d4805eabc42a2106ad66300e31791e05dc294702` (`Implement Coriolis scheduler reconciliation`) passed 540 unit tests and the complete local gate. CIXpress Default pipeline `02mop6` (17:07:04Z-17:08:25Z; top-level and all expected steps `SUCCEEDED`) published CI commit `de1da0f8a1272d9500f81d872951c3c20e414b8f`, tag/chart/app/operator `0.5.26`; the released operator imageID is `sha256:1893f0f65656b346928764340bf8eea73732a034ca7166989f41774be5712f5b`.

The accepted isolated POC ran in namespace/Application `coriolis-scheduler-validation-20260824`. An unmanaged zero-replica scheduler Deployment (UID `470094cc-3c21-499c-9c79-416ba397fa11`, resourceVersion `13686184`) and its Kubernetes-owned zero-replica ReplicaSet stayed exact at 0/65/130 seconds; CR UID `91219b31-8b3f-43aa-be10-02fc3e511960`, generation/observed `1`, held stable `ResourceCollision`, with no operator-created runtime, retained state, or marker. Deleting only the collision at `17:37:33Z` auto-recovered without a CR edit or operator restart; bootstrap succeeded at `17:39:53Z`, and scheduler became available at `17:40:03Z`.

The live scheduler matched the frozen owner-referenced one-replica `Recreate` contract at exact digest `sha256:45bea9e...a73c41`, direct command/configuration, UID/GID `42434`, restricted security, projected `/etc/coriolis`, and writable `/tmp` and `/var/log/coriolis`, with no locks mount, Service, ports, probes, PVC, init container, or resources. Fixed-output scheduler diagnostics and expected empty-worker `NoWorkerServiceError` passed repeatedly. Normal scheduler Pod replacement completed in 28 seconds at the same imageID and zero restarts; normal RabbitMQ Pod replacement recovered in 38 seconds while scheduler and conductor identities remained stable and RPC checks reconverged.

Normal CR delete/recreate changed CR UID `91219b31-...` to `cd348d77-...`, preserved all five retained ownerless state Secrets and both Bound PVCs exactly with no writes, recreated every ephemeral owner under the new UID, converged in 75 seconds, and passed scheduler RPC again. Final conditions were `Accepted=True`, `Reconciled=True`, `Degraded=False`, `Ready=False/RuntimeNotImplemented`, `Progressing=False/RuntimeNotImplemented`, and `Upgradeable=False`. Normal cleanup removed CR-owned resources in 38 seconds, the Application in 12 seconds, the namespace in 69 seconds, and both Delete-policy PVs without force, finalizer changes, or individual retained-resource/PV deletion. Final POC scope is absent; shared `argocd/coriolis` is `0.5.26` `Synced/Healthy`, operator `1/1` zero restarts, no CR, and the node is Ready/schedulable.

Post-review validator hardening now rejects declared application-image ports, volumes, or healthchecks, unexpected runtime mounts/tmpfs targets, and mislabelled pull failures. The strengthened exact-image rerun passed 85 stages with 0 failures and `SUMMARY runtime passed 305.963`; graceful shutdown, restart/reprobe, RabbitMQ recovery, and complete auth/Docker cleanup passed. The current full suite is 540 passing tests.

Historical OCI-only checkpoint: the tracked validator qualified the exact `2603.4` scheduler image `sha256:45bea9e...a73c41` and transfer-cron image `sha256:3a44d3b...2b3ea9c` under their direct commands, restricted UID/GID `42434` runtime, read-only generated configuration, and writable `/tmp` and `/var/log/coriolis`. It proved scheduler diagnostics, expected empty-worker `NoWorkerServiceError`, transfer-cron diagnostics after schedule load, restart/reprobe, and RabbitMQ recovery; accepted final evidence was 87 PASS, 0 FAIL, `SUMMARY runtime passed 316.069`, with complete cleanup. Released scheduler `0.5.26` and transfer-cron `0.5.27` supersede this checkpoint for their lifecycle evidence; Keystone-trust and due-transfer execution remain unqualified.

That paragraph records the OCI-only checkpoint; released scheduler `0.5.26` and transfer-cron `0.5.27` accepted Kubernetes POCs above supersede its milestone framing.

The Coriolis conductor slice is released and POC-accepted as `0.5.24`. Implementation source `d7abd778937203c97eb7501c2a2dc1a0c05cd5c4` (`Implement Coriolis conductor reconciliation`) passed the full local gate before publication: 516 unit tests, Ruff lint/format, strict mypy, Helm lint/template, operator container build, the tracked exact-image validator (three passes, final `272.660s`), and `git diff --check`. CIXpress Default pipeline `nd4xu7` (2026-08-23 20:54:39Z-20:55:52Z; top-level and `git-clone`/`kaniko-build`/`helm-update`/`cleanup` all `SUCCEEDED`) published CI release commit `56c0b29dbfc2883633b9cd20578ecae7f124b5a7`, chart/app/operator `0.5.24`. One initial `INPROGRESS` detail carried stale completion metadata and then resolved to consistent terminal success.

The accepted isolated POC ran in context `virt-infra-dev-buc-hq` in namespace/Application `coriolis-conductor-validation-20260823` at exact chart/operator `0.5.24`: Application `Synced/Healthy`, operator `1/1` with zero restarts. An unmanaged conductor Deployment (UID `1759bccc-...`, resourceVersion `13359017`, replicas `0`, ownerless) stayed spec-exact at 0/65/130s; the initial CR (UID `395d9db8-...`, resourceVersion `13359131`, generation/observed `1`) had acceptedVersion absent and stable `ResourceCollision` conditions/RV across 130s with zero runtime or retained writes and an unchanged operator Pod. Conflict deletion at `21:11:07Z` auto-recovered without a CR edit or operator restart: first children about 67s later, the bootstrap Job completed in 74s at about `21:13:35Z`, and convergence occurred about 148s after deletion. Final conditions were `Accepted=True`/`Reconciled=True`/`Degraded=False`, `Ready=False/RuntimeNotImplemented`, `Progressing=False/RuntimeNotImplemented`, and `Upgradeable=False`.

The happy conductor path produced the owner-referenced one-replica `Recreate` Deployment at exact conductor digest `sha256:27495f44...c09c7` with the qualified direct command/configuration, restricted `42434` security, projected configuration, and three writable paths, with no Service/ports/probes/init/resources/PVC; dependencies and API were healthy with the API EndpointSlice on TCP `7667`. An authenticated output-free verifier proved a Keystone-authenticated `GET /v1/{project_id}/endpoints` returned HTTP `200`/list (API→RabbitMQ→conductor→MariaDB) before and after a normal conductor Pod deletion (original Pod UID `7150baef...`, replacement UID `ab6c58d4...` in about 6s, exact imageID, zero restarts, stable 20s); unauthenticated API returned `401`. Normal CR delete/recreate (old UID `395d...` to new UID `4f2fbbb0-...`) preserved all five retained ownerless state Secrets and two retained Bound PVCs exactly (UID/resourceVersion/creation/retention/PVC volume names) with no writes; owner resources recreated under the new UID, convergence about 71s, bootstrap exit `0`/restarts `0`, and a third authenticated verifier plus `401` passed. Normal cleanup removed the CR, the Application absent in about 3s, the namespace absent in about 77s, and exact Delete-policy PVs immediately, without force/finalizer changes or individual retained/PVC/PV deletion; final namespace/Application/CR/PVs/prefix resources are absent. Shared `argocd/coriolis` remains `0.5.24` `Synced/Healthy`, operator `1/1` zero restarts, no CR, node Ready/schedulable.

Pre-release qualification (history): exact image `coriolis-conductor:2603.4@sha256:27495f44fbb8b320098d0aa04cd9dcb2a4b432e57aa17417606efc5403ac09c7` passed the extended tracked validator `scripts/validate-coriolis-bootstrap-runtime.py` three times; the final run completed in `272.660s` (`SUMMARY runtime passed`) with no leftover resources. Restricted direct `/usr/local/bin/coriolis-conductor --config-file=/etc/coriolis/coriolis.conf` ran as UID/GID `42434` with read-only root, dropped `ALL` capabilities, no-new-privileges, read-only generated configuration, and only `/tmp`, `/var/log/coriolis`, and `/opt/coriolis/locks` writable on an internal network with no host port. Local qualification also proved a clean conductor stop/restart and RabbitMQ reconnect without recreation. Local operator reconciliation applies one owner-referenced `Recreate` conductor Deployment (no Service/ports/probes/init/PVC/resources/CRD/RBAC/watch/finalizer) only after a successful immutable common bootstrap and before the marker, with complete preflight and collision-safe guarded SSA. Final local validation passed: 516 unit tests, Ruff lint/format, strict mypy, Helm lint/template, operator container build, tracked runtime validator, and `git diff --check`.

The conductor acceptance scope at that historical checkpoint excluded scheduler. Scheduler is now released and POC-accepted as `0.5.26`, and transfer-cron as `0.5.27`, above; transfer-cron has no Service. Write workflows, nonempty scheduling, Keystone-trust execution, workers/minion/deployer/web, and overall readiness remain unclaimed. Remaining application workloads, then logical Ingress, are the next gates; production CSI/cross-node, backup/restore, HA, RPO/RTO, and rotation remain deferred.

The Coriolis API Service/Deployment slice is released and POC-accepted as `0.5.22`. Implementation source `5a06759d6e0332345262f990b2fb56d9d82fbef7` (`Implement Coriolis API reconciliation`) passed the full local gate immediately before publication: 503 unit tests, Ruff lint/format, strict mypy, Helm lint/template, `docker build -t coriolis-operator:local .`, and `git diff --check`. CIXpress Default pipeline `44muez` (2026-08-23 18:45:00Z-18:46:33Z; top-level and `git-clone`/`kaniko-build`/`helm-update`/`cleanup` all `SUCCEEDED`) published CI release commit `ef86249cecee812482a96da8f41b1985d7cb764b`, chart/app/operator image/tag `0.5.22`. One transient `INPROGRESS` response carried a stale completion timestamp and then resolved to consistent terminal success; all steps succeeded. The isolated POC ran in context `virt-infra-dev-buc-hq` in namespace/Application `coriolis-api-validation-20260823` at exact chart/image `0.5.22`: Application `Synced/Healthy`, operator `1/1` with zero restarts.

Pre-release evidence: exact image `coriolis-api:2603.4@sha256:fce6369f07ef777b5174d3a4f849d4eac914256a20a47ffa0cd1c98081be2705` ran through the direct `/usr/local/bin/coriolis-api --worker-process-count 1 --config-file=/etc/coriolis/coriolis.conf` interface as UID/GID `42434`, with read-only root, dropped capabilities, no-new-privileges, read-only generated configuration, and only `/tmp`, `/var/log/coriolis`, and `/opt/coriolis/locks` writable. One master and one worker remained stable for 30 seconds; output-suppressed `GET /v1` returned the expected unauthenticated `401`; the exact exec probe passed; and normal stop completed in `0.848s` with exit `0`. The legacy Apache command was rejected because its baked module/startup interface did not pass the restricted qualification.

The controller pre-reads and classifies owner-referenced `<appliance>-coriolis-api` Service and Deployment before mutation, after all existing reads. Any API collision is mutation-free; managed resources require resourceVersion-guarded SSA. After a succeeded immutable Coriolis-common bootstrap, reconciliation creates or applies the ClusterIP Service then restricted one-replica `Recreate` Deployment before writing the marker last. The Deployment pins the exact digest, direct command, projected `/etc/coriolis` configuration, protocol probes, and evidenced writable paths; no credentials enter environment or metadata. Existing Service/Deployment RBAC is sufficient, with no CRD, watch, finalizer, Ingress, destructive behavior, or readiness expansion. Full local validation passes: 503 unit tests, Ruff lint/format, strict mypy, Helm lint/template, container build, and `git diff --check`. `Ready=False/RuntimeNotImplemented` remains truthful.

Isolated acceptance evidence in `coriolis-api-validation-20260823` at exact chart/image `0.5.22`: an unmanaged API Service (UID `c70790a3-c791-4477-9e43-aaac5f1351be`) held for over 65s with exact UID/resourceVersion/creation/spec unchanged and no owner refs; the initial CR (UID `b453a56b-e407-4942-b2fc-57aab99a0508`, generation `1`) had acceptedVersion absent, `Reconciled=False/ResourceCollision`, no status churn, and zero CR-owned or new retained writes. Conflict deletion at `18:57:46Z` auto-recovered without a CR edit or operator restart in about 127s; the bootstrap Job completed in 77s; all dependencies `1/1`. The happy API path produced the owner-referenced Service/Deployment: ready EndpointSlice on TCP `7667`, one-replica `Recreate` Deployment at exact API digest `sha256:fce6369f07ef777b5174d3a4f849d4eac914256a20a47ffa0cd1c98081be2705` running the direct binary under restricted UID/GID/fsGroup `42434` with read-only root, projected `/etc/coriolis` configuration, and evidenced writable paths; API Pod Ready with zero restarts; Service-DNS `/v1` returned the exact unauthenticated `401`; marker last; final `Accepted=True`/`Reconciled=True`/`Degraded=False` and `Ready=False/RuntimeNotImplemented`.

A deployment collision/recreation exercised retention: the old CR was deleted normally and five retained state Secrets plus the MariaDB/RabbitMQ PVCs remained exact no-write. The new CR (UID `8aead0cb-3dd3-4002-95cb-b14de37a71b3`) hit an unmanaged zero-replica Deployment (UID prefix `8407babd`) that held about 70s unchanged with no Pod/status churn or new CR-owned writes at `Reconciled=False/ResourceCollision`; conflict deletion at `19:04:40Z` auto-recovered in about 73s with exact retained metadata/resourceVersions/PVC volumes unchanged and bootstrap/dependencies/API healthy under the new owner UID. A normal API Pod deletion produced a distinct-UID replacement in about 5s with the same exact imageID, zero restarts, ready endpoint, and `401`. Normal cleanup removed CR-owned children in 5s and the exact Application, namespace, and both Delete-policy PVs normally without force/finalizer patches or individual retained/PV deletion; final exact Application/namespace/CR/PVs are absent. Shared `argocd/coriolis` resolves `0.5.22`, `Synced/Healthy`, operator `1/1`, no CR, node Ready/schedulable.

The API slice is accepted released-artifact evidence, and the conductor slice is released and POC-accepted as `0.5.24` (recorded above). At that checkpoint, write workflows/diagnostics and scheduler/workers/transfer-cron/minion/deployer remained absent. Deployer-manager is subsequently released and POC-accepted as `0.5.29` above; `Ready=False/RuntimeNotImplemented` remains required. Privileged worker and remaining application workloads, then Ingress, follow. Production CSI/cross-node, backup/restore, HA, RPO/RTO, and rotation remain deferred.

Release `0.5.20` is accepted. Corrective source `5f52c7004b3b393d85c10b07e9301d3fa3587164` (`Continue collision recovery retries`) was published by successful Default pipeline `0km4kz` (15:10:28Z-15:11:36Z; top-level and `git-clone`/`kaniko-build`/`helm-update`/`cleanup` all `SUCCEEDED`) as CI release `61c7b3a0d0cea2e81b290023fa8ee5605e2ff261`; the POC operator imageID was `sha256:8d537d978783496338ba4bda60d412690b9c3bd24f6187e31ee8d1de4735e190`. Full local validation passed: 490 unit tests, Ruff lint/format, mypy, Helm lint/template, container build, and `git diff --check`.

In fresh isolated `coriolis-collision-recovery-20260823`, exact chart/image `0.5.20` held an unmanaged immutable bootstrap ConfigMap collision for 135s (over two timer intervals) without churn: its UID/resourceVersion/creation/immutable/hash were unchanged; CR `90548df2-6321-409e-98fb-7130975200e5` remained generation `1`, resourceVersion `13263783`, `Reconciled=False/ResourceCollision`; operator Pod `076dc2e0-583e-45c8-ab44-eb1f56ec7df7` remained zero-restart; and no appliance children existed. After deletion at `15:25:20.744Z`, recovery started at `15:26:12.507Z` (51.763s) and completed at `15:27:40.726Z` (139.982s total) without a user CR/spec change, generation change, or operator restart. The corrective retry crossed `BootstrapRunning` and continued: the final CR retained UID/generation/observed generation `1`, reported `acceptedVersion=2603.4`, `Accepted=True`, `Reconciled=True`, `Degraded=False`, `Ready=False/RuntimeNotImplemented`; the Job succeeded at `15:27:34Z` (`1` succeeded, `0` failed, Pod exit `0`, restarts `0`); dependencies and four Services/ready EndpointSlices were `1/1`; the managed immutable replacement ConfigMap had a distinct UID and correct ownership; and the operator-state marker was present. Child RBAC remains without list/watch/delete. Exact Application `0.5.20` was `Synced/Healthy`.

Normal cleanup removed CR-owned children in 1s, the exact Application in 18s, the namespace in 50s, and both Delete-policy PVs without forced finalizers, individual retained-resource/PV deletion, or leftovers. An initial wrong short Application-name query let Argo recreate the namespace; deleting the exact Application resolved it normally. Final exact Application/namespace/CR/PVs are absent and the node is Ready/schedulable. Shared `argocd/coriolis` target `0.*.*` resolves to `0.5.20`, is `Synced/Healthy`, and its operator is image `0.5.20`, ready/available `1/1`.

Historical root-cause evidence remains unaccepted: `0.5.19` source `c89b57fcd5b2ee160c1a0986be9fedc9edc19ec8`, pipeline `mg04ql`, CI release `3a2af04270762069f3d50905ad6f4255d699c302` recovered automatically in 28s after collision deletion but dropped the timer retry at `BootstrapRunning`; its Job succeeded while the CR remained `Reconciled=False/BootstrapRunning` for over 10 minutes.

Source `764b9952d1e1c9bc1cbce08afddea8781f391f42` (`Fix Coriolis-common bootstrap dbsync`) was published as accepted `0.5.17` by CIXpress pipeline `ectoq4`, started `2026-08-23T12:16:43Z` and completed `12:18:06Z`; top-level and `git-clone`/`kaniko-build`/`helm-update`/`cleanup` all `SUCCEEDED`. CI commit/release is `6bfc494d50949b7a5aa770c4febb7c5100b3b363`; the operator imageID is `sha256:443e6e5dec8cd6e7f2040ca4fe1f5dcfcfa40ad36184cb30dc54a4be7547d8a6`. Historical `0.5.16` v1 failed before dbsync on provider-continuation parsing; `BootstrapFailed`, no marker, and no mutation were truthful.

The accepted v2 Job ran in place from `12:23:11Z` to `12:23:42Z` (31s), `succeeded=1`, `failed=0`, exit `0`, restarts `0`, on the exact conductor digest. The v1 Job remained failed and untouched; dependencies were `1/1` with one endpoint each. The marker/status were correct: `acceptedVersion=2603.4`, `Accepted=True`, `Reconciled=True`, `Degraded=False`, and `Ready=False/RuntimeNotImplemented`. Independent output-suppressed exact-image verification passed twice (10s each, including post-recreation), confirming required Coriolis schema tables, one unique enabled Coriolis user/default service project/password auth/admin assignment, exact migration service, and exactly one RegionOne admin/internal/public endpoint each; no secrets, logs, or raw data were emitted. Completed v2 Job/immutable ConfigMap reuse on real operator resume preserved UID/resourceVersion/creation/completion and Job Pod identity; replacement operator readiness was 4s with zero restarts.

Normal CR deletion/recreation proved retention but exposed a recovery nuance: the unmanaged immutable v2 ConfigMap collision retained exact UID/resourceVersion/time/key/hash, yielded `ResourceCollision`, and caused zero newly owned writes; all five retained ownerless Secrets and MariaDB/RabbitMQ PVCs retained exact UID/resourceVersion/creation, ownerless retention, and Bound PVC state. Removing only the conflict did not reconcile because metadata/child resources are not watched. A normal operator Pod replacement invoked Kopf resume (replacement 6s), then convergence completed in 124s. This is an operational follow-up, not automatic collision recovery. Final recreated state was v2 only, v1 absent, dependencies healthy, Job successful, and the exact independent verification passed. Cleanup observed the CR for 300s only because expected retained ownerless Secrets remained after CR deletion; Application deletion took 11s, namespace 53s, and both Delete-policy PVs 9s. POC application, namespace, and PVs are absent. Shared `argocd/coriolis` target `0.*.*` resolves to `0.5.17` and is Synced/Healthy; shared operator is `1/1` with zero restarts and the node is Ready/schedulable. Local validation remains 477 tests, Ruff, mypy, Helm, parser, Docker build, and diff; exact renderer runtime was `135.212s`. No application workload, Ingress, or `Ready=True` is claimed. Single-node local-path only; CSI/cross-node, backup/restore, HA, RPO/RTO, rotation, and production storage remain open. The next implementation milestone is backend/application Services and workloads, then Ingress.

Previous accepted dependency milestone: released `0.5.14` passed the accepted isolated single-node Keystone released-artifact POC. Implementation source `f90cae4` was published by CIXpress pipeline `c2pajn` (all expected steps `SUCCEEDED`) as `0.5.13` at CI commit `7eb2215`; its POC was not accepted because the Keystone prepare init failed before db-sync/bootstrap — non-root UID/fsGroup `42425` could not chmod the root-owned emptyDir mount roots during `install -d -m 0700` while MariaDB/RabbitMQ/Memcached stayed healthy. Fix source `087bbc2` (`Fix Keystone emptyDir preparation`, 398 unit tests plus Ruff lint/format, mypy, and `git diff --check`) was published by pipeline `fhzlg6` (all four expected steps `SUCCEEDED`) as accepted release `0.5.14` at CI commit `edd349a`. At POC cleanup, Argo ran `coriolis-operator:0.5.14`, `1/1` Synced/Healthy with zero restarts and no appliance CR; no POC workload remained. `Ready=False/RuntimeNotImplemented` remains truthful overall.

A direct local Helm OCI pull returned HTTP `401` before any disposable Helm release or CR was created, so the exact `0.5.14` chart was safely promoted by a disposable Argo Application in project `default` using the existing registry integration, an existing compatible CRD, and `skipCrds`; no credentials were exposed. The accepted isolated single-node Keystone POC reached Keystone `Available` about 90 seconds after CR creation; the prepare, db-sync, and bootstrap init containers exited `0`; `/v3` returned 200 and an authenticated token request returned 201 with a token header present on the restricted direct-WSGI one-replica `Recreate` Deployment, with ready Service/EndpointSlice and zero restarts across all workloads. This is historical `0.5.14` evidence; later `0.5.17` Coriolis-common acceptance is recorded above.

On 2026-08-22, source `6a5a2b589c0dbfc2f5734f5863f9f8591c5f8c2d` was published by successful Default pipeline `qhvqt1` (13:59:46-14:01:07 UTC) as CI release `c48fd79622a8e760591333bf1ab6a0aa25d2f9d3`, `0.5.10`. Its isolated bootstrap/authenticated-AMQP POC was not accepted: three sequential readiness diagnostics at period `5s`/timeout `5s` produced 37 timeout failures and Endpoint flapping despite zero restarts. Fix source `0d52c57aea1345213e519622106bb7b78236c0f1` changes readiness to period `10s`/timeout `15s`; full validation is 355 tests plus Ruff lint/format, mypy, Helm lint/template, and diff check. Successful Default pipeline `4nj6f5` (14:14:34-14:15:38 UTC) released CI commit `3985a677100a55844dc07ac74a30a24e1e2b03e0`, chart/app/image `0.5.11`.

The accepted `0.5.11` POC in isolated namespace `coriolis-rabbitmq-validation-20260822` used operator imageID `sha256:fbc0cd338f79bf8a7ea06446f05210859ce549b387371594eee02b0ddef8a724`, the approved Rabbit digest, approved helper Coriolis API digest prefix `fce636...2705`, and single-node `local-path`. Probe-fix rollout replaced the Rabbit Pod and produced eight consecutive 15-second Ready/endpoint samples; a durable AMQP marker survived. Normal Pod deletion replaced the Pod in 4s, reached Ready in 29s with zero restarts and the same digest, then remained stable for four 15-second samples; the marker survived. Normal CR deletion removed owned resources in 32s while retaining Rabbit/Maria PVCs and credential Secrets with exact UIDs and unchanged resourceVersions; recreation reached Rabbit StatefulSet in 2s and Ready in 31s, remained stable for four samples, preserved/authenticated the durable marker, and established new owner UID references. The final clean-storage smoke created its CR at 15:20:37Z, reached Rabbit StatefulSet in 59s and Ready in 94s, used a fresh ownerless Bound Rabbit PVC and distinct PV, had zero restarts/no readiness timeouts/four stable samples/ready endpoint, and passed Service-DNS authenticated AMQP durable publish/consume/ack and queue deletion. `Accepted=True` and `Reconciled=True`; `Ready=False` remains truthful.

Normal cleanup removed owned resources in 20s, the namespace in 78s, and final Rabbit/Maria Delete-policy PVs in 1s; earlier POC PVs, release, helpers, and namespace are absent. This is historical RabbitMQ `0.5.11` evidence.

On 2026-08-22, tracked validator `scripts/validate-keystone-runtime.py` completed exact-image standalone validation in `69.063s`. Dedicated database/user creation, least-scope grant, repeated SQL, TCP authentication, table access, unrelated-database rejection, normal MariaDB stop, retained-data restart, `db_sync`/repeat/check, Fernet and credential setup/repeat, exact `0`/`1` key metadata, file-only idempotent admin bootstrap, direct non-root WSGI, `/v3`, HTTP `201` token authentication, normal stop, retained-state restart, and repeat authentication all passed. WSGI first/restart health took `5.628s`/`5.720s`; authentication took `1.480s`/`1.618s`; stops took `0.297s`/`0.277s`. All disposable resources and generated values were removed without emitting credentials, SQL values, DSNs, tokens, keys, headers, bodies, raw logs, or environments. Installed source confirms exact stdlib-compatible initial key generation. The frozen development contract selects three retained Secrets, one restricted `Recreate` Deployment, idempotent init containers, direct WSGI, no PVC, no Job, no new RBAC verbs, and no rotation claim. No reconciliation code, CRD, Helm version, release, deployment, or cluster state changed.

The Keystone reconciliation slice implements the frozen contract: three collision-safe ownerless retained Secrets with absent-only generation and exact no-write reuse; owner-referenced generated ConfigMap/Secret and one restricted direct-WSGI `Recreate` Deployment; MariaDB dedicated database/user bootstrap plus a fixed rollout annotation; six complete pre-reads before mutation; resourceVersion-guarded SSA; and writes ordered as foundational resources and Services, absent Keystone retained Secrets, MariaDB, RabbitMQ, Memcached, Keystone generated resources, then the marker. Retry and collision status remains value-safe, existing RBAC is sufficient, and `Ready=False/RuntimeNotImplemented` remains expected. Full validation passed with 397 unit tests, Ruff lint/format, mypy, Helm lint/template, and `git diff --check`. That slice is now released as `0.5.14` with accepted released-artifact POC evidence (recorded above); no CRD or RBAC changed.

The completed Python 3.12 Kopf skeleton reconciled a namespace-scoped owned marker ConfigMap with collision-safe naming and server-side apply. Its successful reconciliation set status while `Ready=False/RuntimeNotImplemented` remained expected. It has no finalizers and performed no destructive work; this marker-only behavior is completed lifecycle history, not the next runtime target.

Live-cluster installation and full controller lifecycle validation were completed on release `0.5.2` in the approved `coriolis` namespace; that complete scenario was not repeated for later releases. CIXpress observation in `cixpress` remains read-only. The historical `0.5.2` sample `example` completed create, controller replacement/resume, `spec.version` update, and normal deletion lifecycle checks; its owned `example-operator-state` marker was garbage-collected. The later Keystone POC cleanup observed healthy Argo `0.5.14` with no appliance CR. `Ready=False/RuntimeNotImplemented` is expected because the appliance runtime is incomplete.

The chart configures logging, liveness, security context, resource defaults, and the `regcred` image pull secret. Image pull, CRD installation, operator startup, liveness, and Argo deployment health are validated through released `0.5.14`; full historical controller lifecycle behavior remains validated on `0.5.2`. Argo has removed the legacy selector overlap, and the isolated released-artifact Memcached, accepted RabbitMQ, and accepted Keystone POCs are complete.

The runtime architecture decision is recorded: `CoriolisAppliance` will deploy the Kubernetes-native `core` Coriolis stack directly in `coriolis`, not an external VM. RC4 failed (Build `868` exported an OVA but no `registry.cloudbase.it/appliance/coriolis-*` repository carries a `2608*` tag), so RC4 is blocked/OVA-only and must not be used. The approved fallback is exact official release `2603.4`; the metadata and pull gates are complete. The development stack through MariaDB reconciliation is published on `origin/dev` at `55212b0` and included in released operator `0.5.6`, but no `CoriolisAppliance` or Coriolis core runtime workload remains deployed. CIXpress CI/release behavior is documented and the integration runs externally, but its exact configuration and manifests are not stored in this repository. Open project details: promotion policy, exact CIXpress integration files/templates/triggers/credentials, Argo CRD pre-upgrade automation, and licensing as applicable. Older entries below retain their pre-publication history.

On 2026-08-22, local, unpushed, and undeployed MariaDB evidence advanced: the exact approved digest was inspected and direct restricted non-Galera `mariadbd` startup, value-safe idempotent bootstrap, authenticated TCP validation, and volume recreation were proved. `kolla_start` was rejected because its configuration and password handling violate the value boundary. At that evidence stage, MariaDB remained not implementation-eligible pending workload, storage/PVC, manifest, probe, resource, lifecycle/recovery, and reliable log-capture decisions; the later contract paragraph below supersedes that design status. No source/runtime code, CRD, workload, PVC, Job, RBAC, probe/readiness, release/version, deployment, or cluster state changed; deployed `0.5.3` remains marker-only.

On 2026-08-22, the MariaDB reconciliation slice wired the previously pure desired state into `main.py`; it is published on `origin/dev` at `55212b0` and included in released operator `0.5.6`. Missing or invalid complete MariaDB settings return stable mutation-free `InvalidRuntimeConfiguration` status before Kubernetes client construction while preserving accepted-version state. Valid reconciliation preserves the existing marker/foundational/four-Service read prefix, then reads PVC, ConfigMap, configuration Secret, and StatefulSet; all preflight and resource-version checks finish before writes. It creates an absent retained PVC or performs exact no-write reuse, guarded-applies the managed ConfigMap, Secret, and StatefulSet, and writes the marker last. Storage/resources field handlers are present; RBAC adds only PVC `get`/`create` and StatefulSet `get`/`create`/`patch`. `Ready=False/RuntimeNotImplemented` remains truthful. Validation passed before publication: 316 unit tests, Ruff lint/format, mypy, Helm lint/template, and `git diff --check`.

On 2026-08-22, source commit `063e438ef416599e9816a2400afcc5a5a7af9aa0` ("Implement Memcached reconciliation") was published by CIXpress pipeline `4dcpfk` (`Default`, 12:29:20-12:30:25 UTC), with top-level, `git-clone`, `kaniko-build`, `helm-update`, and `cleanup` all `SUCCEEDED`. CI-owned commit `cb6b055eaf5e74c99e26c1c3d662b2d749331627` released chart/app/image `0.5.8`. The released operator imageID was `sha256:9af4b018c2a7c0a23635d115d5335477b17bb81a731979bd0c93083c88461af4`. The released-artifact single-node POC passed in disposable namespace `coriolis-memcached-validation-20260822`: CR-to-Deployment took 44s, Deployment-to-Ready 3s, and MariaDB and Memcached reached `1/1`. The Memcached Pod resolved to approved digest `sha256:746b93082a4f6d07f464e93d4b14f5e30510abf17a9ae0a4af20e111408c8f1e`; the Deployment, Service, EndpointSlice, protocol, replacement, ephemerality, and normal cleanup contracts passed. `Ready=False/RuntimeNotImplemented` remains expected. This is historical `0.5.8` POC evidence; the later Keystone POC cleanup observed Argo `0.5.14`.

Before publication on 2026-08-22, local RabbitMQ reconciliation was implemented, uncommitted, unpushed, undeployed, and runtime-incomplete. The approved image is `cr.virtomat.io/virtomat/coriolis/rabbitmq:2023.1-ubuntu-jammy@sha256:a595bf6f306ded2b6ad01f068ef69255df72eb73d471ba73ce9bbf0470d15d8a`; local image ID is `sha256:f9e28ef3ed172cfdda9e6c3d56c509ceaee672b516381343244ed40332a19e73`. Local Docker evidence proved Linux/amd64 Kolla `16.6.1`, UID/GID `42439` without a supplemental group, direct `/usr/sbin/rabbitmq-server` and `/usr/sbin/rabbitmq-diagnostics`, plaintext `0.0.0.0:5672`, console-only logging, read-only root, dropped `ALL` capabilities, no-new-privileges, and writable `/var/lib/rabbitmq`, `/run/rabbitmq`, and `/var/log/rabbitmq`. The default `dumb-init --single-child --` plus `kolla_start` path is rejected without configuration. File-only bootstrap retains the infrastructure Secret key as a mounted file, uses a random 4-byte salt, streams Rabbit SHA256 definitions without credential/hash argv, environment, logs, or output, creates user `openstack` on vhost `/` with exact permissions, and keeps ephemeral definitions mode `0600`. Two retained-volume local launches reached Ready in 15.024s and 13.533s; sanitized running/local-alarm/listener and user/vhost/permission checks passed, state and a marker reconverged, SIGTERM exited `0` in 6.580s and 6.757s, and disposable Docker artifacts were removed. The selected operator contract is a retained single-node StatefulSet with optional explicit `spec.storage.rabbitmq` and `spec.resources.rabbitmq`, a separate ownerless retained RWO Filesystem PVC, owner ConfigMap, restricted one-replica StatefulSet, existing Service/Secret references, direct script/probes/settings validation, collision-first preflight, ordered reads/writes, guarded SSA, PVC no-write reuse, no new RBAC verbs, and marker last. Writes order MariaDB, RabbitMQ, then Memcached. Full local validation passed: 355 unit tests, Ruff lint/format, mypy, Helm lint/template, and diff check. The accepted released `0.5.11` POC supersedes this pre-publication stage; `Ready=False/RuntimeNotImplemented` remains truthful and Keystone runtime evidence is next.

An isolated `0.5.5` POC on single-node `local-path` storage exposed anonymous MariaDB accounts created by the default test-database initialization; their host-specific matches shadowed `coriolis@%`, so clean first-boot readiness failed. Commit `3ee5d2d` adds `mariadb-install-db --skip-test-db`; CIXpress pipeline `kpv306` completed all expected steps and released `0.5.6` at CI-owned commit `647c63b`. A clean isolated `0.5.6` first boot reached PVC Bound in 4 seconds and Pod Ready in 17 seconds with zero anonymous accounts, no `test` database, file-authenticated admin/application checks, and no repair SQL. The released-artifact POC then proved UID/GID/fsGroup `42434` write access, authenticated probes, an ownerless retained RWO Filesystem PVC, exact no-write PVC and retained-credential reuse across CR recreation, persisted database state, and normal pod deletion in 12 seconds followed by healthy same-node remount. All disposable resources and the `Delete`-policy PV were removed. The full 316-test suite, Ruff lint/format, mypy, Helm lint/template, and diff checks pass. CSI and cross-node attach/detach remain unvalidated; production backup/restore, HA, and RPO/RTO acceptance remain open.

On 2026-08-21 the pure contract/input migration was completed and **committed locally at `e2ddb30` on `dev`, but not pushed or deployed**. It derives deterministic RabbitMQ, Memcached, MariaDB, and Keystone Service names; fixes API bind `0.0.0.0:7667`, configuration path `/etc/coriolis`, and VixDiskLib log path `/var/log/coriolis/vmware-root`; and updates Kubernetes-derived templates for plaintext RabbitMQ `5672`, Keystone HTTP `5000`, and HTTP WSGI without internal CA/TLS directives. Initial pure APIs/preflight retain two credential Secrets plus the owned config ConfigMap/Secret and exclude Step CA. The slice changes the CRD ingress schema/sample and adds pure validation for `coriolis.app.cloudbase.wiki`, class `nginx`, cert-manager `letsencrypt` with derived `<host>-tls` Secret name, or an existing same-namespace TLS Secret without a cert-manager annotation. Validation passed: 218 unit tests, Ruff lint/format, strict mypy, Helm lint/template, `git diff --check`, byte parity for all 23 immutable copied templates, and offline wheel inspection of all 27 packaged template resources. It adds no `main.py`, runtime Kubernetes I/O, SSA/RBAC, actual Service/Ingress/workload resources, or release/chart/image/deployment behavior. Internal Services are planned as trusted-network ClusterIP plaintext; a later logical Ingress will route through community ingress-nginx, which owns public TLS/redirects. The deployed `0.5.3` remains marker-only.

On 2026-08-20 the local API-only `core` runtime slice was implemented and validated. This API slice is committed locally at `ab9df83` on branch `dev` (ahead of `origin/dev` by one commit) but is not pushed or deployed: it is absent from the deployed marker-only `0.5.3`. The `v1alpha1` CRD now has optional/defaulted `spec.profile: core` (enum only `core`), required non-empty `spec.version`, and optional non-empty `status.acceptedVersion`. No CEL/admission immutability was added; the controller enforces the immutable accepted version from the persisted `status.acceptedVersion`. Initial acceptance supports exact `2603.4`, and an omitted profile defaults to `core`; unsupported initial profiles/versions apply no Kubernetes resources and report rejection conditions. A requested version different from `status.acceptedVersion` applies no resources, preserves the accepted state, advances `observedGeneration`, and reports `Accepted=False/VersionChangeRejected`, `Reconciled=False`, and `Upgradeable=False/UpgradeBlocked`. A valid API-only reconcile records only the owned controller-state ConfigMap (`acceptedVersion`, `profile`, `generation`) and reports all six conditions (Accepted, Progressing, Reconciled, Ready, Degraded, Upgradeable); `Ready=False/RuntimeNotImplemented` remains truthful, and profile changes route through the same reconcile path. The sample now uses `profile: core`, `version: "2603.4"`. 25 tests pass; Ruff and mypy pass; Helm lint/template pass. No cluster or external service was changed by this API slice, and the image mirror/pull gate remains passed. No dependencies, bootstrap Jobs, services, storage, secrets, or Coriolis runtime workloads have been implemented or deployed.

The first Milestone 4 deliverable is a documentation-only contract slice: the [Foundational Resource Contract](docs/foundational-resource-contract.md) freezes deterministic naming (a conservative single 63-character DNS-label shape with the existing hash-overflow principle), standard labels and a label-safe appliance identity token, collision handling, ownership/deletion/retention (owner-referenced ephemeral resources vs. retained PVCs/CA/state credentials and never-mutated external Secrets, with no destructive finalizer), three Secret/configuration classes plus an explicit sensitive-rendered-configuration rule, and a dependency evidence inventory with a proposed implementation ordering requiring approval. It is based on authoritative local sources (`coriolis-docker/coriolis_ansible` playbook/roles/templates/group vars, `coriolis-docker/passwords.yml.sample`, and `coriolis-oss` systemd/config sources). Follow-on contract, builder, generator, semantic-validation, rendering, pure preflight, and marker-plus-four runtime slices complete the foundational gate locally; the four-Service slice is committed locally at `797235b` on `dev`, unpushed and undeployed, and implements RabbitMQ, Memcached, MariaDB, and Keystone Services, while remaining Services, Ingress, workloads, storage, probes, readiness, bootstrap, and rotation remain deferred. No credential values are included.

A local metadata-only helper slice is now implemented and validated: `appliance_resource_name`, `appliance_identity`, and `build_resource_metadata` in `src/coriolis_operator/reconcile.py` produce label-safe names/identity and standard metadata (standard `app.kubernetes.io/*` and `coriolis.cloudbase.it/*` labels, full appliance-name annotation, and exactly one lifecycle mode — owner reference or retention annotation). `build_state_config_map` now uses this standard metadata with component `operator-state` while deliberately retaining the shipped `state_config_map_name` (preserving `0.5.2`/`0.5.3` names including dotted/long DNS-subdomain behavior); marker data, the SSA call, `force=True`, API behavior, and status are unchanged. 44 unit tests pass; `uv run ruff check .`, `uv run ruff format --check .`, and `uv run mypy src` pass; `git diff --check` is clean. This helper slice is committed locally at `fbab6e5` on `dev`, but not pushed or deployed; the deployed marker `0.5.3` is unchanged and carries no standard labels. Collision pre-read/enforcement and legacy marker migration for the marker ConfigMap are now implemented locally (described below); retained-resource adoption and all runtime resource construction remain deferred. The later builder, generator, semantic-validation, non-sensitive rendering, pure preflight, sensitive-rendering, and multi-resource semantics slices close their policy and pure gates; a MariaDB vertical slice remains blocked by collision-safe runtime reads/create/guarded SSA for the marker plus four foundational resources, minimal Secret/ConfigMap RBAC, status-then-Kopf-retry wiring, and exhaustive tests.

On 2026-08-21 the local collision/migration marker API-layer slice was implemented and validated and is **committed locally at `d8df00f` on `dev`, but not pushed or deployed**; the deployed `0.5.3` remains marker-only and lacks these semantics. Reconciliation now pre-reads the deterministic marker ConfigMap before SSA: a 404 creates normally; a fully matching managed marker (standard `app.kubernetes.io/*`/`coriolis.cloudbase.it/*` labels, appliance-name annotation, matching controller owner reference) reconciles; and a compatible legacy `0.5.2`/`0.5.3`-shaped marker (no management signature, matching controller owner reference, compatible `acceptedVersion`/`profile`) is normalized in place under the unchanged shipped marker name with its stale generation updated. Partial/conflicting standard metadata, owner mismatch, incompatible legacy data, or owner-plus-retention metadata is a `ResourceCollision`: the object is not patched, adopted, deleted, or renamed, and status keeps all six conditions with `Accepted=True`, `Progressing`/`Reconciled`/`Ready=False`, `Degraded=True`, and `Upgradeable=False/UpgradeNotSupported`; a prior `acceptedVersion` is preserved but not newly established on an initial collision. Non-404 Kubernetes read failures still propagate; SSA content type, field manager, `force=True`, marker data, naming, and patch-error behavior are unchanged. ConfigMap RBAC gains only `get`. Validation passed: 70 unit tests; `uv run ruff check .`; `uv run ruff format --check .`; `uv run mypy src`; `helm lint helm/`; `helm template coriolis-operator helm/ --include-crds`; `git diff --check`.

On 2026-08-21 the pure retained-resource authorization/classification slice was implemented and validated locally and is **committed locally at `1b73045` on `dev`, but not pushed or deployed**; the deployed `0.5.3` remains marker-only and unchanged. It adds `classify_retained_resource` to `src/coriolis_operator/reconcile.py`, returning `RetainedClassification.ABSENT/REUSE/COLLISION`, plus `EXTERNAL_READ_ONLY_RESOURCES`. An absent resource is eligible for creation; a retained resource (PVC, state Secret, CA state) is reused automatically only when its deterministic name/namespace and every operator-controlled identity field match exactly (full appliance-name annotation, standard managed/identity labels, component label, exact retention annotation/class), with **no owner references** (owner plus retention is a collision even if an owner UID matches); unrelated extra labels/annotations are permitted; missing/partial/conflicting identity metadata is a collision and is never normalized; a matching ownerless retained object is `REUSE` with no mutation or adoption patching. The creating appliance CR UID is deliberately **not** part of the identity: retained resources survive CR deletion/recreation, so automatic exact-match reattachment works even when the CR UID changes, and any stale `coriolis.cloudbase.it/appliance-uid` annotation is ignored as unrelated. It is a namespace trust boundary (in-namespace users can forge identity metadata). External/pre-existing resources, especially the registry pull Secret `coriolis-appliance-registry`, fail closed as `COLLISION` (even absent or with forged matching metadata) and remain read-only and outside this classifier/reconciliation policy. The classifier is pure and works with mapping-shaped fakes and real `V1Secret`/`V1PersistentVolumeClaim` model objects. Validation passed: 94 unit tests; `uv run ruff check .`; `uv run ruff format --check .`; `uv run mypy src`; `helm lint helm/`; `helm template coriolis-operator helm/ --include-crds`; `git diff --check`. **No runtime resource is constructed, reconciled, patched, read, or adopted, and no adoption mutations exist.** A MariaDB vertical slice remains blocked by the remaining Secret/configuration/storage/readiness gates. Prior local commit references remain accurate: API slice `ab9df83`, metadata helper `fbab6e5`, status docs `6bf5cb5`, collision/migration slice `d8df00f`.

On 2026-08-21 a documentation-only Secret/configuration contract slice was added to the [Foundational Resource Contract](docs/foundational-resource-contract.md), freezing the foundational Kubernetes Secret/ConfigMap names, key layouts, and the primary `coriolis.conf` split using only known source key names. Retained, ownerless Secrets: `<appliance>-coriolis-credentials` (`coriolis_database_password`, `coriolis_keystone_password`, `temp_keypair_password`), `<appliance>-infrastructure-credentials` (`database_password`, `rabbitmq_password`, `keystone_admin_password`), `<appliance>-step-ca-credentials` (`init_password`). Owner-referenced, rebuildable: ConfigMap `<appliance>-coriolis-config` (exactly `coriolis-api.wsgi`, `wsgi-coriolis.conf`, `vixdisklib.conf`, `api-paste.ini`, `policy.yml`, `coriolis.release`) and Secret `<appliance>-coriolis-config-secret` (exactly `coriolis.conf`), mounted together as a read-only projected volume at `/etc/coriolis` with explicit `items` paths, no credential environment variables, and no `subPath`. The later pure builder slice at `050f16e` closes the concrete manifest-builder gate. Remaining gates are credential generation/value-source policy, collision-safe Secret/ConfigMap pre-reads, SSA, minimal RBAC, and reconciliation; TLS/optional credentials/storage/probes/readiness/rotation remain deferred. This contract slice is **committed locally at `8ce26ba` on `dev`, but not pushed or deployed**.

On 2026-08-21 the pure Secret/ConfigMap builder slice was implemented and validated locally and is **committed locally at `050f16e` on `dev`, but not pushed or deployed**. `src/coriolis_operator/reconcile.py` adds `build_coriolis_credentials_secret`, `build_infrastructure_credentials_secret`, `build_step_ca_credentials_secret`, `build_coriolis_config_map`, and `build_coriolis_config_secret`. They use deterministic names and standard metadata: retained credential Secrets are ownerless with retention metadata, and rebuildable configuration resources are owner-referenced without retention. Exact frozen key sets are enforced; caller-provided opaque string inputs are not mutated; missing, extra, or non-string inputs fail without exposing values. Secrets are `Opaque` with UTF-8/base64 `data` and no `stringData`; ConfigMap data remains plain and limited to the six approved files, never `coriolis.conf` or credentials. Validation passed: 116 total tests (22 new cases from the previous 94; 21 matched the final focused selector), Ruff lint/format, mypy, Helm lint/template, and `git diff --check`. This is pure manifest construction only: no credential generation, `main.py` reconciliation, Kubernetes reads/SSA, RBAC, CRD, runtime resource creation, status/readiness, or deployment change. Deployed `0.5.3` remains marker-only. The next gate is credential generation/value-source policy, followed by collision-safe Secret/ConfigMap pre-reads, SSA, minimal RBAC, and reconciliation; TLS/optional credentials/storage/probes/readiness/rotation remain deferred.

On 2026-08-21 the pure retained credential generation slice was implemented and validated locally and is **committed locally at `a604579` on `dev`, but not pushed or deployed**. `generate_coriolis_credentials`, `generate_infrastructure_credentials`, and `generate_step_ca_credentials` independently generate the seven frozen credential keys through `secrets.token_urlsafe(32)` (32 random bytes/256 bits, URL-safe opaque strings). Injected token factories support deterministic tests only; empty/non-string results fail value-safely. Values compose unchanged with the existing builders and must never appear in documentation, logs, status, events, or errors. The policy is operator-generated only: no inline CR credential values and no external credential Secret source; external/pre-existing Secrets remain read-only and outside it. Runtime generate-once/reuse remains unwired: invoke generators only for `ABSENT` retained Secrets, reuse exact matching ownerless Secrets unchanged, fail closed on collisions, and defer automatic rotation. `main.py` does not call generators. Validation passed: 132 total tests (16 new from 116), focused tests, Ruff lint/format, mypy, Helm lint/template, and `git diff --check`. No controller reconciliation, Kubernetes reads/writes/SSA, RBAC, CRD, runtime resources, status/readiness, chart/release, deployment, or rotation changed; deployed `0.5.3` remains marker-only. Next gates are configuration rendering inputs, retained Secret semantic validation, collision-safe pre-reads, SSA, minimal Secret RBAC, and reconciliation/status failure semantics; TLS/optional credentials/storage/probes/readiness/bootstrap/rotation remain deferred.

On 2026-08-21 the pure retained Secret semantic validation/extraction slice was implemented and validated locally and is **committed locally at `5165629` on `dev`, but not pushed or deployed**. `validated_retained_secret_values` accepts mapping-shaped objects and Kubernetes `V1Secret` models; tolerates absent `apiVersion`/`kind` but rejects conflicting present values; requires `type: Opaque`; rejects persisted `stringData`; requires exact frozen `data` keys with string encoded values; strictly base64-decodes then UTF-8-decodes values; and rejects empty decoded values. It returns a new decoded mapping without mutating input, and fixed/category-only errors do not expose encoded or decoded values. It validates semantics only: no metadata classification, Kubernetes reads/writes, generation, SSA, collision/status handling, or reconciliation. Validation passed: 152 total tests (20 new from 132), focused 20 tests, Ruff lint/format, mypy, Helm lint/template, and `git diff --check`. No `main.py`, RBAC, CRD, runtime resource, chart/release, deployment, or rotation behavior changed; deployed `0.5.3` remains marker-only. Next gates are non-sensitive configuration rendering inputs/templates; collision-safe pre-reads of all frozen resources before mutation; metadata classification before semantic validation, with semantic failure mapped fail-closed to `COLLISION`; `ABSENT`-only generation; SSA; minimal Secret RBAC; and reconciliation/status failure semantics. Returned decoded values remain internal and must never be logged, statused, or evented; TLS/optional credentials/storage/probes/readiness/bootstrap/rotation remain deferred.

On 2026-08-21 non-sensitive configuration rendering was implemented and validated locally at **`97153a7` on `dev`, but not pushed or deployed**. Pure `render_coriolis_config` validates bind address, port, configuration directory, VixDiskLib log directory, and accepted version; it renders exactly the six frozen ConfigMap files with Jinja2 `PackageLoader`, `StrictUndefined`, disabled autoescape, and preserved trailing newlines. Six verbatim Apache-2.0 upstream templates are packaged with source/license attribution, and `accepted_version` maps to legacy `default_coriolis_docker_images_tag`. No `coriolis.conf`, provider fragments, credentials, or Secret content is rendered. Validation passed: 19 focused renderer tests, 171 total tests, wheel resource inspection, and template byte comparison. No `main.py`, reconcile, Kubernetes, RBAC, CRD, runtime, release, or deployment behavior changed; deployed `0.5.3` remains marker-only.

On 2026-08-21 the pure foundational five-resource preflight was implemented and validated locally and is **committed locally at `35eac9b` on `dev`, but not pushed or deployed**. `OwnedClassification.ABSENT/MANAGED/COLLISION`, `classify_owned_resource`, credential-safe frozen `FoundationalResourcePreflight`, and `preflight_foundational_resources` in `src/coriolis_operator/reconcile.py` cover exactly three retained credential Secrets and the owner-referenced configuration ConfigMap and Secret. Metadata for all five is classified before retained Secret semantics: exact owner metadata/controller matches are `MANAGED` despite content/type drift, mismatched ownership or retention metadata is `COLLISION`, and unrelated extra metadata is allowed. Metadata collisions stop semantics and generation; `REUSE` retained Secrets are semantically validated with validation `ValueError` mapped to that resource's `COLLISION`; all reusable semantics finish before only corresponding `ABSENT` generators run after a collision-free preflight. Successful results carry generated/decoded credentials but hide them from `repr`; errors, logs, status, and events remain value-safe. Validation passed: 23 focused tests, 194 total tests, Ruff lint/format, mypy, Helm lint/template, and `git diff --check`. This is pure only: no `main.py`, Kubernetes reads/writes, SSA, RBAC, CRD, runtime construction/adoption, reconciliation/status behavior, chart/release/image version, deployment, TLS/storage/readiness/bootstrap/rotation changes. Deployed `0.5.3` remains marker-only.

On 2026-08-21 the sensitive `coriolis.conf` renderer contract was **committed locally at `574efcf` on `dev`, but not pushed or deployed**. At that commit it was documentation-only: it froze the immutable upstream base template and all 16 provider fragments, authoritative provider lists/order/module maps, prohibited custom module overrides, disabled compression/compressor, explicit internal dependency inputs, source-audited identities/paths, credential mappings, strict value-safe validation, and a redacted interface boundary without CRD fields. The complete rendered file was frozen as exactly one key in the owner-referenced configuration Secret, never ConfigMap/log/status/event/metadata/documentation content; all three generated credential Secrets use exactly `coriolis.cloudbase.it/retention: state-credentials`.

On 2026-08-21 the pure sensitive renderer was implemented and validated locally at **`9bb20f3` on `dev`, but not pushed or deployed**. `SensitiveCoriolisEndpoints`, `SensitiveCoriolisCredentials`, redacted one-key `SensitiveCoriolisConfig`, and `render_sensitive_coriolis_config` compose the exact `coriolis.conf` Secret key with the existing builder and reject ConfigMap output. Inputs are frozen/exact and unmutated; credential/output reprs are redacted; errors are fixed/category-only and value-safe. Jinja `PackageLoader`, `StrictUndefined`, disabled autoescape, and trailing newline preserve byte-identical immutable upstream base and all 16 provider fragments with frozen provider lists/order/module maps, source-audited values/paths, source/license attribution, prohibited custom overrides, and disabled compression/compressor. Validation passed: 40 focused configuration tests, 215 total tests, Ruff lint/format, strict mypy, Helm lint/template, `git diff --check`, 17/17 source byte parity, and offline wheel inspection of exactly 25 expected template resources. No `main.py`, reconciliation, Kubernetes reads/writes/SSA, RBAC, CRD, runtime resources/workloads, chart/release/image version, deployment, TLS/CA/bootstrap, provider/private data, optional credentials, storage, readiness, or rotation behavior changed; deployed `0.5.3` remains marker-only. At that point, runtime collision-safe reads/create/guarded SSA, minimal RBAC, status-then-retry wiring, and exhaustive tests were the immediate gate, and runtime integration remained blocked.

The historical [Foundational Resource Contract](docs/foundational-resource-contract.md) records the superseded five-resource barrier and its non-transactional failure policy. Its local, unpushed, and undeployed workload evidence gate freezes the four dependency image identities and source-backed eligibility requirements, with MariaDB first only after its storage/startup/probe facts close. The marker-plus-four foundational runtime gate is committed locally at `862777d`, with status commit `f219977`; both are unpushed and undeployed. The four-Service slice is committed locally at `797235b` on `dev`, unpushed and undeployed, and adds exactly RabbitMQ, Memcached, MariaDB, and Keystone Services; dependency workloads/bootstrap/storage/readiness, Barbican and other Services, and Ingress routes remain deferred. Deployed `0.5.3` remains marker-only.

On 2026-08-21 the marker-plus-four foundational runtime gate was committed locally at `862777d` on `dev`, with its status commit at `f219977`; both are unpushed and undeployed. Reconciliation performs the marker plus four exact ordered pre-reads, treats only `404` as absent, completes metadata-first preflight/rendering before writes, creates or reuses retained `state-credentials` without writing the reuse path, uses resourceVersion-guarded SSA for managed resources, writes the four foundational resources in order with the marker last, and provides no rollback. Retryable failures publish sanitized `ResourceReadFailed`, `ResourceApplyFailed`, or `MarkerApplyFailed` status before `kopf.TemporaryError`; stable collisions remain `ResourceCollision`, and successful reconciliation remains `Ready=False/RuntimeNotImplemented`. Secret and ConfigMap RBAC is exactly `get`/`create`/`patch`. Validation passed before the current documentation changes: 243 unit tests, Ruff lint/format, mypy, Helm lint/template, and `git diff --check`.

The four-Service slice is committed locally at `797235b` on `dev`, unpushed and undeployed. It pre-reads RabbitMQ `5672`, Memcached `11211`, MariaDB `3306`, and Keystone `5000` after the existing marker-plus-four prefix, then completes classification, foundational preflight/rendering, and all manifest construction before writes. Each is an owner-referenced ClusterIP plaintext Service with deterministic `appliance_resource_name`, standard metadata, no explicit `clusterIP`/`clusterIPs`, selector exactly the label-safe appliance identity plus component, and one named TCP port targeting the same fixed number. Managed Services use resourceVersion-guarded SSA; writes remain the four foundational resources, then the four Services in frozen order, marker last. Stable collisions are mutation-free `ResourceCollision`; sanitized read/apply failures retry without rollback. Service RBAC is exactly `get`/`create`/`patch`; `Ready=False/RuntimeNotImplemented` remains expected. Validation passed: 252 unit tests, Ruff lint, Ruff format check (35 files already formatted), mypy, Helm lint/template, and `git diff --check`. No workloads, endpoints, Ingresses, Jobs, storage, probes/readiness, bootstrap, rotation, additional Services, release/chart/image/deployment, or CRD version change was added; deployed `0.5.3` remains marker-only.

The CIXpress build for commit `b824f5c` correlated to pipeline `hlzfy3` (template `Default`) and failed: top-level state `FAILED`, started `2026-08-20T09:23:41+00:00`, completed `2026-08-20T09:23:56Z`. Detail steps were empty, so per-step confirmation is unavailable. Log metadata remained available without exposing content: `git-clone` returned HTTP 200 with one stream/25 lines and `kaniko-build` returned HTTP 200 with one stream/12 lines; `helm-update` and `cleanup` returned HTTP 404. The confirmed root cause is Kaniko DNS failure resolving `example.invalid` during push-permission checking.

The dummy trigger for commit `c9d9dd5ec3ecfe06f03e0cbfc1eda3ff4b0fd58d` ran as pipeline `jcr0vn` using template `Default`, started `2026-08-20T11:18:10+00:00`, and completed `2026-08-20T11:19:27+00:00`. The top-level state and all expected steps (`git-clone`, `kaniko-build`, `helm-update`, and `cleanup`) were `SUCCEEDED`. It generated release commit and tag `0.2.0` at `49cb5dc7dbe247e432e604db19078ecf1c2b5437`.

Commit `2579c6a44bb8fd3b04c4c3f37f4096fba8f2777e` configured the `regcred` chart default. Pipeline `0kvajx` completed with all expected steps `SUCCEEDED` and generated release `0.3.0` at `61b6bb334ccb50aff51228c3d1428a9f284eaff8`. Its Argo deployment confirmed registry authentication, then exposed an invalid CRD schema and a chart command that bypassed the image virtual environment.

Commit `5a8cc443ec00a81baec7ac6d14de1ca40cd1d736` corrected both deployment defects. Pipeline `gepx3l` ended with all expected steps `SUCCEEDED`, although an intermediate `INPROGRESS` response already contained a completion timestamp and therefore remains inconsistent monitoring evidence. The generated release commit and tag `0.4.0` resolve to `4eee8a9f24eb05640c61ece8fa057ecd49136e85`. Argo CD synchronized `0.4.0` successfully; the application is `Healthy`, the CRD is established, and the operator Deployment is `1/1` available with a ready pod and zero restarts.

The initial `0.5.0` live sample exposed Kubernetes Python client `32.0.1` rejecting `_content_type`. Source commit `d3aecb2ce71fc83730969db174b50727c37fe96c` was released by pipeline `jqd3ri` as `0.5.1` at `5ffc69534a558417405e99e1b75c3145319ac19a`. Release `0.5.1` then exposed missing ConfigMap-create RBAC; source commit `cbdd28fc645e37c5a8146ff900f7fd84f6330210` added ConfigMap `create` and `patch` only. Pipeline `5ly5kg` completed `git-clone`, `kaniko-build`, `helm-update`, and `cleanup` as `SUCCEEDED` and generated release `0.5.2` at `86552e46fd2fb13b05d66cc2b7e25f4968f00846`.
