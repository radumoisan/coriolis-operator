# Coriolis Operator

Kubernetes-native management for a Coriolis Appliance while retaining the current appliance runtime shape.

The operator is built with Python 3.12, uv, and Kopf. Existing Coriolis component repositories and images are immutable upstream inputs; this project orchestrates them rather than modifying or rebuilding them.

`CoriolisAppliance` is a namespaced API at `coriolis.cloudbase.it/v1alpha1`.

The bootstrap/controller skeleton is implemented; controller lifecycle validation passed on release `0.5.2` in the approved `coriolis` namespace, and the currently deployed release is `0.5.3`. The deployed `0.5.3` retains the marker-only controller behavior (it predates the local API slice). `Ready=False/RuntimeNotImplemented` is expected until the recorded Kubernetes-native core runtime is implemented.

On 2026-08-20 the local API-only `core` runtime slice was implemented and validated: the `v1alpha1` CRD defines optional/defaulted `spec.profile: core` (enum only `core`), required non-empty `spec.version`, and optional non-empty `status.acceptedVersion`. The controller enforces the immutable accepted version from the persisted `status.acceptedVersion` (no CEL/admission rules), supports initial exact `2603.4` with omitted profile defaulting to `core`, and otherwise applies no resources and reports rejection or `Upgradeable=False/UpgradeBlocked` conditions. A valid API-only reconcile records only the owned controller-state ConfigMap and reports all six conditions, with `Ready=False/RuntimeNotImplemented` remaining truthful. 25 tests, Ruff, mypy, and Helm lint/template pass; no cluster or external service was changed and no runtime workloads are implemented or deployed. This API slice is committed locally at `ab9df83` but is **not** pushed or deployed and is **not** included in the deployed `0.5.3`.

On 2026-08-21 the local collision/migration marker API-layer slice was implemented and validated and is **committed locally at `d8df00f` on `dev`, but not pushed or deployed**. Reconciliation now pre-reads the deterministic marker ConfigMap before SSA: a 404 creates normally; a fully matching managed marker reconciles; and a compatible legacy `0.5.2`/`0.5.3`-shaped marker (no management signature, matching controller owner reference, compatible `acceptedVersion`/`profile`) is normalized in place under the unchanged shipped marker name with its stale generation updated. Partial/conflicting standard metadata, owner mismatch, incompatible legacy data, or owner-plus-retention metadata is a `ResourceCollision`: the object is not patched, adopted, deleted, or renamed, and status keeps all six conditions with `Accepted=True`, `Progressing`/`Reconciled`/`Ready=False`, `Degraded=True`, and `Upgradeable=False`; a prior `acceptedVersion` is preserved but not newly established on an initial collision. Non-404 Kubernetes read failures still propagate; SSA content type, field manager, `force=True`, marker data, naming, and patch-error behavior are unchanged. ConfigMap RBAC gains only `get`. Validation: 70 unit tests; `uv run ruff check .`; `uv run ruff format --check .`; `uv run mypy src`; `helm lint helm/`; `helm template coriolis-operator helm/ --include-crds`; `git diff --check`. The deployed `0.5.3` remains marker-only and unchanged and lacks these local pre-read/collision semantics.

On 2026-08-21 the pure retained-resource authorization/classification slice was implemented and validated locally and is **committed locally at `1b73045` on `dev`, but not pushed or deployed**. It adds `classify_retained_resource` (returning `RetainedClassification.ABSENT/REUSE/COLLISION`) to `src/coriolis_operator/reconcile.py`, a pure classifier for operator-created retained PVCs, state Secrets, and CA state. An absent resource is eligible for creation; a retained resource is reused automatically only when its deterministic name/namespace and every operator-controlled identity field match exactly (full appliance-name annotation, standard managed/identity labels, component label, exact retention annotation/class), with **no owner references** (owner plus retention is a collision even if an owner UID matches), while unrelated extra labels/annotations are permitted; missing/partial/conflicting identity metadata is a collision and is never normalized. The creating appliance CR UID is deliberately **not** part of the identity: retained resources survive CR deletion/recreation, so automatic exact-match reattachment works even when the CR UID changes, and any stale `coriolis.cloudbase.it/appliance-uid` annotation is ignored as unrelated. It is a namespace trust boundary (in-namespace users can forge identity metadata). External/pre-existing resources, especially the registry pull Secret `coriolis-appliance-registry`, fail closed as `COLLISION` and remain read-only and outside this classifier/reconciliation policy. The classifier is pure and works with mapping-shaped fakes and real `V1Secret`/`V1PersistentVolumeClaim` model objects. Validation: 94 unit tests; `uv run ruff check .`; `uv run ruff format --check .`; `uv run mypy src`; `helm lint helm/`; `helm template coriolis-operator helm/ --include-crds`; `git diff --check`. **No runtime resource is constructed, reconciled, patched, read, or adopted**, and the deployed `0.5.3` remains marker-only and unchanged. A MariaDB vertical slice remains blocked by the remaining gates (actual generators/builders/reconcile reads/SSA/RBAC, storage, probes/readiness, and rotation); the foundational Secret/configuration naming and the primary `coriolis.conf` split are now frozen by the follow-on documentation-only contract slice (see below).

On 2026-08-21 a documentation-only Secret/configuration contract slice was added to [docs/foundational-resource-contract.md](docs/foundational-resource-contract.md). It freezes the foundational Kubernetes Secret/ConfigMap names, key layouts, and the primary `coriolis.conf` split using only known source key names: retained, ownerless Secrets `<appliance>-coriolis-credentials` (`coriolis_database_password`, `coriolis_keystone_password`, `temp_keypair_password`), `<appliance>-infrastructure-credentials` (`database_password`, `rabbitmq_password`, `keystone_admin_password`), and `<appliance>-step-ca-credentials` (`init_password`); plus the owner-referenced, rebuildable ConfigMap `<appliance>-coriolis-config` (the six source-audited non-secret files) and configuration Secret `<appliance>-coriolis-config-secret` (`coriolis.conf`), mounted together as a read-only projected volume at `/etc/coriolis`. The later pure builder slice at `050f16e` closes the concrete manifest-builder gate. Remaining gates are credential generation/value-source policy, collision-safe Secret/ConfigMap pre-reads, SSA, minimal RBAC, and reconciliation; TLS/optional credentials/storage/probes/readiness/rotation remain deferred. This contract slice is **committed locally at `8ce26ba` on `dev`, but not pushed or deployed**.

On 2026-08-21 the pure Secret/ConfigMap builder slice was implemented and validated locally and is **committed locally at `050f16e` on `dev`, but not pushed or deployed**. `src/coriolis_operator/reconcile.py` now provides `build_coriolis_credentials_secret`, `build_infrastructure_credentials_secret`, `build_step_ca_credentials_secret`, `build_coriolis_config_map`, and `build_coriolis_config_secret`. The builders use deterministic names and standard metadata: retained credential Secrets are ownerless with retention metadata, while rebuildable configuration resources are owner-referenced without retention. They enforce the frozen key sets; accept caller-provided opaque strings without mutation; reject missing, extra, or non-string inputs without exposing values; emit `Opaque` Secret manifests with UTF-8/base64 `data` and no `stringData`; and keep ConfigMap data plain and limited to the six approved files, never `coriolis.conf` or credentials. Validation passed: 116 total tests (22 new cases from the prior 94; 21 matched the final focused selector), Ruff lint/format, mypy, Helm lint/template, and `git diff --check`. This is pure manifest construction only: no credential generation, `main.py` reconciliation, Kubernetes reads/SSA, RBAC, CRD, runtime resource creation, status/readiness, or deployment change. The deployed `0.5.3` remains marker-only. The next gate is credential generation/value-source policy, then collision-safe Secret/ConfigMap pre-reads, SSA, minimal RBAC, and reconciliation; TLS/optional credentials/storage/probes/readiness/rotation remain deferred.

On 2026-08-21 the pure retained credential generation slice was implemented and validated locally and is **committed locally at `a604579` on `dev`, but not pushed or deployed**. `generate_coriolis_credentials`, `generate_infrastructure_credentials`, and `generate_step_ca_credentials` independently generate the seven frozen keys with `secrets.token_urlsafe(32)` (32 random bytes/256 bits, URL-safe opaque strings); injectable token factories are test-only, and empty/non-string results fail value-safely. Values compose unchanged with existing builders and must never appear in documentation, logs, status, events, or errors. The policy is operator-generated only: no inline CR credential values or external credential Secret source; external/pre-existing Secrets such as `coriolis-appliance-registry` remain read-only and outside it. Generate-once/reuse is policy only until runtime wiring: generate only for `ABSENT` retained Secrets, reuse exact matching ownerless retained Secrets unchanged, fail closed on collisions, and defer rotation. `main.py` does not call generators. Validation passed: 132 total tests (16 new from 116), focused tests, Ruff lint/format, mypy, Helm lint/template, and `git diff --check`; no reconciliation, Kubernetes reads/writes/SSA, RBAC, CRD, runtime resources, status/readiness, chart/release, deployment, or rotation changed. Next: configuration rendering inputs, retained Secret semantic validation, collision-safe pre-reads, `ABSENT`-only generation, SSA, minimal Secret RBAC, and reconciliation/status failure semantics; TLS/optional credentials/storage/probes/readiness/bootstrap/rotation remain deferred.

On 2026-08-21 the pure retained Secret semantic validation/extraction slice was implemented and validated locally and is **committed locally at `5165629` on `dev`, but not pushed or deployed**. `validated_retained_secret_values` accepts mapping-shaped objects and Kubernetes `V1Secret` models; accepts absent `apiVersion`/`kind` but rejects conflicting values; requires `Opaque`, no persisted `stringData`, exact frozen string-valued encoded `data`, strict base64 then UTF-8 decoding, and non-empty decoded values. It returns a new decoded mapping without input mutation, and fixed/category-only failures reveal no values. This is semantic validation only: no metadata classification, Kubernetes reads/writes, generation, SSA, collision/status handling, or reconciliation. Validation passed: 152 total tests (20 new from 132), focused 20 tests, Ruff lint/format, mypy, Helm lint/template, and `git diff --check`. No `main.py`, RBAC, CRD, runtime resource, chart/release, deployment, or rotation behavior changed; deployed `0.5.3` remains marker-only.

On 2026-08-21 non-sensitive configuration rendering was implemented and validated locally at **`97153a7` on `dev`, but not pushed or deployed**. Pure `render_coriolis_config` validates explicit rendering inputs and uses Jinja2 `PackageLoader` with `StrictUndefined`, disabled autoescape, and preserved trailing newlines to render exactly the six frozen ConfigMap files. The six verbatim Apache-2.0 upstream templates are packaged with source/license attribution; `accepted_version` maps to legacy `default_coriolis_docker_images_tag`. It renders no `coriolis.conf`, provider fragments, credentials, or other Secret content. Validation passed: 19 focused renderer tests, 171 total tests, wheel resource inspection, and template byte comparison. No reconciliation or deployed behavior changed; deployed `0.5.3` remains marker-only.

On 2026-08-21 the pure foundational five-resource preflight was implemented and validated locally and is **committed locally at `35eac9b` on `dev`, but not pushed or deployed**. `src/coriolis_operator/reconcile.py` adds `OwnedClassification.ABSENT/MANAGED/COLLISION`, `classify_owned_resource`, credential-safe frozen `FoundationalResourcePreflight`, and `preflight_foundational_resources` for exactly three retained credential Secrets plus the owner-referenced configuration ConfigMap and Secret. It classifies metadata for all five before retained Secret semantics: exact owner metadata/controller matches are `MANAGED` despite content/type drift, mismatched ownership or retention metadata is `COLLISION`, and unrelated extra metadata is allowed. A metadata collision stops semantic validation and generation; `REUSE` retained Secrets are semantically validated with `ValueError` mapped to that resource's `COLLISION`; all reusable semantics finish before corresponding `ABSENT` generators run, and only when the whole preflight is collision-free. Successful results carry generated/decoded credentials but hide them from `repr`; errors, logs, status, and events remain value-safe. Validation passed: 23 focused tests, 194 total tests, Ruff lint/format, mypy, Helm lint/template, and `git diff --check`. This is pure only: no `main.py`, Kubernetes reads/writes, SSA, RBAC, CRD, runtime construction/adoption, reconciliation/status behavior, chart/release/image version, deployment, TLS/storage/readiness/bootstrap/rotation changes. Deployed `0.5.3` remains marker-only.

On 2026-08-21 the sensitive `coriolis.conf` renderer contract was documented and **committed locally at `574efcf` on `dev`, but not pushed or deployed**. It freezes a future pure renderer from the immutable upstream base template and all 16 provider fragments, preserving authoritative provider lists/order/module maps; custom module overrides and compression/compressor are disabled initially. The complete rendered file remains exactly one key in the owner-referenced configuration Secret and is never ConfigMap/log/status/event/metadata/documentation content. Inputs are explicit internal dependency inputs with fixed source-audited identities/paths and credential mappings; validation and the interface boundary are strict and value-safe, with no CRD fields added. All three generated credential Secrets use exactly `coriolis.cloudbase.it/retention: state-credentials`. Source review covered every base-template variable and all fragments; `git diff --check` passed. This is documentation-only: no renderer implementation/tests or runtime behavior changed; TLS/CA material, provider connection/private data, optional credentials, dependencies, bootstrap, storage, readiness, and rotation remain deferred.

On 2026-08-21 the pure sensitive renderer was implemented and validated locally at **`9bb20f3` on `dev`, but not pushed or deployed**. `SensitiveCoriolisEndpoints`, `SensitiveCoriolisCredentials`, redacted one-key `SensitiveCoriolisConfig`, and `render_sensitive_coriolis_config` render the exact `coriolis.conf` Secret key, compose directly with the existing configuration-Secret builder, and are rejected by the ConfigMap boundary. Inputs are frozen/exact and unmutated; credential/output reprs are redacted; errors are fixed/category-only and value-safe. Jinja uses `PackageLoader`, `StrictUndefined`, disabled autoescape, and a trailing newline for the immutable upstream base plus all 16 provider fragments, with frozen provider lists/order/module maps, source-audited values/paths, source/license attribution, prohibited custom overrides, and disabled compression/compressor. Validation passed: 40 focused configuration tests, 215 total tests, Ruff lint/format, strict mypy, Helm lint/template, `git diff --check`, 17/17 source byte parity, and offline wheel inspection of exactly 25 expected template resources. No `main.py`, reconciliation, Kubernetes reads/writes/SSA, RBAC, CRD, runtime resources/workloads, chart/release/image version, deployment, TLS/CA/bootstrap, provider/private data, optional credentials, storage, readiness, or rotation behavior changed; deployed `0.5.3` remains marker-only.

On 2026-08-21 the pure contract/input migration was completed and **committed locally at `e2ddb30` on `dev`, but not pushed or deployed**. The factory derives deterministic RabbitMQ, Memcached, MariaDB, and Keystone Service names and fixes `0.0.0.0:7667`, `/etc/coriolis`, and `/var/log/coriolis/vmware-root`. Kubernetes-derived templates now use RabbitMQ plaintext `5672`, Keystone HTTP `5000`, and HTTP WSGI with no internal CA/TLS directives; immutable upstream copies/providers remain evidence only. The initial foundational set is two retained credential Secrets plus an owned config ConfigMap/Secret; Step CA is removed from initial pure APIs/preflight. The slice changes the CRD ingress schema/sample and adds pure validation for development host `coriolis.app.cloudbase.wiki`, ingress class `nginx`, cert-manager `letsencrypt` with derived `<host>-tls` Secret name, or an existing same-namespace TLS Secret without a cert-manager annotation. Validation passed: 218 unit tests, Ruff lint/format, strict mypy, Helm lint/template, `git diff --check`, byte parity for all 23 immutable copied templates, and offline wheel inspection of all 27 packaged template resources. It adds no `main.py`, runtime Kubernetes I/O, SSA/RBAC, actual Service/Ingress/workload resources, or release/chart/image/deployment behavior. The deployed `0.5.3` remains marker-only.

## Layout

- `helm/`: Helm chart, including CRDs in `helm/crds/`.
- `docs/`: architecture, contracts, development guidance, and decisions.
- `docs/foundational-resource-contract.md`: foundational Kubernetes resource contracts (naming, labels, ownership/retention, Secrets, dependency order) for the `core` profile.
- `docs/ci.md`: CIXpress CI and release model.
- `docs/cixpress-monitoring.md`: approved polling-only CIXpress pipeline monitoring and troubleshooting procedure.
- `docs/dev-environment.md`: approved dev-environment access, CI observation, and safety boundary.

## Open Details

The historical multi-resource policy remains recorded in [docs/foundational-resource-contract.md](docs/foundational-resource-contract.md). The current next gate is collision-safe marker-plus-four reads/create/guarded SSA, Secret/ConfigMap `get`/`create`/`patch` RBAC, sanitized status then Kopf retry, and exhaustive tests. Services, Ingress, and workloads are later work. Internal dependency Services will be ClusterIP plaintext on the trusted cluster network; community ingress-nginx will own public TLS/redirects while a later operator-owned logical Ingress provides the single-origin path routes, explicit-origin CORS/preflight/auth headers, and WebSocket handling. `coriolis-web-proxy` and Step CA are deferred pending evidence. Deployed `0.5.3` remains marker-only.

CIXpress is the known CI/release automation model and owns future chart version, application version, and image-tag synchronization. The image repository is `cr.virtomat.io/virtomat/coriolis/operator`; publication, authentication, and deployment are validated, and the currently deployed release is `0.5.3`. The Kubernetes-native `core` runtime contract is recorded; RC4 is blocked/OVA-only and the approved fallback is exact official release `2603.4`. All 26 approved images were mirrored to `cr.virtomat.io/virtomat/coriolis` on 2026-08-20 with preserved and verified manifest digests, and Kubernetes pull validation in `virt-infra-dev-buc-hq` namespace `coriolis` passed for all 21 historically selected image references with no residual validation Pods. The image inventory and pull gate are complete. The local pure input migration is committed at `e2ddb30` and creates no runtime resources; deployed `0.5.3` remains marker-only. The immediate next gate is collision-safe marker-plus-four reads/create/guarded SSA, Secret/ConfigMap RBAC, sanitized status then Kopf retry, and exhaustive tests. Services, Ingress, and workloads remain later phases. Promotion, exact CIXpress configuration/templates/triggers/credentials, Argo CRD pre-upgrade automation, and licensing as applicable remain open.
