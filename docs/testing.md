# Testing

Validate the work relevant to each change.

## :material-book-open-page-variant-outline: Readiness 0.5.38 POC Partially Accepted

- Source `9a223667cebeb3094baa86c21dd517cb5ca38809` (`Fix runtime readiness web config validation`) released `0.5.38` through CIXpress Default `82ggo8` (`2026-08-27 05:11:19Z`-`05:12:22Z`), with every expected step `SUCCEEDED`; CI release `7c7b7b70d678bfba98d045834c1cb0ab541c233f` published chart `sha256:fb6dae233cc752959c1758db59054ebbcce37671f349e20e926078af724ecda8` and operator `sha256:8533a05aaceee5fe6c0da93887ed859d7df14993f97442fe55eaf13b7d06200f`. The released predicate accepts required pairs in a Mapping and ignores unrelated extras. Local validation recorded 705 tests/21.98s, 17 focused, Ruff, 70-file formatting, strict mypy/18, Helm lint/template, diff check, and a removed Docker image. The `0.5.37` extra-key failure remains historical.
- In `virt-infra-dev-buc-hq`/`default`, namespace/Application `coriolis-readiness-validation-20260826`, chart `0.5.38`/`skipCrds`, CR `readiness-validation`, and host `coriolis.app.cloudbase.wiki`, fresh preflight found node healthy twice and target/PVs/host absent. Corrected Application source and isolated exact-digest operator `1/1`/zero restart passed. Pull-Secret values were copied into target Kubernetes Secrets through value-silent in-memory handling; no payload was exposed in workspace/tool output, and the target copies were removed with the namespace. A redundant later local `oras` `401` had no deployment effect.
- First CR UID `bdce6ce6-30bb-40e7-8987-6121343cb15f` reached `RuntimeReady` at `05:26:38.843332Z`, 109.843s after creation. It met `acceptedVersion=2603.4`, generation/observed `1`, successful inactive bootstrap, two Bound PVCs, exact StatefulSets, ten current-generation `1/1` Deployments, six Services, 12 ready zero-restart Pods, and three expected Ingresses. Value-safe web-root/config `200`, Keystone auth `201`, Coriolis endpoints `200`, and overall true passed; identity/status/revision/restart snapshots remained stable for 65s.
- Scaling API to zero retained Deployment UID `ad8c181b-a472-4a96-88da-c9071a6941d3`, generation/observed `2`, no API Pod, and unchanged unrelated identities/restarts for 8m05s. It correctly reported `Ready=False/RuntimeStarting`, `Progressing=True/RuntimeStarting`, `Degraded=False`, and `Reconciled=True`; `RuntimeHealthCheckFailed` is only for failed internal checks after structural gates pass. Manual restore at `05:43:12Z` produced exact-digest zero-restart API `1/1` in 13s and `RuntimeReady` at `05:43:36.566440Z` (24.6s), stable more than 65s.
- Normal old-CR delete removed 28 non-Secret old-owner resources in 103s while five retained Secrets and two PVCs/PVs stayed exact. New same-name CR UID `c092ccf0-d7e8-44cf-9cf9-c30a493d8590` was finalized with no status/children for 20m while the operator was healthy: automatic same-name recreation is unaccepted. After an explicitly approved normal isolated operator replacement, resume reached `RuntimeReady` in 158s at `06:33:14.732632Z`; retained baselines stayed exact and 31 direct resources were owned only by the new UID, with zero old refs/restarts and 65s stability. The duplicate post-recreation manual trace was tooling-inconclusive, but the production observer retained `RuntimeReady` and the initial clean trace passed; two checker stops were tooling-only, and corrected per-resource checks passed.
- Cleanup removed final CR/Application/namespace in 23s/16s/68s and PVs in 10s without force, grace-zero, finalizer/owner edits, or individual child/Secret/PVC/PV/Ingress/Certificate deletion. Final target/App/namespace/CR/PVs/host are absent; shared exact `0.5.38` and node are healthy. The next gate is to investigate/fix or operationally define automatic same-name CR recreation, then fresh targeted retest. `Ready=True` remains selected single-replica internal-core health, not production readiness, HA, storage, backup/restore, external/browser/provider/write workflow acceptance.

## :material-book-open-page-variant-outline: Accepted Same-Name Recreation 0.5.40

- Source `1f4f4250b0e0716a2aa40e6e4e50f2866e8fd2d8` (`Recover uninitialized appliance reconciliation`) extends the existing 60s per-object timer to reconcile wholly absent or empty status as well as stable collisions and in-flight Kopf retry. It does not claim broad periodic drift repair. The local gate passed 707 unit tests in 15.61s, six focused collision-timer tests, Ruff, 70-file format, strict mypy/18, Helm lint/template, and diff check. CIXpress Default `yxf23` ran the exact source from `2026-08-27T08:31:44+00:00` to `08:32:59+00:00` with top-level `SUCCEEDED`; per-step confirmation is unavailable because the five-character ID failed approved six-character monitor validation. Release commit/tag `280d74dc76c05c63e6845ab5bcbb3a8adee5b9d4`/`0.5.40` published operator digest `sha256:a095f1e443e39ecf3c1fc2d312de042ed879fc3369308b1c501e2cde7ebad67b`.
- The isolated `virt-infra-dev-buc-hq`/`default` POC used namespace/Application `coriolis-recreation-validation-20260827`, chart `0.5.40`, `skipCrds`, the exact digest, CR `readiness-validation`, and host `coriolis.app.cloudbase.wiki`. The historical `0.5.38` failure remains nondeterministic history. A fresh pre-fix `0.5.38` recreation automatically removed 31 old children in 38s; new UID `c717a978-2e0a-4a6f-b4c7-37b124cbba77` reached `RuntimeReady` in 121s, stayed stable for 83s, retained the exact state, and needed no restart.
- Released `0.5.40` removed old UID `c717a978-2e0a-4a6f-b4c7-37b124cbba77` and 31 children in 29s. New UID `e405b201-5bad-4757-a77d-706a5f265262` was created `08:45:18Z`, Accepted `08:45:19.403533Z`, bootstrap completed `08:46:18Z`, Reconciled `08:46:25.875009Z`, and reached `RuntimeReady` `08:47:08.260743Z` in about 110s, stable for 245s. It recorded `acceptedVersion=2603.4`, observed generation `1`, new UID only with no old references, six Services with ready endpoints, all workloads/Pods Ready with zero restarts, unchanged zero-restart operator Pod/digest, and a Ready node. The exact 31 children were 10 Deployments, 2 StatefulSets, 1 Job, 6 Services, 3 Ingresses, 6 ConfigMaps, and 3 Secrets.
- Eight ownerless retained, external, or TLS Secrets retained exact UID/resourceVersion metadata without writes, and both PVC/PV UID/resourceVersion metadata and bindings remained exact without writes; five retained Secrets are generated credentials. No Secret values or payload-bearing annotations were retrieved or exposed. Normal cleanup removed the CR and 31 children in 47s, Application in 19s, namespace in 110s, and Delete-policy PVs in 124s after the namespace deletion request, with no force, grace-zero, finalizer/owner/cascade edits, or individual retained-resource deletion. Final target/Application/namespace/CR/PVs/host are absent; shared Argo/operator `0.5.40` and node are healthy. Milestone 5 is accepted and closed for bounded same-name recreation. Production readiness, HA, storage, backup/restore, broad drift self-heal, provider, browser, and write workflows remain excluded.

## :material-book-open-page-variant-outline: Accepted OpenStack Provider Qualification 0.5.42

- Isolated chart/operator `0.5.42` with `skipCrds` accepted CR `openstack-validation` UID `3cb8e4ac-29dd-45b4-816e-34f1ac2aab77` at version `2603.4`. It reached `RuntimeReady` in 128s, remained stable and `Synced/Healthy`, had 11 ready Deployments, 2 ready StatefulSets, successful bootstrap, and zero Pod restarts, including after provider calls.
- Provider `1.2.7` live schemas accepted password authentication with canonical name/domain scope and exposed no application-credential fields. Source endpoint `84818c95-f7a7-4013-8b8b-888fa676aae2` and destination endpoint `8a32df62-31b7-4d83-be7d-26fc00dc2927` were created through the API with inline `connection_info`, validated through Coriolis API/conductor/worker, and revalidated after a 30-second hold. No credentials, request bodies, headers, raw responses, logs, status, events, or configuration payloads were retained in evidence.
- Provider API option contracts were preserved for Milestone 7: `replica_export_mechanism` is always returned but is not a filterable source option, so the request omitted that `options` filter; floating-IP pools use `network/subnet` composites, with selected name `ext_net_gts/ext_subnet_gts` and selected IDs `c5815350-3a4c-4a6a-a567-db9f0d6e5a19/08e4c993-ad01-49fe-ada4-050f7339984c`. The corrected requests left no failed-attempt endpoints or cloud writes.
- Source options reported `replica_export_mechanism: swift_backups`; the source-inventory result contained only retained test VM `309e9943-05d5-48f9-bf21-38112d83788e`, `ACTIVE`, `c1.small`, network `ext_net_gts`, and volume backend `rbd1`. Cinder volume `81c2ad4d-c6bc-46f4-b27b-da7962711805` produced backup `04fdf896-2822-4e2c-a85f-7ca699341628`, available in 192s; its dedicated Swift container held 125 objects totaling 593,683,060 bytes. The backup and container were normally deleted before endpoint qualification.
- Destination candidates and mappings passed through Coriolis and independent inventory: image `ubuntu-24.04`/`b480e10c-edc9-400a-8b70-49883cb68392`; flavor `c1.small`/`dcb9647a-2167-4c23-908e-fdec0c93d715` (4096 MiB, 2 vCPU); worker network `infra-dev-buc-hq-net`/`8b1d447d-a1f4-4bd1-bc47-6e3b79cb0389`; floating-IP pool `ext_net_gts/ext_subnet_gts`; and volume type `__DEFAULT__`/`a7a3b70f-1e24-406b-8c27-fd82996dcfc9`. Quota headroom was 10,240 MiB RAM, 4 cores, 8 instances, 8 volumes, and 760 GiB, supporting exactly two `c1.small` temporary workers.
- Both endpoint deletes returned `204` and final endpoint count was zero. The source server, volume, backup, and port were deleted normally; destination inventory remained unchanged. CR, Application, namespace, and the exact Delete-policy PVs were removed through graceful cleanup without force, grace-zero, finalizer/owner/cascade edits, or individual retained/PVC/PV deletion. Milestone 6 is accepted and closed. It does not accept transfer, migration, deployment, target write, worker VM creation, browser/UI, Barbican, `live_migration`, production readiness, HA, or Kubernetes runtime backup/restore.

## :material-book-open-page-variant-outline: Next OpenStack-To-OpenStack Acceptance Gate

- Milestone 7 is the current gate: create both endpoints from scratch through the API using inline `connection_info`, then run one-shot `live_migration` with `clone_disks=true`, `skip_os_morphing=true`, `shutdown_instances=true`, and `auto_deploy=true`. Poll transfer and auto-created deployment separately, verify destination `ACTIVE` and basic reachability, then clean up normally without exposing credentials.
- Milestones 8-11 remain logging, secure UI/browser prerequisites, UI POC, and separately evidenced hardening. Step CA, web-proxy, compressor, and console editor remain omitted or superseded; licensing and Metal Hub remain deferred.

## :material-book-open-page-variant-outline: Accepted Logical-Origin Ingress 0.5.35 POC

- Source `016a25744a7c946a02fa6d89e38fa39e24e32146` passed the local gate (640 tests in 17.84s plus existing checks and image `bada188f716e`) and CIXpress pipeline `4srgi9` (`12:30:18Z`-`12:31:50Z`), with all expected steps `SUCCEEDED`. Release `a9a594bcdcdc5532e7b676067d55edb9566d81a4` synchronized version `0.5.35`; the accepted operator digest is `sha256:43e5787aa6bb96bbffc895fc792727afd85be5aa4aeae7fbcde0cb2386257a4f`.
- The isolated POC used context `virt-infra-dev-buc-hq`, project `default`, namespace/Application `coriolis-ingress-validation-20260826`, host `coriolis.app.cloudbase.wiki`, chart `0.5.35`, and `skipCrds`. `Ready=False/RuntimeNotImplemented` remained truthful. Ownerless collision fixtures held for web 223s, Keystone 195s, and API at least 130s with the exact stable `ResourceCollision` set, absent acceptedVersion, and zero child, retained, PVC, or PV writes. Deleting only the API fixture auto-recovered; first-child and final observations are bounded observations, not guaranteed latency.
- The controller owns three Ingresses: web `/` `Prefix` to TCP `3000` without rewrite; Keystone `/identity(/|$)(.*)` to TCP `5000` with `/v3/$2`; and API `/coriolis(/|$)(.*)` to TCP `7667` with `/v1/$2`. The primary web Ingress alone carries the cert-manager annotation. The production LetsEncrypt Certificate was Ready; all routes use the same shared TLS Secret, DNS resolved to `89.34.101.238`, HTTP redirected with `308`, and TLS was trusted. Web returned `200`, identity paths returned `200`, and Coriolis paths returned `401`.
- Exact-origin CORS returned `204` with credentials, `GET`, and requested headers including `Authorization`; a foreign origin was not reflected. Deliberate Keystone managed drift remained unchanged for 195s because the timer is deliberately collision-only. Normal operator Pod replacement invoked resume and repaired the same UID and exact managed fields while preserving a foreign label and annotation; those added keys were then explicitly removed. This is operator-resume repair only, not a broad periodic self-heal claim.
- CR recreation preserved the five retained ownerless state credential Secrets, both Bound PVCs/PVs, and the TLS Secret UID/resourceVersion exactly; owned resources and the Certificate moved to the new CR/web owner and workloads/routes reconverged. Two observer harnesses failed to capture ADDED order (first: lowercase Go-template fields with suppressed stderr; second: empty-list resourceVersion from a lowercase field). A stale-summary restore manifest was API-rejected before creation, then an installed-schema exact CR restored and reconverged. Fine-grained live creation order remains inconclusive and must not be inferred from later resourceVersions.
- One Secret metadata baseline command emitted a last-applied annotation payload into internal tool output. Do not reproduce values. It did not mutate the Secret, and the target namespace and copies were deleted. The existing explicit rotation waiver and residual risk remain; no remediation is claimed.
- Normal cleanup removed the CR in 12s, Application in 10s, namespace in 76s, and Delete-policy PVs in 12s. The TLS Secret survived CR-owned garbage collection exactly and was removed only by namespace deletion; no force/finalizer/owner edit or individual child, retained, or PV deletion occurred. The final target is absent; shared `0.5.35` is `Synced/Healthy`, the operator is Ready with zero restarts, the node is Ready, and public HTTP returns `404`.
- Accepted limits: no browser login, valid-credential/provider/write workflow, HA, production storage or backup/restore, multi-CR uniqueness, periodic self-heal, or exact live creation order. This accepted POC supersedes the historical local-only Ingress framing below and in the progress log.

## :material-book-open-page-variant-outline: Accepted Coriolis Web 0.5.33 POC

- Source `942557a0914b7455af6dbeac6ae5966417bd1223` was published by CIXpress Default `opfrnr` (`08:39:32Z`-`08:40:58Z`); top-level and `git-clone`/`kaniko-build`/`helm-update`/`cleanup` all `SUCCEEDED`. CI commit `9f7151af10e2275e15718a325a12e850601ec5f3` synchronized tag/chart/app/operator `0.5.33`; chart digest `sha256:0e0452229c22c2a4067c55df25c6e09c1ffeefcc8e92eb3fba77ab477713e27e`, operator digest `sha256:8f3e80f3c6ea2a79c83feff5078ca45ef8d8615b060ae91b1fae235428a31273`, and web digest `sha256:32ebc391ac46fe627185694b3fd252afd7587b152f526dff38ae0a5b887c0db1` are accepted.
- The exact web image is linux/amd64, root/unset user, entrypoint `npm run start`, no Cmd, workdir `/root/coriolis-web`, exposed `3000`, and no volume/healthcheck. `scripts/validate-coriolis-web-runtime.py` passed all 17 stages (`SUMMARY runtime passed 31.889`) with fixed output/body suppression and no network, host ports, mounts, devices, or leftovers: root/config `200`, exact relative logical-origin URLs, expected missing-fingerprint `500`, first-launch POST `200` then config `false`, 20-second stability, recreation reset to `true`, and upstream stop exit `1` in `0.747s` (bound `5s`). The accepted exception remains: UID/GID `42434` exited `243` before Ready, read-only root made first-launch POST return `500`, and direct Node exceeded 10 seconds and was killed `137`; the contract therefore preserves image entrypoint, root `0:0`, writable ephemeral root, dropped `ALL`, no privilege escalation, and `RuntimeDefault`.
- The isolated POC used context `virt-infra-dev-buc-hq`, Argo project `default`, namespace/Application `coriolis-web-validation-20260826`, exact chart `0.5.33`, and `skipCrds`; node preflight was healthy twice. Separate Service (`175s`) and Deployment/ReplicaSet (`273s`) collisions preserved their exact fixture and CR identities/status, absent acceptedVersion, and zero managed/retained/marker/PVC/PV writes. The Service collision CR and fixture were removed normally after the hold; deleting only the Deployment fixture auto-recovered without a CR edit or operator restart.
- The succeeding bootstrap `web-poc-common-bootstrap-v2` had exit `0`, failed `0`, and restarts `0`; the last marker was `web-poc-operator-state`. The owner-referenced web ClusterIP Service TCP `3000`, one ready EndpointSlice, and exact one-replica `Recreate` Deployment/Pod matched the local contract and omissions, with Ready/zero restarts and no Ingress. A fixed-output fresh Service-DNS probe emitted exactly `coriolis-web-poc-ok` after 21 seconds: root/config `200`, exact relative URLs, expected fingerprint `500`, then first-launch POST `200` and config `false`. Earlier failed diagnostics were verifier or command defects before workload mutation; the final fresh manifest passed.
- Normal web Pod replacement produced a distinct UID, Ready in 5 seconds, the exact digest, zero restarts, endpoint retargeting, and a first-launch-reset probe that emitted exactly `coriolis-web-reset-ok` after 21 seconds. Controlled Service-selector and Deployment-replica drift recovered through normal operator replacement, then in-place same-UID exact-hash repair, endpoint restoration, and a new exact-digest zero-restart web Pod Ready in 84 seconds. CR recreation changed UID prefix `bf8202ab...` to `e708184d...`, reused Secrets/PVCs/PVs without writes, moved 28 direct resources to the new owner with no old owner references, and observed ADDED resourceVersions in order: API Service `14332509`, API Deployment `14332516`, web Service `14332528`, web Deployment `14332535`, marker `14332542`; the Job completed `10:22:07Z` and marker was written `10:22:14Z`.
- Final conditions were `Accepted=True`, `Reconciled=True`, `Degraded=False`, `Ready=False/RuntimeNotImplemented`, `Progressing=False/RuntimeNotImplemented`, and `Upgradeable=False`. Normal cleanup removed the CR in 36 seconds, Application in 25 seconds, and namespace in 106 seconds; both Delete-policy PVs were naturally absent. Shared `0.5.33` was healthy with no CR and the node healthy. Startup connection-refused warnings were bounded before Ready with zero restarts; one transient operator and Rabbit liveness warning caused neither restart nor persistence, and no warnings occurred after web Ready. Ingress/TLS/external routing, browser/API/provider/write flow, persisted first-launch state, HA/production storage/backup/restore, and overall readiness remain unclaimed.
- A delegated broad cleanup inventory command accidentally emitted Secret payload data into internal tool output. Do not reproduce a value. No Secret was changed and disposable copies were deleted; do not claim that the `coriolis/regcred` and `coriolis/coriolis-appliance-registry` credentials were not exposed. The user explicitly waived credential rotation for this work and accepted the residual risk, allowing logical-origin Ingress to proceed; this does not invalidate the accepted POC.

## :material-book-open-page-variant-outline: Accepted Coriolis Worker 0.5.31 POC

- Source commit `d1f45c7297ffa925bd9412a26dedd50d1a95e50f` (`Implement Coriolis worker reconciliation`) was published by CIXpress Default `c9jv45` (`04:42:50Z`-`04:44:20Z`); top-level and `git-clone`/`kaniko-build`/`helm-update`/`cleanup` all `SUCCEEDED`. CI release commit `1b7835146242e945d40660cd6462486a860c7f70` synchronized chart/app/operator `0.5.31`; released operator digest `sha256:52762fa5741ca65b8b60df976f7253db72dac650f8ba1bb7b0ee03fe1dbdf7e9`, chart digest `sha256:8e2578b401a7261c0556ac65605fec6b1aa8ef88987ec6c33d08bafac9e5b2e5`.
- The controller owns one owner-referenced, one-replica `Recreate` worker Deployment at exact `cr.virtomat.io/virtomat/coriolis/coriolis-worker:2603.4@sha256:ff30999d6e43709411f197b1b6b80dbce1d7e5498a27f869df93a061626ab2c9`, applied after deployer-manager and before the API, with direct `/usr/local/bin/coriolis-worker --worker-process-count 1 --config-file=/etc/coriolis/coriolis.conf`, 30s grace, and `imagePullPolicy: Always`. The Pod hostname is the deterministic label-safe runtime name for stable `(host,binary,topic)` registration. Security: root `runAsUser/Group 0`, `privileged: true`, `allowPrivilegeEscalation: true`, read-only root, `Unconfined` seccomp; automount token and service links disabled; read-only config Secret; memory `/tmp`; `emptyDir` logs/export; `hostPath` `/dev` RW and `/lib/modules` RO. No Service, port, probe, PVC, lock, init container, service account, resource block, `envFrom`, host networking, PID/IPC, mount propagation, or VDDK/Hyper-V/libvirt/logger/profiling/other mount.
- Reconciliation coverage validates collision-safe full pre-reads/preflight before writes, mutation-free collisions, resourceVersion-required guarded SSA repair, successful-common-bootstrap gating, deployer-manager-then-worker-then-API ordering, marker-last, and sanitized apply failures that leave the marker unwritten. `Ready=False/RuntimeNotImplemented` remains required.
- Runtime qualification: the exact image is linux/amd64, root/unset user, `/entrypoint.sh`, image Cmd direct worker/config, and no declared port, volume, or healthcheck. A hardened no-network/no-write `find_spec` image-contract stage proves all active provider module roots are present and `coriolis_provider_oracle_vm`, `coriolis_provider_opc`, `coriolis_provider_nutanix`, and `coriolis_provider_cloudstack` are absent, so the active Kubernetes provider class list excludes only oracle-vm/opc/nutanix/cloudstack while module maps/templates remain provenance. Kubernetes config adds `[luks] tpm2_pcrs = 7`. The registration-only validator (internal disposable network, no host mounts/devices/provider actions) proves expected `NoWorkerServiceError`, one exact worker enabled/UP, direct worker RPC and scheduler selection, the same hashed DB identity across a clean stop/restart and RabbitMQ recovery, unchanged containers, 20s stability, and complete cleanup; final `SUMMARY runtime passed 478.013`. During qualification full-stack long runs reproducibly hit exit 137 at historical 15s bounds for deployer/scheduler/worker, which now use 30s; conductor uses 45s. Mandatory exit 0 remains; final shutdown timings deployer `24.431s`, worker `22.135s`, minion `8.762s`, transfer `7.369s`, scheduler `20.518s`, conductor `30.689s`.
- Isolated POC in context `virt-infra-dev-buc-hq`, project `default`, namespace/Application `coriolis-worker-validation-20260826`, OCI chart `coriolis/helm/coriolis-operator:0.5.31`, `skipCrds`: preflight node Ready/schedulable/no pressure twice; an initial schema-valid but runtime-incomplete CR produced stable mutation-free `InvalidRuntimeConfiguration` and was normally deleted/recreated with source-backed `local-path` storage. Ownerless zero-replica collision Deployment/RS UIDs (`2858a477-...`, `13e33901-...`) and CR UID `b6082e05-...` held exact across three snapshots spanning 204s with stable `ResourceCollision`, absent acceptedVersion, and no CR-owned/retained/marker/PVC/PV writes. Deleting only the collision at `05:16:01Z` yielded first child `05:16:46Z`, bootstrap `05:18:09Z`, worker Ready `05:18:13Z`, final reconcile `05:18:16Z`, without CR edit/operator restart.
- Live worker passed the exact privileged contract with zero restarts and truthful final conditions; a warning-suppressed fixed-output probe emitted exactly `coriolis-worker-ok`, direct Worker RPC plus scheduler showed exactly one enabled/UP worker, and hashed DB service identity `6c0f0800ab89c2cf30d5bc01eb22e6040a261bb9ab707681a1231ae30d18d983` stayed identical through stability, worker replacement, RabbitMQ recovery, and CR recreation. Normal worker replacement (UID `ca6512f3-24bc-4819-88a5-f8b611ca6398`, Ready `05:37:22Z`) and RabbitMQ replacement (UID `ea8693bd-b493-4987-a33d-2d0da8e044bd`, Ready `05:38:47Z`) were zero-restart with exact image/node; Rabbit PVC identity stayed exact. Normal CR delete/recreate (`b6082e05-...` to `4a701a89-...`) preserved five retained ownerless Secrets and both PVCs exactly and converged all 26 direct resources by about `05:45:30Z`; probe/hash and 20s stability passed after recreation. Normal cleanup removed CR/Application/namespace and both Delete-policy PVs without force/finalizer/owner edits or individual deletion; shared `argocd/coriolis` is `0.5.31` `Synced/Healthy`, operator 1/1 zero restarts, no shared CR, node Ready/schedulable. Accepted: released worker reconciliation, exact privileged host mounts working for Pod/registration lifecycle, registration-only RPC/scheduler/DB identity, collision/recovery/replacement/retention/cleanup. Excluded: provider imports/classes beyond `find_spec`, provider/migration/task/write/Keystone-trust/VDDK/Hyper-V/libvirt/iSCSI/logger workflows, HA/cross-node/storage-persistence production claims, backup/restore, overall readiness.
- Retained local gate evidence: full local gate passed 589 unit tests in 13.88s, Ruff check, Ruff format check (43 files), strict mypy over 17 source files, Helm lint/template, local operator image `d4e637d378ad`, `git diff --check`, and an independent review whose findings were corrected and requalified.

## :material-book-open-page-variant-outline: Accepted Coriolis Deployer-Manager 0.5.29 POC

- Source `4382c000f18cc323bacebc0e1df78c8b2f052d20` was published by CIXpress Default `wk1zo8` (`07:43:50Z`-`07:45:23Z`); top-level and `git-clone`/`kaniko-build`/`helm-update`/`cleanup` were `SUCCEEDED`. CI release `c35afc7032de19ec43f3870b6300e2f4f02370aa` published chart/app/operator `0.5.29`, operator digest `sha256:37634ac8afd4ce7e7445aeb5f5786867cdd009d0f41b13b9baf95b1e92be4b08`.
- The isolated POC used `virt-infra-dev-buc-hq`, project `default`, namespace/Application `coriolis-deployer-manager-validation-20260825`, OCI chart `coriolis/helm/coriolis-operator:0.5.29`, and `skipCrds`. Node preflight recovered from `Ready=False` to two consecutive `Ready=True` observations before mutation and stayed Ready. Ownerless zero-replica Deployment UID `866472b2-adf6-415a-8395-2421a079ab0e`, RV `13913626`, ReplicaSet UID `94388c5f-24f8-4c59-93b6-45d30716762c`, spec hash `831ed01de99bb0854032749f2ede0e613e5ad437620d5f105d9948b2c482a095`, and CR UID/RV `bb144cd3-a3ae-45e6-8eb3-69b00b6eb220`/`13913723` stayed exact over 143s. `ResourceCollision` remained stable with absent acceptedVersion and zero CR-owner refs, managed/retained/marker writes, PVC/PV claims, or restarts.
- Collision deletion at `08:08:04Z` produced first children in 54s, bootstrap in 135s, managed deployer creation in 140s, final reconcile in about 141s, and Available in 143s without CR edit/restart. The live one-replica `Recreate` Deployment pins `sha256:a2a7091daf8e172b96fa0b48d19ffad285d7bfaad42fc7e8cd44a688f06f36aa`, direct command, config Secret only, UID/GID/fsGroup `42434`, restricted security, memory `/tmp` plus `/var/log/coriolis` only, and every documented omitted surface. Initial Pod UID `e998c567-7edf-4ef4-a41d-0428be6ef464` was Ready/zero-restart; `Ready=False/RuntimeNotImplemented` and `Degraded=False/NotDegraded` remained truthful.
- Initial 22s stability passed. Two normal deployer Pod deletions produced distinct Ready/zero-restart/exact-digest replacements (`aed98ed1-ce5b-40d6-b58e-ae309d9f6724`, then `669e570f-1101-4148-b820-531743a6e88d`) and 20s stability; exact replacement latency is deliberately unclaimed because two local monitor assertions failed after deletion. RabbitMQ replacement (`cd8cb7bf-c1eb-4640-b3c9-bc5e8e6969cd` to `9c6a86fb-9034-4139-a9ed-990067457d83`) was requested `08:28:30Z`, created `08:28:33Z`, Ready `08:29:01Z`, zero-restart; deployer stayed unchanged/zero-restart and Rabbit PVC UID/RV/volume remained exact.
- CR recreation created UID `ddfea8e8-baeb-4dc4-84cf-4c0e8cf04a24` at `08:39:39Z`, final transition `08:40:48.942Z`, preserved five ownerless retained Secrets and two Bound PVCs at exact metadata/RV/volume, and transferred 25 direct ephemeral resources (6 ConfigMaps, 8 Deployments, 1 Job, 3 Secrets, 5 Services, 2 StatefulSets). A duplicate local `kubectl create` returned `AlreadyExists` after the new CR existed, so exact recreation-command timing/latency is unclaimed. Cleanup removed the CR/Application/namespace in 23s/6s/73s and both Delete-policy PVs by `08:50:09Z` without force, grace-zero, finalizer patches, owner edits, or individual deletion. Normal Argo Events remain. Shared `argocd/coriolis` is `0.5.29` `Synced/Healthy`; operator exact digest/zero-restart, no shared CR, node Ready/schedulable. Excluded claims: deployer RPC, auto-deployment, Keystone trust, provider/migration/nonempty/write workflows, HA, production storage, backup/restore, and overall readiness.

## :material-book-open-page-variant-outline: Accepted Coriolis Minion-Manager 0.5.28 POC

- Source `5294df9a50e6c8a15c6cad374c66c3a9ab95b077` passed `uv run pytest tests/unit -q` (558 passed), Ruff, 61-file format check, strict mypy over 15 source files, Helm lint/template, operator build `690bdd8663a8`, runtime rerun `SUMMARY runtime passed 255.112`, and diff check. CIXpress Default `f3pgh0` completed `05:18:39Z`-`05:19:50Z` with all expected steps `SUCCEEDED`, publishing CI commit `69c60fdcaec2ec86df99d5cf91520d7051000691`, release `0.5.28`, and operator imageID `sha256:4e1a90f...88886b`.
- In `virt-infra-dev-buc-hq`, project `default`, namespace/Application `coriolis-minion-manager-validation-20260825`, OCI chart `coriolis/helm/coriolis-operator:0.5.28` with `skipCrds`, ownerless zero-replica Deployment UID `403307cd-c06b-4b83-ac0d-83f42221bc27`, RV `13873650`, and digest `070cb7df...8d74` stayed exact with CR UID `13c491c4-6ced-4e0a-8495-459615491336`, stable `ResourceCollision`, absent acceptedVersion, zero writes, and unchanged zero-restart operator across three complete snapshots spanning 295s.
- Collision deletion at `05:42:21Z` produced children in 55s, completed bootstrap in 127s, and reached reconciliation/managed availability in 158s without CR edit/restart. The live owner-referenced one-replica `Recreate` Deployment matched the exact minion image/command, UID/GID/fsGroup `42434`, restricted security, projected config, writable tmp/logs, and no Service/`envFrom`/ports/probes/locks/PVC/init/service-account/resources.
- Fixed-output diagnostics and `get_minion_pools == []` passed before/after 20s stability. Normal minion Pod deletion replaced UID `541a99d0-...` with `b1d07f0d-...` Ready in 7s at the same digest/zero restarts. RabbitMQ replacement changed UID `79e45f28-...` to `31ab40c2-...` Ready in 31s while minion/unrelated identities and RabbitMQ PVC UID/RV remained unchanged; RPC passed on the first post-recovery attempt.
- Normal CR deletion removed old-UID-owned resources in 8s; recreation changed UID to `454dd96c-e204-4512-9f27-6c633d880dd3`, retained five ownerless state Secrets and two Bound PVCs at exact UID/RV, moved 24 ephemeral resources to the new owner, converged in 65s, and passed RPC again. Conditions remained truthful, including `Ready=False/RuntimeNotImplemented` and `Degraded=False/NotDegraded`.
- Normal cleanup removed the CR in 9s, Application in 9s, namespace in 78s, and both Delete-policy PVs without force, finalizer changes, or individual retained-resource deletion. Shared `argocd/coriolis` remained `0.5.28` `Synced/Healthy`; shared operator identity/zero restarts and node readiness were unchanged. Accepted limits exclude nonempty pools, automatic refresh, provisioning, provider operations, Keystone trust, writes, HA, production storage, and overall readiness.

## :material-book-open-page-variant-outline: Accepted Coriolis Transfer-Cron 0.5.27 POC

- Source `293500a0a64f6e10c776d674c141ae1ea3ba05e4` passed 548 unit tests, Ruff, 59-file format check, strict mypy over 14 source files, Helm lint/template, operator build image ID prefix `03163df34d52`, validator `SUMMARY runtime passed 320.708`, and `git diff --check` with zero validator leftovers. CIXpress Default `ermyhd` completed `2026-08-24T19:58:15Z`-`19:59:44Z` with top-level, `git-clone`, `kaniko-build`, `helm-update`, and `cleanup` all `SUCCEEDED`, publishing CI commit `3f3074e68d9b786986e688649646f08bcd6fbd63`, tag/chart/app/operator `0.5.27`, and imageID `sha256:7c70f0e1c54559440d02d5595b054c48c7b0d586f9f61b86aeaf717364b104cf`.
- In `virt-infra-dev-buc-hq`, project `default`, namespace/Application `coriolis-transfer-cron-validation-20260824`, chart `0.5.27` with `skipCrds`, an ownerless zero-replica Deployment (UID `daed6df8-8b2d-4fff-86fc-3d57edc58c64`, RV `13728367`, spec digest `0646ae84...d2f64`) and ReplicaSet UID `21b1cedc-202e-4336-8d79-35fc47339591` held exact at 0/+65/+130s. Initial CR `f8699078-0ebd-4870-9cdb-fa9b688867a5` held stable `ResourceCollision`, absent acceptedVersion, zero owned/retained/marker writes, and no operator restart. Collision deletion at `20:24:07Z` reached first child `20:24:25Z`, bootstrap complete `20:25:48Z`, convergence `20:25:52Z`, and Available transfer `20:25:55Z` (108s), without CR edit/restart.
- The live owner-referenced one-replica `Recreate` transfer Deployment matched `sha256:3a44d3b...2b3ea9c`, direct command, UID/GID/fsGroup `42434`, restricted security, safe environment, projected config, writable tmp/logs, and no Service/`envFrom`/ports/probes/locks/PVC/init/service-account/resources. Pod Ready/zero restarts and fixed probes twice 20s apart passed. Conductor replacement took 45s; transfer replacement took 7s at exact digest/zero restarts and proved empty post-fork schedule load; RabbitMQ replacement took 35s without conductor/scheduler/transfer identity change.
- CR recreation retained five ownerless state Secrets and two Bound PVCs exactly without writes, moved 20 ephemeral resources to new UID `bff7959a-37f3-4450-8593-7ea8c1ec00ac`, completed bootstrap in 51s, and converged in 77s. Final conditions retain `Ready=False/RuntimeNotImplemented`. A jq polling failure was corrected using Kubernetes timestamps for the accepted 7s replacement; a rejected immediate aggregate probe was replaced by independent diagnostics and corrected warning-suppressed `CORIOLIS_MESSAGING_OK` probes. These were tooling-only corrections, not workload changes.
- Cleanup deleted CR/Application/namespace/PVs normally without force, finalizer/patch, or individual resource deletion; exact CR-owned GC time is unclaimed after one broad metadata query failed, but per-kind metadata checks proved no old owner refs. Accepted limits exclude nonempty schedules, cron firing/due transfers, Keystone trust, writes/migrations/providers, in-memory persistence, HA, cross-node/production storage, backup/restore, and overall readiness. No transfer Service/Ingress exists.

## :material-book-open-page-variant-outline: Accepted Coriolis Scheduler 0.5.26 POC

- Implementation source `d4805eabc42a2106ad66300e31791e05dc294702` passed 540 unit tests and the complete local gate. CIXpress Default pipeline `02mop6` (17:07:04Z-17:08:25Z) completed top-level and `git-clone`/`kaniko-build`/`helm-update`/`cleanup` as `SUCCEEDED`, publishing CI commit `de1da0f8a1272d9500f81d872951c3c20e414b8f`, tag/chart/app/operator `0.5.26`.
- Isolated namespace/Application `coriolis-scheduler-validation-20260824` reached `Synced/Healthy` using Argo project `default`; an initial attempt with project `coriolis` was rejected by destination policy before workloads or a CR existed. The released operator was `1/1`, zero restarts, imageID `sha256:1893f0f...5712f5b`.
- An ownerless zero-replica scheduler Deployment (UID `470094cc-...`, resourceVersion `13686184`) and its Kubernetes-owned zero-replica ReplicaSet remained exact at 0/65/130 seconds. Initial CR UID `91219b31-...`, generation/observed `1`, held stable `ResourceCollision` with no operator-created runtime, retained writes, or marker. Deleting only the collision at `17:37:33Z` auto-recovered without CR/operator changes; bootstrap succeeded at `17:39:53Z`, scheduler became available at `17:40:03Z`, and final conditions retained `Ready=False/RuntimeNotImplemented`.
- Live contract matched exact scheduler digest `sha256:45bea9e...a73c41`, one-replica `Recreate`, direct command/config, UID/GID/fsGroup `42434`, container `RuntimeDefault`/drop `ALL`/read-only root, safe `HOME` and `PYTHONDONTWRITEBYTECODE`, projected configuration, writable `/tmp` and `/var/log/coriolis`, and no Service/ports/probes/locks/PVC/init/resources/`envFrom`. Fixed-output scheduler diagnostics plus expected empty-worker `NoWorkerServiceError` passed repeatedly. Normal scheduler Pod replacement took 28 seconds at the same imageID/zero restarts; RabbitMQ replacement took 38 seconds while scheduler/conductor identities stayed unchanged and RPC reconverged.
- CR delete/recreate changed UID `91219b31-...` to `cd348d77-...`, preserved all five retained ownerless state Secrets and both Bound PVCs exactly with no writes, recreated all ephemeral owners under the new UID, converged in 75 seconds, and passed scheduler RPC again. Normal cleanup removed CR-owned resources in 38 seconds, Application in 12 seconds, namespace in 69 seconds, and both Delete-policy PVs without force/finalizer or individual retained-resource deletion. Final POC scope is absent; shared Argo is `0.5.26` `Synced/Healthy`, operator `1/1` zero restarts, no CR, node Ready/schedulable.
- Accepted limits: the POC proves empty-worker scheduler behavior, exact live Deployment identity, collision recovery, scheduler replacement, RabbitMQ recovery, retained no-write recreation, and garbage collection. It does not prove nonempty worker selection, due-transfer execution, Keystone trust, write workflows, HA, cross-node storage, backup/restore, or production readiness. Transfer-cron is locally implemented and validated in the current section above; its publication and released-artifact POC remain required.

## :material-book-open-page-variant-outline: Pre-Release Local Scheduler Controller Validation (History)

- Local source implements only the owner-referenced one-replica `Recreate` scheduler Deployment at exact `cr.virtomat.io/virtomat/coriolis/coriolis-scheduler:2603.4@sha256:45bea9e0bab4cac0fdddee6d3eac52006d12cf7de1e798e2949dd9ebc2a73c41`, direct `/usr/local/bin/coriolis-scheduler --config-file=/etc/coriolis/coriolis.conf`, UID/GID `42434`, hardened read-only root, projected `/etc/coriolis`, and writable `/tmp` and `/var/log/coriolis`, without locks, Service, ports, probes, PVC, or resources.
- Controller coverage validates pre-read/preflight before writes, mutation-free collisions, resourceVersion-required guarded SSA repair, successful-bootstrap gating, conductor then scheduler then API ordering, marker last, and sanitized apply failures that do not write the marker. `Ready=False/RuntimeNotImplemented` remains required; no CRD, Helm values, RBAC, Service, readiness, or version changed.
- Final pre-publication validation passed 540 unit tests, Ruff check, Ruff format check (57 files), strict mypy (13 source files), Helm lint/template, local operator Docker build, and `git diff --check`. This was local-only evidence at that checkpoint and is superseded by the accepted released-artifact POC above.
- Post-review validator hardening rejects declared application-image ports, volumes, or healthchecks, unexpected runtime mounts/tmpfs targets, and mislabelled pull failures. The strengthened exact-image rerun passed 85 stages with 0 failures and `SUMMARY runtime passed 305.963`; graceful shutdown, restart/reprobe, RabbitMQ recovery, and complete auth/Docker cleanup passed. The current full suite is 540 passing tests.
- The exact-image OCI qualification below remains separate at 87 PASS / 0 FAIL. Scheduler publication and POC acceptance are recorded below; transfer-cron is now locally implemented and validated in the current section above.

## :material-book-open-page-variant-outline: Pre-Release Scheduler And Transfer-Cron Runtime Qualification (History)

- The tracked runtime validator now qualifies the exact `2603.4` scheduler image `sha256:45bea9e...a73c41` and transfer-cron image `sha256:3a44d3b...2b3ea9c` through their exact direct commands under `/usr/local/bin/<binary> --config-file=/etc/coriolis/coriolis.conf`; source metadata is root/empty user, linux/amd64, and no ports, volumes, or healthcheck. Both run as numeric UID/GID `42434` with read-only root, dropped `ALL`, no-new-privileges, read-only generated `/etc/coriolis`, writable only `/tmp` and `/var/log/coriolis`, private internal network, no host ports, no Service/listener, and no locks mount.
- Source dependency graph: the scheduler is one messaging worker requiring RabbitMQ that directly reads MariaDB worker-service state; the expected empty database produced `NoWorkerServiceError`, proving that path. It constructs a conductor client, but the inspected qualified scheduler path does not call conductor. Transfer-cron is one messaging worker whose post-fork startup loads nonexpired schedules through conductor RPC and owns an in-memory cron, so RabbitMQ and conductor are startup dependencies and MariaDB is indirect via conductor. Keystone-trust and due-transfer execution paths require nonempty schedules and remain unqualified. Neither workload needs the API as a listener/backend, and neither gets a Service.
- Fixed-output probe proved scheduler diagnostics, the scheduler empty-worker DB path, and transfer-cron diagnostics after schedule load; stable over 20s; leaf-first stop/restart; exact accepted timings transfer `0.901s`, scheduler `1.904s`, conductor `29.181s` within the evidence bound 30; restart/reprobe and RabbitMQ restart/recovery passed with the same container identities. The first two runs exposed an inherited 15s conductor shutdown bound (65 and 75 prior PASS stages); the accepted correction preserved the 15s leaf bounds and exit-0, gave the eight-worker conductor a bounded 30s, and did not weaken assertions.
- Accepted final: 87 PASS, 0 FAIL, `SUMMARY runtime passed 316.069`; fixed-category/output-suppressed; ephemeral registry auth not persisted or reported; complete cleanup with no Docker/scratch/auth leftovers; cluster read-only with no Kubernetes resources. This is pre-release OCI history for scheduler `0.5.26` and transfer-cron `0.5.27`, whose released/live acceptance is recorded above. `Ready=False/RuntimeNotImplemented` remains required. Local focused tests at this checkpoint were 11; the later scheduler controller gate passed 540 tests.
- The 11-test statement is historical to the qualification-only checkpoint; the later scheduler controller gate above passed the current 540-test full suite.

## :material-book-open-page-variant-outline: Accepted Coriolis Conductor 0.5.24 POC

- Implementation source `d7abd778937203c97eb7501c2a2dc1a0c05cd5c4` (`Implement Coriolis conductor reconciliation`) passed the full local gate before publication: `uv run pytest tests/unit -q` (516 passed), Ruff lint/format, strict mypy, Helm lint/template, `docker build -t coriolis-operator:local .`, the tracked exact-image validator (three passes, final `272.660s`), and `git diff --check`. CIXpress Default pipeline `nd4xu7` (2026-08-23 20:54:39Z-20:55:52Z; top-level and `git-clone`/`kaniko-build`/`helm-update`/`cleanup` all `SUCCEEDED`) published CI release commit `56c0b29dbfc2883633b9cd20578ecae7f124b5a7`, chart/app/operator `0.5.24`. One initial `INPROGRESS` detail carried stale completion metadata and then resolved to consistent terminal success.
- Isolated POC in `virt-infra-dev-buc-hq` namespace/Application `coriolis-conductor-validation-20260823` at exact chart/operator `0.5.24`: Application `Synced/Healthy`, operator `1/1` zero restarts. An unmanaged conductor Deployment (UID `1759bccc-...`, resourceVersion `13359017`, replicas `0`, ownerless) stayed spec-exact at 0/65/130s; the initial CR (UID `395d9db8-...`, resourceVersion `13359131`, generation/observed `1`) had acceptedVersion absent, stable `ResourceCollision` conditions/RV across 130s, and zero runtime/retained writes. Conflict deletion at `21:11:07Z` auto-recovered without a CR edit or operator restart; first children ~67s later, bootstrap Job completed in 74s at ~`21:13:35Z`, convergence ~148s after deletion. Final conditions `Accepted=True`/`Reconciled=True`/`Degraded=False`/`Ready=False/RuntimeNotImplemented`/`Progressing=False/RuntimeNotImplemented`/`Upgradeable=False`.
- Happy conductor path: owner-referenced one-replica `Recreate` Deployment at exact conductor digest `sha256:27495f44...c09c7`, qualified direct command/configuration, restricted `42434` security, projected configuration, three writable paths, no Service/ports/probes/init/resources/PVC; dependencies/API healthy, API EndpointSlice TCP `7667`. An authenticated output-free verifier proved Keystone-authenticated `GET /v1/{project_id}/endpoints` HTTP `200`/list (API→RabbitMQ→conductor→MariaDB) before and after normal conductor Pod deletion (Pod UID `7150baef...` → `ab6c58d4...` in ~6s, exact imageID, zero restarts, stable 20s); API unauth `401`.
- CR delete/recreate (old UID `395d...` to new UID `4f2fbbb0-...`): all five retained ownerless state Secrets and two retained Bound PVCs preserved exact UID/resourceVersion/creation/retention and PVC volume names with no writes; owner resources recreated under the new UID, convergence ~71s, bootstrap exit `0`/restarts `0`, third authenticated verifier and `401` passed.
- Normal cleanup removed the CR, Application absent ~3s, namespace absent ~77s, exact Delete-policy PVs immediately, without force/finalizer changes or individual retained/PVC/PV deletion; final namespace/Application/CR/PVs/prefix resources absent. Shared `argocd/coriolis` remains `0.5.24` `Synced/Healthy`, operator `1/1` zero restarts, no CR, node Ready/schedulable.
- Accepted scope and limits: authenticated read-only conductor RPC is accepted; at that checkpoint, write workflows/diagnostics and scheduler/workers/transfer-cron/minion/deployer remained absent. Deployer-manager is subsequently released and POC-accepted as `0.5.29` above; `Ready=False/RuntimeNotImplemented` remains required. Privileged worker and remaining application workloads then Ingress follow. Production CSI/cross-node, backup/restore, HA, RPO/RTO, and rotation remain deferred.

## :material-book-open-page-variant-outline: Pre-Release Local Coriolis Conductor Qualification (History)

- Pre-publication exact-image qualification: exact pinned image `coriolis-conductor:2603.4@sha256:27495f44fbb8b320098d0aa04cd9dcb2a4b432e57aa17417606efc5403ac09c7` passed the extended tracked validator `scripts/validate-coriolis-bootstrap-runtime.py` three times; the final run completed in `272.660s` (`SUMMARY runtime passed`) and cleanup left no resources. Restricted direct `/usr/local/bin/coriolis-conductor --config-file=/etc/coriolis/coriolis.conf` ran as UID/GID `42434`, read-only root, dropped `ALL` capabilities, no-new-privileges, read-only generated configuration, and writable `/tmp`, `/var/log/coriolis`, and `/opt/coriolis/locks` on an internal network with no host port. Local qualification also proved a clean conductor stop/restart and RabbitMQ reconnect without recreation; no secrets/tokens/raw values were emitted.
- Local operator reconciliation applies one owner-referenced `Recreate` conductor Deployment only after a successful immutable common bootstrap and before the marker, with complete preflight and collision-safe guarded SSA. This was local pre-release evidence; the slice was subsequently released and POC-accepted as `0.5.24` (see the accepted section above).

## :material-book-open-page-variant-outline: Accepted Coriolis API 0.5.22 POC

- Implementation source `5a06759d6e0332345262f990b2fb56d9d82fbef7` (`Implement Coriolis API reconciliation`) passed the full local gate before publication: `uv run pytest tests/unit -q` (503 passed), Ruff lint/format, strict mypy, Helm lint/template, `docker build -t coriolis-operator:local .`, and `git diff --check`. CIXpress Default pipeline `44muez` (2026-08-23 18:45:00Z-18:46:33Z; top-level and `git-clone`/`kaniko-build`/`helm-update`/`cleanup` all `SUCCEEDED`) published CI release commit `ef86249cecee812482a96da8f41b1985d7cb764b`, chart/app/operator image/tag `0.5.22`. One transient `INPROGRESS` response carried a stale completion timestamp and then resolved to consistent terminal success; all steps succeeded.
- Isolated POC in `virt-infra-dev-buc-hq` namespace/Application `coriolis-api-validation-20260823` at exact chart/image `0.5.22`: Application `Synced/Healthy`, operator `1/1` zero restarts. An unmanaged API Service (UID `c70790a3-c791-4477-9e43-aaac5f1351be`) held over 65s with exact UID/resourceVersion/creation/spec unchanged and no owner refs; the initial CR (UID `b453a56b-e407-4942-b2fc-57aab99a0508`, generation `1`) had no acceptedVersion, `Reconciled=False/ResourceCollision`, no status churn, and zero CR-owned or new retained writes. Conflict deletion at `18:57:46Z` auto-recovered without a CR edit or operator restart in about 127s; bootstrap Job completed in 77s; all dependencies `1/1`.
- Happy API: owner-referenced ClusterIP Service ready EndpointSlice TCP `7667` and one-replica `Recreate` Deployment at exact API digest `sha256:fce6369f07ef777b5174d3a4f849d4eac914256a20a47ffa0cd1c98081be2705`, direct binary, restricted UID/GID/fsGroup `42434` with read-only root, projected `/etc/coriolis`, and evidenced writable paths; API Pod Ready zero restarts; Service-DNS `/v1` exact unauthenticated `401`; marker last; final `Accepted=True`/`Reconciled=True`/`Degraded=False`/`Ready=False/RuntimeNotImplemented`.
- Deployment collision/recreation: old CR deleted normally; five retained state Secrets and the MariaDB/RabbitMQ PVCs remained exact no-write. New CR UID `8aead0cb-3dd3-4002-95cb-b14de37a71b3` hit an unmanaged zero-replica Deployment (UID prefix `8407babd`) held about 70s unchanged at `Reconciled=False/ResourceCollision`; conflict deletion at `19:04:40Z` auto-recovered in about 73s with retained metadata/resourceVersions/PVC volumes unchanged and bootstrap/dependencies/API healthy under the new owner UID. Normal API Pod deletion replaced with a distinct UID in about 5s, same exact imageID, zero restarts, ready endpoint, `401`.
- Normal cleanup removed CR-owned children in 5s and the exact Application, namespace, and both Delete-policy PVs without force/finalizer patches or individual retained/PV deletion; final Application/namespace/CR/PVs absent; shared `argocd/coriolis` resolves `0.5.22` `Synced/Healthy`, operator `1/1`, no CR, node Ready/schedulable. Authenticated workflow completeness beyond the verified read-only RPC smoke remains unclaimed; `Ready=False/RuntimeNotImplemented` remains required. The conductor slice is now released and POC-accepted as `0.5.24` (see the section above); remaining application workloads and Ingress follow.

## :material-book-open-page-variant-outline: Local Coriolis API Runtime Validation

- Exact image qualification used `coriolis-api:2603.4@sha256:fce6369f07ef777b5174d3a4f849d4eac914256a20a47ffa0cd1c98081be2705` with synthetic operator-rendered configuration and fixed-category/output-suppressed diagnostics. Direct `/usr/local/bin/coriolis-api --worker-process-count 1 --config-file=/etc/coriolis/coriolis.conf` ran as UID/GID `42434`, read-only root, dropped `ALL`, no-new-privileges, read-only `/etc/coriolis`, and writable `/tmp`, `/var/log/coriolis`, and `/opt/coriolis/locks`. It produced one master and one worker, remained stable for 30 seconds, returned exact unauthenticated HTTP `401` for `/v1`, passed the exact exec probe, and stopped normally with exit `0` in `0.848s`. No real credentials, raw logs, environments, configuration, headers, or response bodies were emitted; all disposable containers and scratch files were removed.
- Manifest coverage fixes the exact image, direct command, one replica, `Recreate`, Service/port/selectors, projected ConfigMap/Secret items and modes, three writable `emptyDir` mounts, UID/GID/fsGroup, restricted security context, disabled service links/token automount, termination, and startup/readiness/liveness probes. It rejects credential environment variables, `subPath`, init containers, resource invention, Apache/TLS assumptions, and extra RBAC.
- Controller coverage proves all API reads/classification precede writes; bootstrap active state creates no API resource; absent API Service/Deployment apply after succeeded bootstrap and before marker; both collision positions are mutation-free; managed resources use resourceVersion-guarded SSA; missing resourceVersions stop before writes; and Service/Deployment apply failures are sanitized and skip the marker. Full local validation passes: `uv run pytest tests/unit -q` (503 passed), Ruff lint/format, strict mypy, Helm lint/template, container build, and `git diff --check`.
- This is local pre-release implementation evidence; the slice was subsequently released and POC-accepted as `0.5.22` (see the accepted section above). `Ready=False/RuntimeNotImplemented` remains expected because conductor and the remaining application workloads/internal checks are absent.

## :material-book-open-page-variant-outline: Accepted Release 0.5.20 Collision Recovery POC

- Corrective source `5f52c7004b3b393d85c10b07e9301d3fa3587164` (`Continue collision recovery retries`) passed Default pipeline `0km4kz` (15:10:28Z-15:11:36Z; top-level and `git-clone`/`kaniko-build`/`helm-update`/`cleanup` all `SUCCEEDED`) and released `0.5.20` at CI commit `61c7b3a0d0cea2e81b290023fa8ee5605e2ff261`; the POC operator imageID was `sha256:8d537d978783496338ba4bda60d412690b9c3bd24f6187e31ee8d1de4735e190`. Full local validation passed: 490 unit tests, Ruff lint/format, mypy, Helm lint/template, container build, and `git diff --check`.
- Fresh isolated namespace/Application `coriolis-collision-recovery-20260823` used exact chart/image `0.5.20`. The unmanaged immutable bootstrap ConfigMap collision held for 135s, over two timer intervals, with conflict UID/resourceVersion/creation/immutable/hash unchanged; CR `90548df2-6321-409e-98fb-7130975200e5` remained generation `1`, resourceVersion `13263783`, `Reconciled=False/ResourceCollision`; operator Pod `076dc2e0-583e-45c8-ab44-eb1f56ec7df7` stayed zero-restart; no appliance children or status churn occurred.
- Conflict deletion at `15:25:20.744Z` started recovery at `15:26:12.507Z` (51.763s) and reached final convergence at `15:27:40.726Z` (139.982s total). The corrective retry crossed `BootstrapRunning` and continued without a user CR/spec change, generation change, or operator restart. Final CR retained UID/generation/observed generation `1`, with `acceptedVersion=2603.4`, `Accepted=True`, `Reconciled=True`, `Degraded=False`, and `Ready=False/RuntimeNotImplemented`; Job completion at `15:27:34Z` was `succeeded=1`, `failed=0`, Pod exit `0`, restarts `0`.
- MariaDB/RabbitMQ StatefulSets and Memcached/Keystone Deployments were `1/1`; four Services each had one ready EndpointSlice. The replacement immutable bootstrap ConfigMap had a distinct UID and correct ownership, and the operator-state marker was present. Child RBAC has no list/watch/delete. Exact Application `0.5.20` was `Synced/Healthy`. Normal cleanup removed CR-owned children in 1s, exact Application in 18s, namespace in 50s, and both Delete-policy PVs without force/finalizer changes or individual retained-resource/PV deletion; final Application/namespace/CR/PVs are absent, node Ready/schedulable. Shared `argocd/coriolis` `0.*.*` resolves to `0.5.20`, `Synced/Healthy`, operator image `0.5.20`, ready/available `1/1`.

## :material-book-open-page-variant-outline: Release 0.5.19 Retry-Continuation Regression

- Release `0.5.19` is published but its POC is unaccepted: source `c89b57fcd5b2ee160c1a0986be9fedc9edc19ec8`, pipeline `mg04ql`, and CI release `3a2af04270762069f3d50905ad6f4255d699c302`. Persistent collision remained no-churn. Conflict deletion at `14:50:14Z` started timer recovery at `14:50:42Z` (28s), but the subsequent Kopf retry saw `BootstrapRunning` rather than `ResourceCollision` and no-oped.
- The Job completed at `14:51:42Z` with exit `0` and restarts `0`, and dependencies were healthy, but the CR remained `Reconciled=False/BootstrapRunning` for more than 10 minutes. This is not accepted POC evidence.
- Corrective coverage added the regression: the collision call publishes `BootstrapRunning` and raises `kopf.TemporaryError`; a subsequent `retry=1` call with persisted `BootstrapRunning` continues reconciliation and publishes the accepted-status delta. Noncollision `retry=0` remains a no-op. `uv run pytest tests/unit/test_reconcile.py -k 'resource_collision_predicate or collision_timer'` passed: 11 selected tests. Full validation passed at 490 unit tests, Ruff lint/format, mypy, Helm lint/template, container build, and `git diff --check`; accepted `0.5.20` evidence above closes the release and re-POC gate.

## :material-book-open-page-variant-outline: Local Bounded Collision Recovery Validation

- `uv run pytest tests/unit/test_reconcile.py -k 'resource_collision_predicate or collision_timer or unchanged_computed_status or unchanged_retry_status or handler_updates_patch_status or handler_patches_sanitized_status_before_retry'` passed: 14 selected tests. Coverage includes the exact persisted `Reconciled=False/ResourceCollision` predicate, malformed/noncollision no-ops, timer routing through normal reconciliation, persistent-collision status-churn suppression followed by recovered-status publication, normal and retry unchanged computed-status patch suppression, and existing handler behavior.
- At the initial local stage, full validation passed at 489 unit tests, Ruff lint/format, mypy, Helm lint/template, container build, and `git diff --check`. The implementation added only a 60-second initial-delay/60-second interval collision-gated timer, with no RBAC, CRD, watch, finalizer, destructive behavior, broad periodic reconciliation, or readiness change; the later `0.5.19` regression and accepted `0.5.20` correction are recorded above.

## :material-book-open-page-variant-outline: Released RabbitMQ Validation And POC

- Source `6a5a2b589c0dbfc2f5734f5863f9f8591c5f8c2d` passed successful Default pipeline `qhvqt1` (13:59:46-14:01:07 UTC) and released `0.5.10` at CI commit `c48fd79622a8e760591333bf1ab6a0aa25d2f9d3`. Its isolated clean bootstrap/authenticated-AMQP POC was not accepted: three sequential readiness diagnostics at period `5s`/timeout `5s` caused 37 timeout failures and Endpoint flapping despite zero restarts.
- Fix source `0d52c57aea1345213e519622106bb7b78236c0f1` changed readiness to period `10s`/timeout `15s`. Full validation passed: `uv run pytest tests/unit` (355 passed), Ruff lint/format, mypy, Helm lint/template, and `git diff --check`. Successful Default pipeline `4nj6f5` (14:14:34-14:15:38 UTC) released `0.5.11` at CI commit `3985a677100a55844dc07ac74a30a24e1e2b03e0`.
- Coverage includes optional explicit RabbitMQ storage/resources settings validation; collision-first reads and preflight; exact ownerless retained RWO Filesystem PVC create/no-write reuse; owner ConfigMap and restricted one-replica StatefulSet construction; existing Service and retained infrastructure-Secret references; direct file-only bootstrap/probes; guarded SSA; no new RBAC verbs; MariaDB then RabbitMQ then Memcached writes; and marker-last ordering. `Ready=False/RuntimeNotImplemented` remains expected.
- Before publication, local image/runtime evidence covered approved `rabbitmq:2023.1-ubuntu-jammy@sha256:a595bf6f306ded2b6ad01f068ef69255df72eb73d471ba73ce9bbf0470d15d8a`, image ID `sha256:f9e28ef3ed172cfdda9e6c3d56c509ceaee672b516381343244ed40332a19e73`, direct server/diagnostics, restricted filesystem/security context, file-only definitions, retained-volume restart, and sanitized broker checks. That local evidence was not a Kubernetes POC.
- The accepted `0.5.11` POC in `coriolis-rabbitmq-validation-20260822` used single-node `local-path`, operator imageID `sha256:fbc0cd338f79bf8a7ea06446f05210859ce549b387371594eee02b0ddef8a724`, the approved Rabbit digest, and approved helper Coriolis API digest prefix `fce636...2705`. Probe-fix rollout produced eight consecutive 15-second Ready/endpoint samples and preserved a durable AMQP marker. Normal Pod deletion replaced in 4s, reached Ready in 29s with zero restarts/same digest, stayed stable for four samples, and preserved the marker. CR deletion removed owned resources in 32s while retained PVCs/credential Secrets preserved exact UIDs and unchanged resourceVersions; recreate reached Rabbit StatefulSet in 2s and Ready in 31s, preserved/authenticated the marker, and used new owner UID references.
- Final clean-storage smoke reached Rabbit StatefulSet in 59s and Ready in 94s with fresh ownerless Bound Rabbit PVC/PV, zero restarts, corrected probe, no readiness timeouts, four stable samples, and ready endpoint. Service-DNS authenticated AMQP durable publish/consume/ack and queue deletion passed. Normal cleanup removed owned resources in 20s, namespace in 78s, and final Rabbit/Maria Delete-policy PVs in 1s; all earlier POC PVs, release, helpers, and namespace are absent. CSI/cross-node, backup/restore, HA, RPO/RTO, credential rotation, and production storage remain open. Keystone is now complete and accepted as released `0.5.14` POC evidence (recorded below).

## :material-book-open-page-variant-outline: Keystone Runtime Evidence Checkpoint

- Local disposable inspection validated exact Keystone digest `sha256:7c57962762f5e6fdb1a109097e8f3e2e5f6218ad9c09f10a585adb67ed245cf0`, image ID `sha256:94cd15e8f645f97f65bd21a38713a13b5da44c67049de3a06436e0741f66d1ec`, Linux/amd64, Keystone `23.0.2`, unset OCI user/root default, account `42425:42425` with supplemental group `42400`, default `dumb-init --single-child --` plus `kolla_start`, and no declared port or healthcheck.
- The default command fails without Kolla `config.json`; no Keystone configuration, Apache site, Fernet keys, or credential keys are baked in. Tracked validator `scripts/validate-keystone-runtime.py` completed in `69.063s` with file-only inputs and sanitized fixed-category output. Dedicated database creation/user/password/grant, repeated SQL, TCP authentication, table access, unrelated-database rejection, normal MariaDB stop, retained-data restart, schema sync/repeat/check, key setup/repeat, exact key metadata, bootstrap/repeat, direct WSGI, `/v3`, token authentication, normal restart, repeat authentication, and cleanup passed.
- WSGI first/restart health took `5.628s`/`5.720s`; token authentication took `1.480s`/`1.618s`; normal stops took `0.297s`/`0.277s`. Runtime used UID/GID `42425:42425`, supplemental group `42400`, read-only root, dropped capabilities, no-new-privileges, private networking, read-only configuration, retained key state, and limited tmpfs writable paths. No credentials, SQL values, DSNs, tokens, key contents/hashes, headers, bodies, raw logs, or environments were emitted. All containers, networks, volumes, scratch files, and generated credentials were removed. No Kubernetes, reconciliation, CRD, RBAC, release, or deployment change occurred.

## :material-book-open-page-variant-outline: Keystone Reconciliation Validation

- Local implementation covers independent retained database/Fernet/credential Secret generation, exact no-write reuse, semantic and collision checks, generated non-sensitive/sensitive configuration partitioning, dedicated MariaDB bootstrap and rollout annotation, the restricted direct-WSGI Deployment, and exact command/probe/security/mount contracts.
- Controller coverage proves all six Keystone reads occur before mutation; every read and collision position is mutation-free or sanitized; retained creates precede MariaDB; owner resources follow Memcached; managed resources use guarded SSA; create and patch failures stop at every Keystone write position; and the marker remains last. `Ready=False/RuntimeNotImplemented` remains expected.
- Full local validation passed: `uv run --offline pytest tests/unit -q` (397 passed), `uv run --offline ruff check .`, `uv run --offline ruff format --check .`, `uv run --offline mypy src`, `helm lint helm/`, `helm template coriolis-operator helm/ --include-crds`, and `git diff --check`. The implementation is now released as `0.5.14` with accepted released-artifact POC evidence; no CRD or RBAC changed.

## :material-book-open-page-variant-outline: Released Keystone 0.5.14 Validation And POC

- Keystone implementation source `f90cae4` was published by CIXpress pipeline `c2pajn`; all expected `git-clone`/`kaniko-build`/`helm-update`/`cleanup` steps `SUCCEEDED`, releasing `0.5.13` at CI commit `7eb2215`. Its isolated single-node POC was not accepted: the Keystone prepare init failed before db-sync/bootstrap because non-root UID/fsGroup `42425` could not chmod the root-owned emptyDir mount roots during `install -d -m 0700`; MariaDB, RabbitMQ, and Memcached stayed healthy.
- Fix source `087bbc2` (`Fix Keystone emptyDir preparation`) passed 398 unit tests plus Ruff lint/format, mypy, and `git diff --check`; pipeline `fhzlg6` succeeded at all four expected steps and released accepted `0.5.14` at CI commit `edd349a`. A direct local Helm OCI pull returned HTTP `401` before any disposable Helm release or CR was created, so the exact `0.5.14` chart was safely promoted by a disposable Argo Application in project `default` using the existing registry integration, an existing compatible CRD, and `skipCrds`; no credentials were exposed.
- Accepted isolated single-node POC: Keystone reached `Available` about 90 seconds after CR creation; prepare, db-sync, and bootstrap init containers exited `0`; `/v3` returned 200 and an authenticated token request returned 201 with a token header present on the restricted direct-WSGI one-replica `Recreate` Deployment, with ready Service/EndpointSlice and zero restarts across all workloads. Normal Pod replacement produced a distinct UID, the exact same image digest, zero restarts, init exit `0`, unchanged retained Keystone Secret UID/resourceVersion/timestamp, and passing probes/auth. CR deletion/recreation removed owned resources, reused retained Keystone Secrets and MariaDB/RabbitMQ PVCs exactly without writes under a new CR UID/ownerRefs, reached Keystone `Available` about 37 seconds later, and passed idempotent init/probes/auth.
- A deliberate unmanaged Keystone ConfigMap collision remained unchanged with `Reconciled=False/ResourceCollision` and zero managed writes; removing it restored healthy reconciliation. This is historical `0.5.14` evidence; the accepted `0.5.17` Coriolis-common record is below.

## :material-book-open-page-variant-outline: Accepted Coriolis-Common 0.5.17 POC

- Fix source `764b9952d1e1c9bc1cbce08afddea8781f391f42` was published by CIXpress pipeline `ectoq4` (`2026-08-23T12:16:43Z`-`12:18:06Z`), with all expected steps `SUCCEEDED`, as `0.5.17` at CI commit `6bfc494d50949b7a5aa770c4febb7c5100b3b363`; operator imageID is `sha256:443e6e5dec8cd6e7f2040ca4fe1f5dcfcfa40ad36184cb30dc54a4be7547d8a6`. Historical `0.5.16` v1 failed on provider-continuation parsing before dbsync and correctly remained `BootstrapFailed`, markerless, and unmodified.
- v2 completed in place in 31s (`12:23:11Z`-`12:23:42Z`), `succeeded=1`, `failed=0`, exit `0`, restarts `0`, on the exact conductor digest; v1 remained failed and untouched. Dependencies were `1/1` with one endpoint each. Final recreated state was v2 only, v1 absent, `acceptedVersion=2603.4`, `Accepted=True`, `Reconciled=True`, `Degraded=False`, `Ready=False/RuntimeNotImplemented`.
- Output-suppressed exact-image verification passed twice in 10s, including post-recreation, covering required schema tables, unique enabled user/default project/password auth/admin assignment, migration service, and exactly one RegionOne admin/internal/public endpoint each. No secrets, logs, or raw data were emitted. Resume reused the completed immutable Job/ConfigMap exactly without writes; replacement operator was ready in 4s with zero restarts. Local validation passed 477 tests, Ruff, mypy, Helm, parser, Docker build, diff, and exact renderer runtime `135.212s`.
- Collision removal is not automatic: the unmanaged immutable v2 ConfigMap remained exact and produced `ResourceCollision` with zero newly owned writes; five retained ownerless Secrets and MariaDB/RabbitMQ PVCs remained exact and Bound. Removing the conflict alone did not reconcile because metadata/children are not watched. Normal operator replacement invoked Kopf resume in 6s and converged in 124s; this remains an operational follow-up. Cleanup left only expected retained Secrets during the 300s CR observation; Application/namespace/both Delete-policy PVs deleted in 11s/53s/9s. POC application, namespace, and PVs are absent; shared `argocd/coriolis` `0.*.*` is `0.5.17` Synced/Healthy, operator `1/1` zero restarts, node Ready/schedulable. No application workload, Ingress, or `Ready=True`; single-node local-path only and production gates remain open.

## :material-book-open-page-variant-outline: Current MariaDB Reconciliation Validation

- The development stack through MariaDB reconciliation is published on `origin/dev` at `55212b0`, including the foundational gate, four-Service slice, MariaDB evidence/contract, and pure preparation commit `5a2dfce`; it is undeployed.
- Validation passed before publication: `uv run pytest tests/unit` (316 passed); Ruff lint/format; mypy; Helm lint/template; and `git diff --check`.
- Coverage includes the marker-plus-four foundational gate and Service slice, followed by MariaDB desired-state preparation and local reconciliation integration: ordered reads; only `404` absent; all validation, classification, preflight, rendering, and manifest construction before writes; stable mutation-free `InvalidRuntimeConfiguration`; exact PVC create/no-write reuse; guarded ConfigMap, Secret, Service, and StatefulSet SSA; foundational writes, Services in frozen order, MariaDB resources, then marker last; PVC `get`/`create`; StatefulSet `get`/`create`/`patch`; no rollback; sanitized retry failures; stable `ResourceCollision`; and `Ready=False/RuntimeNotImplemented`.

The four-Service slice, MariaDB pure desired-state preparation, and reconciliation are part of the published `55212b0` development stack on `origin/dev` and released operator `0.5.6`. Production backup/restore, HA, and RPO/RTO remain open; `Ready=False/RuntimeNotImplemented` remains truthful.

## :material-book-open-page-variant-outline: Released Memcached Validation And POC

- Source `063e438ef416599e9816a2400afcc5a5a7af9aa0` ("Implement Memcached reconciliation") was published by CIXpress pipeline `4dcpfk` using `Default`, started `2026-08-22T12:29:20+00:00`, completed `12:30:25+00:00`, and reported top-level plus `git-clone`, `kaniko-build`, `helm-update`, and `cleanup` as `SUCCEEDED`. CI-owned commit `cb6b055eaf5e74c99e26c1c3d662b2d749331627` released chart/app/image `0.5.8`.
- In disposable single-node namespace `coriolis-memcached-validation-20260822`, CR `memcached-validation` created the Memcached Deployment in 44s; the original Deployment reached Ready 3s later, and MariaDB and Memcached both reached `1/1`. The released operator image was `cr.virtomat.io/virtomat/coriolis/operator:0.5.8` with imageID `sha256:9af4b018c2a7c0a23635d115d5335477b17bb81a731979bd0c93083c88461af4`; the Memcached Pod imageID matched approved digest `sha256:746b93082a4f6d07f464e93d4b14f5e30510abf17a9ae0a4af20e111408c8f1e`.
- The live Deployment passed its CR owner, one-replica `Recreate`, exact approved Memcached digest, direct command/args, port/pull Secret, UID/GID `42457`, disabled automount/service links, 30-second grace, restricted context, exact protocol probes, and absence of init containers, volumes/mounts, environment, configuration, credentials, PVC, and resource API. The Service selector and EndpointSlice targeted the Ready Pod on TCP `11211`.
- Service-DNS `version` and fixed set/get passed. Normal Pod deletion produced a distinct-UID replacement Ready in 3.603s with zero restarts, the same digest, and a ready endpoint; the original key returned only `END`, then fresh version/set/get passed. This proves ephemerality only, not persistence, HA, credentials, configuration, resource API, production readiness, or overall appliance readiness.
- The CR correctly remained `Ready=False/RuntimeNotImplemented` while reconciled dependencies were healthy. Normal cleanup removed the CR, Helm release, namespace, retained PVC, Delete-policy PV `pvc-b2856ccb-88ed-4d60-9878-89ef06331be8`, and copied registry Secret; at that POC cleanup point the namespace/PV/release were absent, the node was Ready and schedulable, and zero appliances remained cluster-wide. That historical POC used Argo `coriolis-operator:0.5.8`; the later Keystone POC cleanup observed healthy `0.5.14`, `1/1`, with zero appliance CRs and no POC workload.
- That Memcached source baseline remains `uv run --offline pytest tests/unit` (326 passed), Ruff lint/format, mypy, Helm lint/template, and `git diff --check`. RabbitMQ `0.5.11` and Keystone `0.5.14` accepted released-artifact POC evidence are documented above. MariaDB CSI/cross-node and production gates remain open.

## :material-book-open-page-variant-outline: Live MariaDB POC

- CIXpress pipeline `8nownk` succeeded for source `6ba9c7e`; the isolated operator pulled `0.5.5` at `sha256:14e991746aaf42334f2e48b2982493c8a6544909e13bf9d55f80eccb50fa062e`.
- Clean first boot on `0.5.5` exposed anonymous MariaDB accounts shadowing `coriolis@%`; commit `3ee5d2d` adds `--skip-test-db`, passed `uv run --offline pytest tests/unit` (316 passed), Ruff lint/format, mypy, Helm lint/template, and `git diff --check`, and was released by pipeline `kpv306` as `0.5.6`.
- Released `0.5.6` clean first boot on single-node `local-path` passed without repair: PVC Bound in 4 seconds, Pod Ready in 17 seconds, zero anonymous accounts/test database, `fsGroup: 42434` write access, authenticated probes, retained-PVC and credential identity across CR recreation, no-write PVC reuse, database persistence, normal 12-second pod termination, and healthy same-node remount.
- CSI and cross-node attach/detach were not available in this one-node environment. Cleanup removed the CR, release, namespace, PVC, and reclaimed local-path PV without force or finalizer changes.

Barbican and all other Services remain deferred; no Ingress route is emitted before its backend Service exists.

For the controller skeleton, these local validations have passed:

- Python 3.12 Ruff format.
- Ruff lint.
- mypy.
- Unit tests.
- Helm lint.
- Helm template with CRDs.
- Container image build and non-root runtime identity.
- `git diff --check`.

For the local API-only `core` runtime slice, these local validations passed:

- 25 unit tests covering the CRD schema (profile enum/default, required non-empty `spec.version`, `status.acceptedVersion`, and the absence of CEL immutability rules), controller enforcement of the immutable accepted version, rejection and version-change-blocked conditions, the accepted API-only reconcile, profile defaulting and profile-change routing, and no-resource paths.
- Ruff lint.
- mypy.
- Helm lint and Helm template with CRDs.
- `git diff --check`.

This API slice is committed locally at `ab9df83` (branch `dev`, not pushed/deployed) and is absent from the deployed operator. No cluster or external service was changed by this API slice; the image mirror/pull gate remains passed, and no dependencies, bootstrap Jobs, services, storage, secrets, or Coriolis runtime workloads are implemented or deployed.

For the local metadata-only helper slice, these local validations passed:

- 44 unit tests covering `appliance_resource_name` (single lowercase DNS label <=63; dotted/overflow dot-to-hyphen prefix plus 12-character SHA-256; invalid appliance/component rejection), `appliance_identity`, `build_resource_metadata` (standard `app.kubernetes.io/*` and `coriolis.cloudbase.it/*` labels, full appliance-name annotation, exactly one of owner reference or retention), and `build_state_config_map` (standard metadata with component `operator-state` while retaining the shipped `state_config_map_name`).
- `uv run ruff check .` and `uv run ruff format --check .`.
- `uv run mypy src`.
- `git diff --check`.

The helper slice is committed locally at `fbab6e5` on `dev`, but not pushed or deployed; the deployed marker `0.5.3` is unchanged and carries no standard labels. The collision/migration marker API-layer slice is described below; retained-resource adoption and all runtime resource construction remain deferred.

For the local collision/migration marker API-layer slice, these local validations passed:

- 70 unit tests covering the pre-read classification (404 create; fully matching managed marker proceeds with unchanged body; compatible legacy `0.5.2`/`0.5.3` marker normalization in place with stale generation updated, including dotted/long names; `ResourceCollision` for partial/conflicting standard metadata, owner mismatch, incompatible legacy data, and owner-plus-retention metadata, never patching/adopting/deleting/renaming; preservation of a prior `acceptedVersion` and condition transition time), non-404 read error propagation without patching, `V1ConfigMap` object handling, and deterministic `ResourceCollision` conditions.
- `uv run ruff check .` and `uv run ruff format --check .`.
- `uv run mypy src`.
- `helm lint helm/` and `helm template coriolis-operator helm/ --include-crds`.
- `git diff --check`.

This slice is **committed locally at `d8df00f` on `dev`, but not pushed or deployed**; the deployed marker `0.5.3` is unchanged and lacks these pre-read/collision semantics. ConfigMap RBAC gains only `get`.

For the local retained-resource authorization/classification slice, these local validations passed:

- 94 unit tests (24 new for this slice) covering the pure `classify_retained_resource` classifier returning `RetainedClassification.ABSENT/REUSE/COLLISION`: absent resource eligible for creation; exact matching PVC/state Secret/CA-state reuse; changed creating-appliance UID with otherwise exact retained identity is `REUSE` (UID is deliberately ignored; a stale `coriolis.cloudbase.it/appliance-uid` annotation is treated as unrelated); name/namespace/appliance/component/retention mismatches collide; missing/partial labels and annotations collide; any owner reference collides (even a matching owner UID); unrelated extra labels/annotations are allowed; the external `coriolis-appliance-registry` Secret fails closed as `COLLISION` both when absent and when forged with exact matching metadata; mapping-shaped dict and real `V1Secret`/`V1PersistentVolumeClaim` model representations; and no input mutation.
- `uv run ruff check .` and `uv run ruff format --check .`.
- `uv run mypy src`.
- `helm lint helm/` and `helm template coriolis-operator helm/ --include-crds`.
- `git diff --check`.

This slice is **committed locally at `1b73045` on `dev`, but not pushed or deployed**; the deployed marker `0.5.3` is unchanged. It constructs/reconciles/patch/reads/adopts no runtime resource and adds no adoption mutations; external/pre-existing resources such as `coriolis-appliance-registry` fail closed as `COLLISION` and remain read-only and outside this classifier/reconciliation policy. A MariaDB vertical slice remains blocked by the remaining generators/builders/RBAC, storage, probes/readiness, and rotation gates.

For the local documentation-only Secret/configuration contract slice, these validations passed:

- No code, builders, values, RBAC, CRD, or runtime behavior changed; the change is confined to [docs/foundational-resource-contract.md](foundational-resource-contract.md) and the tracking/docs pages.
- The modified contract page was reviewed for contradictory claims: concrete Secret/ConfigMap names and key layouts and the primary `coriolis.conf` split are stated as frozen, not unresolved, and the remaining genuinely-unresolved items are listed under the contract's Remaining Unresolved Secret Items and Unresolved Gates.
- `uv run pytest`; `uv run ruff check .`; `uv run ruff format --check .`; `uv run mypy src`; `helm lint helm/`; `helm template coriolis-operator helm/ --include-crds`; `git diff --check`.

This slice is **committed locally at `8ce26ba` on `dev`, but not pushed or deployed**; the deployed `0.5.3` remains marker-only and unchanged, and no runtime resources or adoption mutations exist.

## :material-book-open-page-variant-outline: Pure Secret/ConfigMap Builder Slice

- 116 total tests, including 22 new builder cases from the previous 94; 21 cases matched the final focused selector. Coverage includes deterministic names/standard metadata; ownerless retained credential Secrets with retention metadata; owner-referenced rebuildable configuration resources without retention; exact key sets; opaque caller-provided string inputs without mutation; missing, extra, and non-string input failures without value exposure; `Opaque` UTF-8/base64 Secret `data` with no `stringData`; and plain six-file ConfigMap data that excludes `coriolis.conf` and credentials.
- `uv run ruff check .`; `uv run ruff format --check .`; `uv run mypy src`; `helm lint helm/`; `helm template coriolis-operator helm/ --include-crds`; and `git diff --check` passed.

The five pure builders (`build_coriolis_credentials_secret`, `build_infrastructure_credentials_secret`, `build_step_ca_credentials_secret`, `build_coriolis_config_map`, and `build_coriolis_config_secret`) are **committed locally at `050f16e` on `dev`, but not pushed or deployed**. This covers manifest construction only: no credential generation, `main.py` reconciliation, Kubernetes reads/SSA, RBAC, CRD, runtime resources, status/readiness, or deployment changed; deployed `0.5.3` remains marker-only.

## :material-book-open-page-variant-outline: Pure Retained Credential Generation Slice

- 132 total tests, including 16 new cases from the previous 116, plus focused tests. Coverage includes independent generation of all seven frozen keys through `secrets.token_urlsafe(32)` (32 random bytes/256 bits, URL-safe opaque strings), deterministic token-factory injection for tests only, invalid empty/non-string factory outputs failing without value exposure, unchanged composition with existing builders, and no credential values in failures.
- `uv run ruff check .`; `uv run ruff format --check .`; `uv run mypy src`; `helm lint helm/`; `helm template coriolis-operator helm/ --include-crds`; and `git diff --check` passed.

The pure helpers `generate_coriolis_credentials`, `generate_infrastructure_credentials`, and `generate_step_ca_credentials` are **committed locally at `a604579` on `dev`, but not pushed or deployed**. They are not called by `main.py`; no controller reconciliation, Kubernetes reads/writes/SSA, RBAC, CRD, runtime resources, status/readiness, chart/release, deployment, or rotation changed, and deployed `0.5.3` remains marker-only. The frozen policy is operator-generated only, with no inline CR credential values or external credential Secret source. Runtime generate-once/reuse remains pending: generate only for `ABSENT`, reuse exact matching ownerless retained Secrets unchanged, fail closed on collisions, and defer rotation.

## :material-book-open-page-variant-outline: Retained Secret Semantic Validation/Extraction Slice

- 152 total tests, including 20 new cases from the previous 132, plus focused 20 tests. Coverage includes mapping-shaped objects and Kubernetes `V1Secret` models; optional non-conflicting `apiVersion`/`kind`; required `Opaque` type; rejection of persisted `stringData`; exact frozen `data` keys with string encoded values; strict base64 then UTF-8 decoding; empty decoded-value rejection; a new decoded mapping without input mutation; and fixed/category-only failures without value exposure.
- `uv run ruff check .`; `uv run ruff format --check .`; `uv run mypy src`; `helm lint helm/`; `helm template coriolis-operator helm/ --include-crds`; and `git diff --check` passed.

`validated_retained_secret_values` is **committed locally at `5165629` on `dev`, but not pushed or deployed**. It validates semantics only: no metadata classification, Kubernetes reads/writes, generation, SSA, collision/status handling, or reconciliation. No `main.py`, RBAC, CRD, runtime resource, chart/release, deployment, or rotation behavior changed; deployed `0.5.3` remains marker-only. The subsequent pure preflight classifies metadata first and maps semantic failure fail-closed to `COLLISION`; decoded values remain internal and are never logged, statused, or evented.

## :material-book-open-page-variant-outline: Non-Sensitive Configuration Rendering Slice

- 19 focused renderer tests and 171 total tests passed. Coverage includes explicit input validation, Jinja2 `PackageLoader`/`StrictUndefined` behavior, disabled autoescape, preserved trailing newlines, all six frozen ConfigMap keys, `accepted_version` mapping to legacy `default_coriolis_docker_images_tag`, value-safe failures, wheel resource inspection, and byte comparison against the six verbatim Apache-2.0 upstream templates.
- No `main.py`, reconciliation, Kubernetes reads/writes, SSA, RBAC, CRD, runtime resources, workload, release/chart/image version, deployment, TLS, storage, readiness, bootstrap, or rotation behavior changed.

Pure `render_coriolis_config` is **committed locally at `97153a7` on `dev`, but not pushed or deployed**. It renders no `coriolis.conf`, provider fragments, credentials, or other Secret content; deployed `0.5.3` remains marker-only.

## :material-book-open-page-variant-outline: Pure Foundational Five-Resource Preflight

- 23 focused tests and 194 total tests passed. Coverage includes `OwnedClassification.ABSENT/MANAGED/COLLISION`, `classify_owned_resource`, credential-safe frozen `FoundationalResourcePreflight`, and `preflight_foundational_resources` across exactly three retained credential Secrets plus the owner-referenced configuration ConfigMap and Secret.
- Metadata is classified for all five before retained Secret semantics: exact owner metadata/controller matches are `MANAGED` despite content/type drift; mismatched ownership or retention metadata is `COLLISION`; unrelated extra metadata is allowed; a metadata collision stops semantics and generation. `REUSE` retained Secrets are semantically validated, validation `ValueError` maps to that resource's `COLLISION`, all reusable semantics complete before generation, and generators run only for corresponding `ABSENT` retained Secrets after a collision-free preflight. Generated/decoded credentials are excluded from `repr`, errors, logs, status, and events.
- `uv run ruff check .`; `uv run ruff format --check .`; `uv run mypy src`; `helm lint helm/`; `helm template coriolis-operator helm/ --include-crds`; and `git diff --check` passed.

This pure slice is **committed locally at `35eac9b` on `dev`, but not pushed or deployed**. It changes no `main.py`, Kubernetes reads/writes, SSA, RBAC, CRD, runtime construction/adoption, reconciliation/status behavior, chart/release/image version, deployment, TLS/storage/readiness/bootstrap/rotation behavior; deployed `0.5.3` remains marker-only.

## :material-book-open-page-variant-outline: Sensitive Renderer Contract (Documentation-Only)

- The documentation-only contract is **committed locally at `574efcf` on `dev`, but not pushed or deployed**. No renderer implementation/tests or runtime behavior changed; deployed `0.5.3` remains marker-only.
- Documentation validation reviewed every immutable upstream base-template variable and all 16 provider fragments. It preserved authoritative provider lists/order/module maps, froze no initial custom module overrides and disabled compression/compressor, and confirmed the complete `coriolis.conf` remains exactly one key in the owner-referenced configuration Secret, never ConfigMap/log/status/event/metadata/documentation content.
- At that stage, documentation validation also confirmed explicit internal dependency inputs, source-audited identities/paths, credential mappings, strict value-safe validation, redacted interface boundary, no CRD fields, and exactly `coriolis.cloudbase.it/retention: state-credentials` on all three generated credential Secrets. TLS/CA material, provider connection/private data, optional credentials, dependency Services/workloads, bootstrap, storage, readiness, and rotation remained deferred.
- `git diff --check` passed. This is documentation validation evidence, not code-test evidence; no code test count is claimed.

At `574efcf`, the next sequence was pure renderer implementation/tests, then multi-resource semantics; this is retained documentation-contract history.

## :material-book-open-page-variant-outline: Pure Sensitive Configuration Rendering Slice

- 40 focused configuration tests and 215 total tests passed. Coverage includes `SensitiveCoriolisEndpoints`, `SensitiveCoriolisCredentials`, redacted one-key `SensitiveCoriolisConfig`, `render_sensitive_coriolis_config`, frozen/exact unmutated inputs, redacted credential/output reprs, fixed/category-only value-safe errors, exact one-key composition with the existing configuration-Secret builder, and ConfigMap-boundary rejection.
- Jinja `PackageLoader`, `StrictUndefined`, disabled autoescape, and trailing newline validation passed for the immutable upstream base plus all 16 provider fragments; 17/17 source byte parity and offline wheel inspection of exactly 25 expected template resources passed. Frozen provider lists/order/module maps, source-audited values/paths, source/license attribution, prohibited custom overrides, and disabled compression/compressor are covered.
- Ruff lint/format, strict mypy, Helm lint/template, and `git diff --check` passed.

The pure renderer is **committed locally at `9bb20f3` on `dev`, but not pushed or deployed**; deployed `0.5.3` remains marker-only. No `main.py`, reconciliation, Kubernetes reads/writes/SSA, RBAC, CRD, runtime resources/workloads, chart/release/image version, deployment, TLS/CA/bootstrap, provider/private data, optional credentials, storage, readiness, or rotation behavior changed. Historical five-resource policy remains documentation evidence; the current runtime gate is collision-safe marker-plus-four reads/create/guarded SSA, Secret/ConfigMap `get`/`create`/`patch` RBAC, sanitized status then Kopf retry, and exhaustive tests.

## :material-book-open-page-variant-outline: Foundational Multi-Resource Failure Contract (Documentation-Only)

- The historical, superseded policy in [Foundational Resource Contract](foundational-resource-contract.md) records validation; marker and five-resource pre-reads/classification; pure preparation before mutation; canonical operations; marker last; non-transactional failure handling; and value-safe status outcomes.
- This documentation-only slice changes no runtime code, Kubernetes interaction, RBAC, CRD, resource, chart/release, or deployment behavior. `git diff --check` is its only validation; no code test count is claimed. The current code coverage fact remains that API failures presently propagate, which is distinct from the frozen future runtime semantics.
- At that stage, the next implementation was collision-safe marker-plus-four reads/create/guarded SSA, Secret/ConfigMap `get`/`create`/`patch` RBAC, sanitized status then Kopf retry, and exhaustive tests. Services, Ingress, and workloads followed later.

## :material-book-open-page-variant-outline: Current Pure Contract/Input Migration

- 218 unit tests passed. Focused code evidence covered 186 tests and focused ingress/CRD evidence covered 32 tests.
- The pure factory derives deterministic RabbitMQ, Memcached, MariaDB, and Keystone Service names; fixes API `0.0.0.0:7667`, `/etc/coriolis`, and `/var/log/coriolis/vmware-root`; and updates Kubernetes-derived templates to RabbitMQ plaintext `5672`, Keystone HTTP `5000`, and HTTP WSGI without internal CA/TLS directives. The slice changes the CRD ingress schema/sample and adds pure validation for host/class/TLS inputs, including the derived `<host>-tls` Secret name.
- Ruff lint/format, strict mypy, Helm lint/template, and `git diff --check` passed. Byte comparison preserved all 23 immutable copied upstream templates; the offline wheel contained all 27 expected template resources, including the two Kubernetes-derived variants.

This migration is **committed locally at `e2ddb30` on `dev`, but not pushed or deployed**. It adds no `main.py`, runtime Kubernetes I/O, SSA/RBAC, actual Service/Ingress/workload resources, or release/chart/image/deployment behavior; deployed `0.5.3` remains marker-only.

Live-cluster controller lifecycle validation passed for release `0.5.2` in the approved `coriolis` namespace and was not repeated in full for later releases. The separate Memcached POC used healthy `0.5.8`; the later Keystone POC cleanup observed healthy `0.5.14` with no appliance CR.

The approved dev cluster is `infra-dev-buc-hq` (`virt-infra-dev-buc-hq`); CIXpress remains approved for read-only pipeline troubleshooting and monitoring only. The dedicated operator namespace is `coriolis`. See [Development Environment](dev-environment.md).

The completed `0.5.2` lifecycle validation covered:

- Apply the sample and check status, ownership, and marker configuration data.
- Replace the controller pod and verify reconciliation resumes without changing ownership, marker uniqueness, or condition transition times.
- Update `spec.version` and verify generation, observed generation, status, and marker data.
- Delete the resource and verify normal garbage collection.

The local controller coverage includes:

- CRD structure and namespaced scope.
- Namespace-scoped watch configuration.
- Idempotent marker ConfigMap reconciliation.
- Successful reconciliation status.
- `Ready=False` until an appliance runtime exists.
- Kubernetes API failures propagate without a custom failure condition.
- Absence of finalizers and destructive behavior.

CIXpress CI behavior is documented but not configured in this repository. Pending integration validation includes receiving the exact pipeline configuration, Template, and Job manifests; validating version alignment and the dev tag check; and defining trigger and monitoring credentials without storing secrets. Argo CRD pre-upgrade automation must be validated as promotion/deployment work, not assumed to be handled by the standard build pipeline.

Do not treat registry publication/authentication validation, promotion, or licensing decisions as settled test assumptions.
