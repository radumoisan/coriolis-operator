# Progress Log

This log is append-only. Add a dated entry for meaningful project progress.

## :material-book-open-page-variant-outline: 2026-08-24: Coriolis Scheduler 0.5.26 Released And POC Accepted

Implementation source `d4805eabc42a2106ad66300e31791e05dc294702` passed 540 unit tests plus the complete local gate and was published by CIXpress Default pipeline `02mop6` (17:07:04Z-17:08:25Z; top-level and all expected steps `SUCCEEDED`) as CI commit `de1da0f8a1272d9500f81d872951c3c20e414b8f`, tag/chart/app/operator `0.5.26`. The shared Argo Application automatically resolved `0.5.26` and remained `Synced/Healthy` with operator `1/1` and zero restarts.

The isolated POC ran in namespace/Application `coriolis-scheduler-validation-20260824`. The initial disposable Application project `coriolis` was rejected by destination policy before workloads or a CR existed; switching only the disposable Application to established project `default` produced `Synced/Healthy`. An ownerless zero-replica scheduler Deployment and its Kubernetes-owned zero-replica ReplicaSet stayed exact at 0/65/130 seconds while CR UID `91219b31-...`, generation/observed `1`, held stable `ResourceCollision`; there were no operator-created runtime resources, retained writes, or marker. Deleting only the collision at `17:37:33Z` auto-recovered without CR or operator changes; bootstrap succeeded at `17:39:53Z`, and scheduler became available at `17:40:03Z`.

The live one-replica `Recreate` scheduler Deployment matched the exact `2603.4` digest and frozen security/configuration contract with no Service, ports, probes, locks, PVC, init container, or resources. Fixed-output scheduler diagnostics and expected empty-worker `NoWorkerServiceError` passed repeatedly. Normal scheduler Pod deletion replaced UID `80431bc1-...` with `a897a2ae-...` in 28 seconds at the same imageID and zero restarts. Normal RabbitMQ Pod deletion recovered in 38 seconds while scheduler and conductor identities remained stable; scheduler RPC reconverged and remained stable.

Normal CR delete/recreate changed UID `91219b31-...` to `cd348d77-...`, preserved five retained ownerless state Secrets and both Bound PVCs exactly with no writes, recreated ephemeral owners under the new UID, converged in 75 seconds, and passed scheduler RPC again. Final conditions remained `Accepted=True`, `Reconciled=True`, `Degraded=False`, `Ready=False/RuntimeNotImplemented`, `Progressing=False/RuntimeNotImplemented`, and `Upgradeable=False`. Normal cleanup removed CR-owned resources in 38 seconds, Application in 12 seconds, namespace in 69 seconds, and both Delete-policy PVs without force, finalizer changes, or individual retained-resource/PV deletion. Final POC scope is absent; shared `argocd/coriolis` is `0.5.26` `Synced/Healthy`, operator `1/1` zero restarts, no CR, and the node is Ready/schedulable. Transfer-cron remains qualification-only and unimplemented; nonempty scheduling, due-transfer execution, Keystone trust, write workflows, HA, and production storage remain unclaimed.

## :material-book-open-page-variant-outline: 2026-08-24: Pre-Release Local Scheduler Controller Slice (History)

Local source now implements a scheduler-only owner-referenced one-replica `Recreate` Deployment at exact qualified image `cr.virtomat.io/virtomat/coriolis/coriolis-scheduler:2603.4@sha256:45bea9e0bab4cac0fdddee6d3eac52006d12cf7de1e798e2949dd9ebc2a73c41`, using direct `/usr/local/bin/coriolis-scheduler --config-file=/etc/coriolis/coriolis.conf`. The Pod runs as UID/GID `42434` with hardened read-only root, read-only projected `/etc/coriolis`, and writable `/tmp` and `/var/log/coriolis`; it has no locks mount, Service, ports, probes, PVC, or resources.

Reconciliation completes reads/preflight before mutation, fails collisions closed, requires resourceVersion and guarded SSA for managed repair, gates scheduler apply on successful bootstrap, and writes conductor then scheduler then API then the state marker last. Apply failures are sanitized and do not write the marker. `Ready=False/RuntimeNotImplemented` remains mandatory; no CRD, Helm values, RBAC, Service, readiness, or version changed. Local validation passed 533 unit tests, Ruff check, Ruff format check (57 files), strict mypy (13 source files), Helm lint/template, and a local operator Docker build.

Post-review validator hardening rejects declared application-image ports, volumes, or healthchecks, unexpected runtime mounts/tmpfs targets, and mislabelled pull failures. The strengthened exact-image rerun passed 85 stages with 0 failures and `SUMMARY runtime passed 305.963`; graceful shutdown, restart/reprobe, RabbitMQ recovery, and complete auth/Docker cleanup passed. The current full suite passes 540 tests.

This is local, uncommitted, unpublished implementation evidence, not a released artifact or Kubernetes POC. It makes no claim about live Deployment behavior, Pod recovery, garbage collection, or released acceptance. The next checkpoint is review and explicit authorization to commit/publish, followed by an isolated released-artifact scheduler POC. Transfer-cron remains qualification-only and unimplemented, and follows only after that milestone unless priorities change.

## :material-book-open-page-variant-outline: 2026-08-24: Scheduler And Transfer-Cron OCI Qualification Checkpoint (History)

Local scheduler and transfer-cron runtime qualification is accepted; this remains qualification-only evidence, not a published, released, or POC slice. The scheduler now also has the separate local controller implementation recorded above, while transfer-cron has no Kubernetes manifest. The tracked validator now qualifies the exact `2603.4` scheduler image `sha256:45bea9e...a73c41` and transfer-cron image `sha256:3a44d3b...2b3ea9c` through their exact direct commands under `/usr/local/bin/<binary> --config-file=/etc/coriolis/coriolis.conf`; source metadata is root/empty user, linux/amd64, and no ports, volumes, or healthcheck. Both run as numeric UID/GID `42434` with read-only root, dropped `ALL`, no-new-privileges, read-only generated `/etc/coriolis`, writable only `/tmp` and `/var/log/coriolis`, on a private internal network with no host ports, no Service/listener, and no locks mount.

Source dependency graph: the scheduler is one messaging worker requiring RabbitMQ that directly reads MariaDB worker-service state, with the expected empty database producing `NoWorkerServiceError` (proving that path); it constructs a conductor client, but the inspected qualified scheduler path does not call conductor. Transfer-cron is one messaging worker whose post-fork startup loads nonexpired schedules through conductor RPC and owns an in-memory cron, making RabbitMQ and conductor startup dependencies with MariaDB indirect via conductor. Keystone-trust and due-transfer execution paths require nonempty schedules and remain unqualified. Neither workload needs the API as a listener/backend, and neither gets a Service.

The fixed-output probe proved scheduler diagnostics, the scheduler empty-worker DB path, and transfer-cron diagnostics after schedule load; stable over 20s with leaf-first stop/restart; exact accepted timings transfer `0.901s`, scheduler `1.904s`, conductor `29.181s` within the evidence bound 30; restart/reprobe and RabbitMQ restart/recovery passed with the same container identities. The first two runs exposed an inherited 15s conductor shutdown bound (65 and 75 prior PASS stages); the accepted correction preserved the 15s leaf bounds and exit-0, gave the eight-worker conductor a bounded 30s, and did not weaken assertions.

Accepted final: 87 PASS, 0 FAIL, `SUMMARY runtime passed 316.069`; fixed-category/output-suppressed; ephemeral registry auth not persisted or reported; complete cleanup with no Docker/scratch/auth leftovers; cluster read-only with no Kubernetes resources. This OCI qualification remains distinct from the local scheduler implementation and establishes no released/live manifest; overall `Ready=False/RuntimeNotImplemented` remains. Transfer-cron follows only after scheduler publication and isolated released-artifact POC acceptance unless priorities change. Remaining managers/worker/web and write workflows are still absent. Local focused tests are currently 11; a full test suite is not claimed.

The 11-test statement is historical to this qualification-only checkpoint. The later scheduler controller gate above passed the current 540-test full suite.

## :material-book-open-page-variant-outline: 2026-08-24: Coriolis Conductor 0.5.24 Released And POC Accepted

The conductor slice is released and POC-accepted as `0.5.24`. Implementation source `d7abd778937203c97eb7501c2a2dc1a0c05cd5c4` (`Implement Coriolis conductor reconciliation`) passed the full local gate before publication: 516 unit tests, Ruff lint/format, strict mypy, Helm lint/template, operator container build, the tracked exact-image validator (three passes, final `272.660s`), and `git diff --check`. CIXpress Default pipeline `nd4xu7` (2026-08-23 20:54:39Z-20:55:52Z; top-level and `git-clone`/`kaniko-build`/`helm-update`/`cleanup` all `SUCCEEDED`) published CI release commit `56c0b29dbfc2883633b9cd20578ecae7f124b5a7`, chart/app/operator `0.5.24`. One initial `INPROGRESS` detail carried stale completion metadata and then resolved to consistent terminal success.

The accepted isolated POC ran in context `virt-infra-dev-buc-hq` in namespace/Application `coriolis-conductor-validation-20260823` at exact chart/operator `0.5.24`: Application `Synced/Healthy`, operator `1/1` zero restarts. An unmanaged conductor Deployment (UID `1759bccc-...`, resourceVersion `13359017`, replicas `0`, ownerless) stayed spec-exact at 0/65/130s; the initial CR (UID `395d9db8-...`, resourceVersion `13359131`, generation/observed `1`) had acceptedVersion absent, stable `ResourceCollision` conditions/RV across 130s, zero runtime/retained writes, and an unchanged operator Pod. Conflict deletion at `21:11:07Z` auto-recovered without a CR edit or operator restart: first children ~67s later, bootstrap Job completed in 74s at ~`21:13:35Z`, convergence ~148s after deletion. Final conditions `Accepted=True`/`Reconciled=True`/`Degraded=False`, `Ready=False/RuntimeNotImplemented`, `Progressing=False/RuntimeNotImplemented`, `Upgradeable=False`.

The happy conductor path produced the owner-referenced one-replica `Recreate` Deployment at exact conductor digest `sha256:27495f44...c09c7` with qualified direct command/configuration, restricted `42434` security, projected configuration, three writable paths, and no Service/ports/probes/init/resources/PVC; dependencies/API healthy with the API EndpointSlice on TCP `7667`. An authenticated output-free verifier proved Keystone-authenticated `GET /v1/{project_id}/endpoints` HTTP `200`/list (API→RabbitMQ→conductor→MariaDB) before and after a normal conductor Pod deletion (original Pod UID `7150baef...`, replacement UID `ab6c58d4...` in ~6s, exact imageID, zero restarts, stable 20s); unauthenticated API returned `401`. Normal CR delete/recreate (old UID `395d...` to new UID `4f2fbbb0-...`) preserved all five retained ownerless state Secrets and two retained Bound PVCs exactly with no writes; owner resources recreated under the new UID, convergence ~71s, bootstrap exit `0`/restarts `0`, and a third authenticated verifier plus `401` passed. Normal cleanup removed the CR, Application absent ~3s, namespace absent ~77s, and exact Delete-policy PVs immediately, without force/finalizer changes or individual retained/PVC/PV deletion; final namespace/Application/CR/PVs/prefix resources absent. Shared `argocd/coriolis` remains `0.5.24` `Synced/Healthy`, operator `1/1` zero restarts, no CR, node Ready/schedulable.

Accepted scope and limits: authenticated read-only conductor RPC is accepted; write workflows/diagnostics and scheduler/workers/transfer-cron/minion/deployer remain absent, and overall `Ready=False/RuntimeNotImplemented` remains required. The remaining application workloads then Ingress follow. Production CSI/cross-node, backup/restore, HA, RPO/RTO, and rotation remain deferred. This supersedes the local qualification record below.

## :material-book-open-page-variant-outline: 2026-08-23: Local Coriolis Conductor Qualification And Reconciliation (Superseded)

The conductor slice is implemented and locally qualified but is **not published or released**. Exact pinned image `coriolis-conductor:2603.4@sha256:27495f44fbb8b320098d0aa04cd9dcb2a4b432e57aa17417606efc5403ac09c7` passed the extended tracked validator `scripts/validate-coriolis-bootstrap-runtime.py` three times; the final run completed in `272.660s` (`SUMMARY runtime passed`) and cleanup left no resources. Restricted direct `/usr/local/bin/coriolis-conductor --config-file=/etc/coriolis/coriolis.conf` ran as UID/GID `42434` with read-only root, dropped `ALL`, no-new-privileges, read-only generated configuration, and writable `/tmp`, `/var/log/coriolis`, and `/opt/coriolis/locks` on an internal network with no host port. A Keystone-authenticated `GET /v1/{project_id}/endpoints` returned HTTP `200`/list, proving the API→RabbitMQ→conductor→MariaDB path; it stayed stable after a 20s settling period, a clean conductor SIGTERM/exit `0`/restart, and a RabbitMQ restart without conductor recreation, with no secrets/tokens/raw values emitted.

Local operator reconciliation applies one owner-referenced `Recreate` conductor Deployment (no Service/ports/probes/init/PVC/resources/CRD/RBAC/watch/finalizer) only after a successful immutable common bootstrap and before the marker, with complete preflight and collision-safe guarded SSA. Final local validation passed: 516 unit tests, Ruff lint/format, strict mypy, Helm lint/template, operator container build, tracked runtime validator, and `git diff --check`. Overall status deliberately remains `Ready=False/RuntimeNotImplemented`; write workflows/diagnostics and scheduler/workers/transfer-cron/minion/deployer remain absent. At that pre-release stage the conductor slice was not yet released or cluster-accepted, and the next gate was conductor publication/released-artifact POC acceptance, followed by the remaining application workloads and Ingress. That gate is now closed by the accepted `0.5.24` entry above, which supersedes this local record and the conductor-next milestone framing recorded in the `0.5.22` entry below.

## :material-book-open-page-variant-outline: 2026-08-23: Release 0.5.22 Coriolis API Accepted

Implementation source `5a06759d6e0332345262f990b2fb56d9d82fbef7` (`Implement Coriolis API reconciliation`) passed the full local gate before publication: 503 unit tests, Ruff lint/format, strict mypy, Helm lint/template, `docker build -t coriolis-operator:local .`, and `git diff --check`. It was published by CIXpress Default pipeline `44muez` (2026-08-23 18:45:00Z-18:46:33Z; top-level and `git-clone`/`kaniko-build`/`helm-update`/`cleanup` all `SUCCEEDED`) as CI release commit `ef86249cecee812482a96da8f41b1985d7cb764b`, chart/app/operator image/tag `0.5.22`. One transient `INPROGRESS` response carried a stale completion timestamp and then resolved to consistent terminal success; all steps succeeded.

The isolated POC ran in `virt-infra-dev-buc-hq` in namespace/Application `coriolis-api-validation-20260823` at exact chart/image `0.5.22`: Application `Synced/Healthy`, operator `1/1` with zero restarts. An unmanaged API Service (UID `c70790a3-c791-4477-9e43-aaac5f1351be`) held over 65s with exact UID/resourceVersion/creation/spec unchanged and no owner refs; the initial CR (UID `b453a56b-e407-4942-b2fc-57aab99a0508`, generation `1`) had no acceptedVersion, `Reconciled=False/ResourceCollision`, no status churn, and zero CR-owned or new retained writes. Conflict deletion at `18:57:46Z` auto-recovered without a CR edit or operator restart in about 127s; the bootstrap Job completed in 77s; all dependencies `1/1`.

The happy API path produced the owner-referenced ClusterIP Service (ready EndpointSlice TCP `7667`) and one-replica `Recreate` Deployment at exact digest `sha256:fce6369f07ef777b5174d3a4f849d4eac914256a20a47ffa0cd1c98081be2705` running the direct binary under restricted UID/GID/fsGroup `42434` with read-only root, projected `/etc/coriolis`, and evidenced writable paths; API Pod Ready zero restarts; Service-DNS `/v1` exact unauthenticated `401`; marker last; final `Accepted=True`/`Reconciled=True`/`Degraded=False`/`Ready=False/RuntimeNotImplemented`.

A deployment collision/recreation exercised retention: the old CR was deleted normally and five retained state Secrets plus the MariaDB/RabbitMQ PVCs remained exact no-write. The new CR (UID `8aead0cb-3dd3-4002-95cb-b14de37a71b3`) hit an unmanaged zero-replica Deployment (UID prefix `8407babd`) that held about 70s unchanged at `Reconciled=False/ResourceCollision`; conflict deletion at `19:04:40Z` auto-recovered in about 73s with retained metadata/resourceVersions/PVC volumes unchanged and bootstrap/dependencies/API healthy under the new owner UID. A normal API Pod deletion produced a distinct-UID replacement in about 5s with the same exact imageID, zero restarts, ready endpoint, and `401`.

Normal cleanup removed CR-owned children in 5s and the exact Application, namespace, and both Delete-policy PVs without force/finalizer patches or individual retained/PV deletion; final exact Application/namespace/CR/PVs are absent. Shared `argocd/coriolis` resolves `0.5.22`, `Synced/Healthy`, operator `1/1`, no CR, node Ready/schedulable.

The API slice is accepted released-artifact evidence. Authenticated API/RPC remains unclaimed until a conductor exists, and overall `Ready=False/RuntimeNotImplemented` remains required. The immediate next implementation milestone is conductor qualification/contract/reconciliation, then the remaining application workloads and Ingress. Production CSI/cross-node, backup/restore, HA, RPO/RTO, and rotation remain deferred.

## :material-book-open-page-variant-outline: 2026-08-23: Release 0.5.20 Collision Recovery Accepted

Corrective source `5f52c7004b3b393d85c10b07e9301d3fa3587164` (`Continue collision recovery retries`) was published by Default pipeline `0km4kz` (15:10:28Z-15:11:36Z; top-level and `git-clone`/`kaniko-build`/`helm-update`/`cleanup` all `SUCCEEDED`) as release `0.5.20` at CI commit `61c7b3a0d0cea2e81b290023fa8ee5605e2ff261`; POC operator imageID was `sha256:8d537d978783496338ba4bda60d412690b9c3bd24f6187e31ee8d1de4735e190`. Full local validation passed: 490 unit tests, Ruff lint/format, mypy, Helm lint/template, container build, and `git diff --check`.

Fresh isolated `coriolis-collision-recovery-20260823` used exact chart/image `0.5.20`. An unmanaged immutable bootstrap ConfigMap collision remained unchanged for 135s (over two timer intervals): UID/resourceVersion/creation/immutable/hash, CR `90548df2-6321-409e-98fb-7130975200e5` generation `1`/resourceVersion `13263783` at `Reconciled=False/ResourceCollision`, zero-restart operator Pod `076dc2e0-583e-45c8-ab44-eb1f56ec7df7`, and zero appliance children. After conflict deletion at `15:25:20.744Z`, recovery began at `15:26:12.507Z` (51.763s) and completed at `15:27:40.726Z` (139.982s total), crossing `BootstrapRunning` without a user CR/spec change, generation change, operator restart, or status churn. The final CR retained UID/generation/observed generation `1` and reported `acceptedVersion=2603.4`, `Accepted=True`, `Reconciled=True`, `Degraded=False`, `Ready=False/RuntimeNotImplemented`; the Job succeeded at `15:27:34Z` (`1` succeeded, `0` failed, exit `0`, restarts `0`), dependencies were `1/1`, each of four Services had one ready EndpointSlice, the managed immutable replacement ConfigMap had a distinct UID and correct ownership, and the operator-state marker was present. Child RBAC remains without list/watch/delete; exact Application `0.5.20` was `Synced/Healthy`.

Normal cleanup removed CR-owned children in 1s, the exact Application in 18s, namespace in 50s, and both Delete-policy PVs without forced finalizers or individual retained-resource/PV deletion. An initial wrong short Application-name query allowed Argo to recreate the namespace; deleting the exact Application resolved cleanup normally. Final Application/namespace/CR/PVs are absent; the node is Ready/schedulable; shared `argocd/coriolis` target `0.*.*` resolves to `0.5.20`, `Synced/Healthy`, with operator image `0.5.20`, ready/available `1/1`.

## :material-book-open-page-variant-outline: 2026-08-23: Release 0.5.19 POC Unaccepted; Local Retry Correction

Release `0.5.19` was published from source `c89b57fcd5b2ee160c1a0986be9fedc9edc19ec8` by successful pipeline `mg04ql` as CI release `3a2af04270762069f3d50905ad6f4255d699c302`, but its POC is unaccepted. Persistent collision correctly caused no churn. Removing the conflict at `14:50:14Z` triggered timer recovery at `14:50:42Z` (28s); it created resources and raised `TemporaryError` with `BootstrapRunning`. Kopf retried after the status changed, but the timer rechecked only `ResourceCollision` and no-oped. The bootstrap Job still succeeded at `14:51:42Z` with exit `0` and restarts `0`, and dependencies were healthy, while the CR remained `Reconciled=False/BootstrapRunning` for more than 10 minutes.

The local, unpublished correction explicitly accepts the Kopf timer `retry` argument and invokes normal reconciliation for either the original exact collision or `retry > 0`; a noncollision initial timer call remains a no-op. It preserves the 60-second timer, RBAC, status suppression, and scope. Full validation passes at 490 unit tests, Ruff lint/format, mypy, Helm lint/template, container build, and `git diff --check`; release and a re-POC remain pending, and no success is claimed.

## :material-book-open-page-variant-outline: 2026-08-23: Local Bounded Collision Recovery

Local, unpublished implementation adds a Kopf timer with a 60-second initial delay and 60-second interval. It invokes normal reconciliation only when persisted status contains the exact `Reconciled=False/ResourceCollision` condition, allowing a removed collision to converge without metadata or child watches; malformed and noncollision status are no-ops. Status delivery patches only changed operator-owned computed fields and preserves unrelated status. Full local validation passes at 489 unit tests, Ruff lint/format, mypy, Helm lint/template, container build, and `git diff --check`. No RBAC expansion, CRD change, finalizer, destructive behavior, broad periodic reconciliation, or readiness change is included. Released-artifact POC remains pending.

## 2026-08-20: Documentation and Tracking Baseline

Created the project documentation and tracking baseline before implementation of the bootstrap/controller skeleton. Recorded the initial namespace-scoped, non-destructive controller contract and pending operational decisions.

## 2026-08-20: Controller Skeleton Implemented Locally

Implemented and locally validated the Python 3.12 Kopf controller skeleton: namespace-scoped marker ConfigMap reconciliation, bounded generated names, server-side apply, successful reconciliation status, configurable logging and liveness, and Helm security and resource defaults. Chart release conventions, CI version and image-tag synchronization, registry publication, and licensing remain open.

## 2026-08-20: Container Image Validated Locally

Built the Python 3.12 container image and verified that it runs as the non-root `operator` user. A later consistency sweep replaced reliance on a base-image group with a dedicated `coriolis` group.

## 2026-08-20: Safe Validation Boundary

Local validation is complete. Live-cluster smoke validation remains pending and must run only in an isolated disposable Kubernetes cluster, never a shared or production context.

## 2026-08-20: Documentation Consistency Sweep

Aligned the documented handler result, collision-safe naming, standard conditions, and CRD/RBAC scope with the local implementation. Documented the restricted runtime contract and pending disposable-cluster smoke test without treating garbage collection as proven behavior.

## 2026-08-20: Consistency Sweep Validated

Validated the dependency lock, Python formatting and linting, typing, nine unit tests, Helm lint and rendering, synchronized project/chart/image versions, and a fresh non-root container build. Live behavior and RBAC remain pending verification in a disposable Kubernetes cluster.

## 2026-08-20: CIXpress CI Model Documented

Documented the supplied CIXpress Kubernetes Job pipeline, release version policy, observability contract, and non-transactional publication behavior. This repository does not yet contain CIXpress configuration, Templates, Job manifests, triggers, or credentials; CRD pre-upgrade automation remains promotion/deployment work.

## 2026-08-20: Dev Environment Boundary Received

Recorded the approved read-only CIXpress monitoring boundary for `infra-dev-buc-hq` and its explicit Kubernetes context and namespace. Received the report that CI accepted and started a build for commit `b824f5c`; no pipeline ID or final step outcome is known, so no pipeline success is claimed. The dedicated operator namespace remains TBD and requires approval before deployment or live validation.

## 2026-08-20: CIXpress Pipeline Result And Polling Procedure

Recorded the observed result for commit `b824f5c`: pipeline `hlzfy3` using template `Default` failed after starting at `2026-08-20T09:23:41+00:00` and completing at `2026-08-20T09:23:56Z`. Detail steps were empty. Safe log metadata showed one 25-line `git-clone` stream and one 12-line `kaniko-build` stream, while `helm-update` and `cleanup` returned HTTP 404; no log content was exposed and root cause remains unknown. Added the bounded polling-only monitoring procedure and project-local OpenCode skill, including explicit context/namespace, GET-only API access, metadata-first log handling, and no SSE or `/stream`.

## 2026-08-20: Provisional Registry Baseline And Confirmed CI Failure

Confirmed that pipeline `hlzfy3` failed because Kaniko could not resolve `example.invalid` during push-permission checking. Set the provisional synchronized `0.0.0` image baseline to `cr.virtomat.io/virtomat/coriolis/operator`. Registry publication/authentication and promotion remain pending validation.

## :material-book-open-page-variant-outline: 2026-08-20: CIXpress Release 0.2.0 Published

The dummy trigger for commit `c9d9dd5ec3ecfe06f03e0cbfc1eda3ff4b0fd58d` completed as pipeline `jcr0vn` using template `Default`: it started at `2026-08-20T11:18:10+00:00`, completed at `2026-08-20T11:19:27+00:00`, and reported top-level plus `git-clone`, `kaniko-build`, `helm-update`, and `cleanup` states as `SUCCEEDED`. CI image push and OCI chart publication were validated. The generated release commit and tag `0.2.0` resolve to `49cb5dc7dbe247e432e604db19078ecf1c2b5437`; image pull/deployment and promotion remain pending.

## :material-book-open-page-variant-outline: 2026-08-20: Argo Deployment Validated

Configured the `regcred` image pull secret through the source-controlled Helm default and published release `0.3.0`. Its deployment confirmed registry authentication but exposed an invalid CRD schema and a chart command that could not find the virtual-environment console script. Removed the forbidden CRD schema keywords, corrected the command, validated the CRD with Kubernetes server-side dry-run, and published release `0.4.0`. Pipeline `gepx3l` reported all expected steps as `SUCCEEDED`, but one intermediate `INPROGRESS` response already contained a completion timestamp; publication was independently confirmed by release commit and tag `4eee8a9f24eb05640c61ece8fa057ecd49136e85`. Argo synchronized `0.4.0` successfully in `coriolis`; the application is `Healthy`, the CRD is established, and the operator Deployment is `1/1` available with a ready pod and zero restarts.

## :material-book-open-page-variant-outline: 2026-08-20: Releases 0.5.1 and 0.5.2 Published

The initial `0.5.0` live sample exposed Kubernetes Python client `32.0.1` rejecting `_content_type`. Source commit `d3aecb2ce71fc83730969db174b50727c37fe96c` was released by pipeline `jqd3ri` as `0.5.1` at `5ffc69534a558417405e99e1b75c3145319ac19a`. Release `0.5.1` exposed missing ConfigMap-create RBAC; source commit `cbdd28fc645e37c5a8146ff900f7fd84f6330210` added ConfigMap `create` and `patch` only. Pipeline `5ly5kg` verified `git-clone`, `kaniko-build`, `helm-update`, and `cleanup` as `SUCCEEDED` and generated `0.5.2` at `86552e46fd2fb13b05d66cc2b7e25f4968f00846`.

## :material-book-open-page-variant-outline: 2026-08-20: Release 0.5.2 Lifecycle Validated

Argo Application `argocd/coriolis` targeting `0.*.*` resolves to release `0.5.2` and is `Synced` and `Healthy`; its Deployment uses image `0.5.2`, is `1/1` available, and has one Running/Ready pod with zero restarts. In approved namespace `coriolis`, the `example` sample completed create, controller replacement/resume, `spec.version` update, and normal deletion checks. Generation and observed generation started at `1`; conditions were `Accepted=True`, `Reconciled=True`, and expected `Ready=False/RuntimeNotImplemented`. The single owned `example-operator-state` marker held `development/1`; replacement/resume preserved marker state, ownership, uniqueness, and condition transition times. The old pod exceeded its grace period but disappeared naturally before authorized force cleanup, so no force command ran. Updating to `development-updated` propagated generation, observed generation, status, and marker to `2` while preserving transition times. Normal CR deletion garbage-collected the marker; no sample CR or marker remains. Appliance runtime work remains open pending an explicit runtime/API contract.

## :material-book-open-page-variant-outline: 2026-08-20: Kubernetes-Native Core Runtime Agreed

Recorded the next runtime architecture: creating `CoriolisAppliance` deploys the selected complete Coriolis stack as Kubernetes workloads directly in `coriolis`, not an external VM; OpenStack and VMware remain migration endpoints. The first `core` profile targets `2608.0-rc2`, with initial acceptance limited to complete bootstrap and internally healthy/reachable UI and API. The first blocking gate is a complete application/support image inventory, immutable digests, runtime compatibility, `registry.cloudbase.it/appliance` access, and an approved pull Secret; work stops if the release is OVA-only or lacks a complete compatible image set. Recorded the ordered implementation packages, immutable `spec.version` with `Upgradeable=False` and reason `UpgradeBlocked`, readiness conditions, recovery-retention deletion policy, development-only host mounts and local-path storage, and deferred production/feature scope. Upstream Coriolis images and code remain immutable inputs.

## :material-book-open-page-variant-outline: 2026-08-20: Runtime Target Advanced To RC4

Advanced the active `core` inventory target from `2608.0-rc2` to `2608.0-rc4`. Recorded the canonical Confluence environment source, appliance Jenkins job `1_coriolis-appliance-setup`, and reference Build `868`. Registry work will validate metadata before pulls and use a dedicated pull Secret created securely from Jenkins credential ID `docker-appliance-creds`; secret values remain outside Git and reports. The safe unauthenticated Build `868` metadata request timed out from the workspace, and work is paused while the user restores access.

## :material-book-open-page-variant-outline: 2026-08-20: RC4 Inventory Gate Sanity Check

Confirmed Jenkins network reachability but anonymous Build `868` APIs return HTTP `403` with no documented approved authentication mechanism. Confirmed registry TLS/V2 reachability but anonymous `/v2/` returns HTTP `401` Bearer authentication required. The `virt-infra-dev-buc-hq` `coriolis` namespace has only the Running/Ready `0.5.3` operator and no CoriolisAppliance CRs or core runtime workloads. Recorded the blocked gate and candidate-only inventory in [RC4 Image Inventory Gate](rc4-image-inventory.md); no credentials were exposed.

## :material-book-open-page-variant-outline: 2026-08-20: RC4 Blocked; Exact 2603.4 Fallback Inventory

Authenticated findings supersede the candidate-only RC4 inventory. Jenkins job `1_coriolis-appliance-setup` Build `868` succeeded and exported an OVA, but all 15 `registry.cloudbase.it/appliance/coriolis-*` repositories carry no `2608*` tag; representative `coriolis-api:latest` predates RC4. RC4 is therefore blocked/OVA-only for Kubernetes and must not be used. Approved the exact official release `2603.4` (not `2603.41`/`2603.42`) as the fallback, sourced from the official Confluence release page; official OVA is 9,934,848,000 bytes with SHA-256 `e8c7fad6a07bc96aad281876bfc47728df94c40b5f562ec45a16baabce9b93c9`. Replaced the RC4 ledger with the authoritative [Image Inventory](image-inventory.md): Linux/amd64, root/empty user, no OCI healthchecks (probes are operator-owned), API `7667`, web `3000`, proxy `443`, privileged worker with `/dev` and `/lib/modules`; recorded exact `2603.4` application digests, `2023.1-ubuntu-jammy` support digests, and pending `step-ca:latest`/`influxdb:1.7` candidates (logger/InfluxDB deferred). Initial core workload is API, conductor, scheduler, transfer-cron, minion-manager, deployer-manager, privileged worker, compressor, web, web-proxy; `coriolis-common` is a base image. Licensing, Metal Hub, console editor, and logger/InfluxDB are deferred. Pull validation in `virt-infra-dev-buc-hq` namespace `coriolis` remains pending; no runtime implementation before it passes. No credentials were exposed.

## :material-book-open-page-variant-outline: 2026-08-20: All Approved Images Mirrored

All 26 approved images were mirrored serially on 2026-08-20 to `cr.virtomat.io/virtomat/coriolis` with preserved and verified manifest digests using the reusable `scripts/mirror-images.py`: 15 application images at tag `2603.4`, 10 support images at `2023.1-ubuntu-jammy`, and Step CA at `2603.4` pinned from source digest `sha256:e9e8fa3262bf37b130962ffddbf6a64ac188f0bbb80959cf3ddc04c6bf294c3d` of `smallstep/step-ca`. The utility holds source auth only in anonymous memfd storage, skips matching destinations, refuses conflicting tags, copies by digest with `--preserve-digests`, and verifies each destination digest; Harbor required a bounded retry for transient immediate post-push unauthorized responses. Independent destination verification of Step CA returned the exact expected digest. Logger/InfluxDB remain deferred; Step CA moved from mutable candidate wording to a pinned/mirrored exact source digest. Kubernetes pull validation in `virt-infra-dev-buc-hq` namespace `coriolis` is still pending: a destination-registry pull Secret must be configured and the initial-runtime image pulls validated before runtime implementation begins. No credentials were exposed.

## :material-book-open-page-variant-outline: 2026-08-20: Image Pull Gate Passed

The image inventory and pull gate are complete. In `virt-infra-dev-buc-hq` namespace `coriolis`, the existing retained Secret `coriolis-appliance-registry` (name, labels, and type preserved; type `kubernetes.io/dockerconfigjson`) was used as the destination pull Secret. `scripts/validate-image-pulls.py` validated serially, using one short-lived Pod at a time with `imagePullPolicy: Always`, explicit context/namespace, and the destination Secret; each successful Pod was removed and failures are left for diagnosis. All exact 21 initial-runtime image references passed: 10 application images at tag `2603.4`, 10 support images at `2023.1-ubuntu-jammy`, and Step CA at `2603.4`. Independent main-agent validation repeated Step CA successfully and returned the expected image reference digest `sha256:e9e8fa3262bf37b130962ffddbf6a64ac188f0bbb80959cf3ddc04c6bf294c3d`. No pull-validation Pods remain. All 26 approved images remain mirrored with preserved/verified registry manifest digests; logger/InfluxDB remain deferred and RC4 remains blocked/OVA-only with exact fallback `2603.4`. Runtime implementation is therefore unblocked and is the next phase; no Coriolis core runtime workloads have been implemented or deployed yet.

## :material-book-open-page-variant-outline: 2026-08-20: Local API-Only Core Runtime Slice

Implemented and locally validated the `core` runtime API slice. The `v1alpha1` CRD now has optional/defaulted `spec.profile: core` (enum only `core`), required non-empty `spec.version`, and optional non-empty `status.acceptedVersion`. No CEL/admission immutability was added: the controller enforces the immutable accepted version using the persisted `status.acceptedVersion`, so a rejected change remains observable in status. Initial acceptance supports exact `2603.4`; an omitted profile defaults to `core`, and unsupported initial profiles/versions apply no Kubernetes resources and report rejection conditions. A requested version different from `status.acceptedVersion` applies no resources, preserves the accepted state, advances `observedGeneration`, and reports `Accepted=False/VersionChangeRejected`, `Reconciled=False`, and `Upgradeable=False/UpgradeBlocked`. A valid API-only reconcile records only the owned controller-state ConfigMap (`acceptedVersion`, `profile`, `generation`) and reports all six conditions (Accepted, Progressing, Reconciled, Ready, Degraded, Upgradeable); `Ready=False/RuntimeNotImplemented` remains truthful, and profile changes route through the same reconcile path. The sample now uses `profile: core`, `version: "2603.4"`. 25 tests pass; Ruff and mypy pass; Helm lint/template pass. No cluster or external service was changed by this API slice, and the image mirror/pull gate remains passed. No dependencies, bootstrap Jobs, services, storage, secrets, or Coriolis runtime workloads have been implemented or deployed; the next milestone is naming/ownership/retention/generated configuration and foundational dependency/resource construction.

## :material-book-open-page-variant-outline: 2026-08-20: Current Deployment Boundary Confirmed

Confirmed the deployed-vs-local boundary. The currently deployed operator is release `0.5.3` at commit `97f6b96844822819c8072a8a43e9987d70b8dc5a`, published by CIXpress and deployed by Argo in `virt-infra-dev-buc-hq` namespace `coriolis`; it is `Synced`/`Healthy`, Deployment `1/1`, with a ready zero-restart Pod. Full create/resume/update/delete controller lifecycle validation was performed on release `0.5.2`, not repeated for `0.5.3`. The deployed `0.5.3` predates the local API-only `core` slice and remains the marker-only controller behavior; the API slice is uncommitted/local, not published or deployed, and is therefore not included in `0.5.3`.

## :material-book-open-page-variant-outline: 2026-08-21: Foundational Resource Contract Slice (Milestone 4, First Deliverable)

Documented the first Milestone 4 deliverable in [docs/foundational-resource-contract.md](foundational-resource-contract.md), a documentation-only slice that freezes foundational Kubernetes resource contracts from authoritative local evidence. Extracted runtime behavior from `coriolis-docker/coriolis_ansible` (playbook `appliance.yml`, `bootstrap.yml`, `group_vars/all.yml`, `library/kolla_deployment_facts.py`, the `common/mariadb`, `coriolis/common`, `coriolis/api`, and `bootstrap/step-ca` roles/templates/vars), `coriolis-docker/passwords.yml.sample` (the empty credential key schema), and `coriolis-oss` (systemd services and `etc/coriolis/coriolis.conf` reference). Froze deterministic naming (a conservative single 63-character DNS-label shape `<appliance>-<component>` with no dots and the existing 12-character SHA-256 overflow principle; the current ConfigMap helper's subdomain-with-dots shape is not generalized), proposed `app.kubernetes.io/*` labels plus a deterministic label-safe `coriolis.cloudbase.it/appliance` identity token (full CR name in an annotation), collision handling (never adopt/overwrite a mismatched object), ownership/deletion/retention (owner-reference ephemeral resources; retain PVCs, CA state, and state credentials; never mutate external Secrets such as `coriolis-appliance-registry`; no destructive finalizer), three Secret/configuration classes, and an explicit sensitive-rendered-configuration rule (`coriolis.conf` embeds RabbitMQ/MariaDB/Keystone/temp-keypair credentials and must never be a ConfigMap). Recorded a dependency evidence inventory (bootstrap.yml imports common bootstrap then Step CA; appliance.yml starts with MariaDB; Kolla facts prove dependency endpoints only) and a proposed implementation ordering requiring approval and a readiness design, not a proven readiness sequence or Kubernetes Job design. Automatic reattachment/adoption of retained resources by a recreated CR is an unresolved safety gate. Recorded unresolved items (Class 1 retained Secret names/keys mapping the `passwords.yml.sample` schema, the Secret/ConfigMap split and mount design, storage sizes, probes, commands, and readiness checks) rather than guessing. No credential values were included and no code, build, cluster, or Git metadata was changed. Also corrected stale wording: the API slice is now committed locally at `ab9df83` (branch `dev`, ahead of `origin/dev` by one commit) but remains unpushed and undeployed.

## :material-book-open-page-variant-outline: 2026-08-21: Metadata-Only Helper Slice Implemented Locally

Implemented and validated the local metadata-only helper slice in `src/coriolis_operator/reconcile.py`: `appliance_resource_name` (single lowercase DNS label <=63; dotted/overflow names use a visible dot-to-hyphen prefix plus the first 12 SHA-256 characters of the full unmodified input; invalid appliance/component values rejected), `appliance_identity` (label-safe appliance identity token), and `build_resource_metadata` (standard `app.kubernetes.io/*` and `coriolis.cloudbase.it/*` labels, the full appliance-name annotation, and exactly one lifecycle mode — owner reference or retention annotation). `build_state_config_map` now routes through this standard metadata with component `operator-state` while deliberately retaining the shipped `state_config_map_name`, preserving the `0.5.2`/`0.5.3` names including dotted/long DNS-subdomain behavior. Marker data, the SSA call, `force=True`, API behavior, and status are unchanged. Validation passed: 44 unit tests; `uv run ruff check .`; `uv run ruff format --check .`; `uv run mypy src`; `git diff --check` was clean before doc alignment. The helper slice is uncommitted and unpushed; the deployed marker `0.5.3` is unchanged and carries no standard labels. Collision pre-read/enforcement, legacy marker migration, collision status, retained-resource adoption, and all runtime resource construction remain deferred to the separate collision/migration API-layer slice; a MariaDB vertical slice remains blocked by unresolved Secret/configuration/storage/readiness gates.

## :material-book-open-page-variant-outline: 2026-08-21: Metadata-Only Helper Commit Recorded

The metadata-only helper slice implementation from the previous entry is now **committed locally at `fbab6e5` on branch `dev`**, but it is **not pushed and not deployed**. The deployed release `0.5.3` remains unchanged/marker-only and carries no standard labels. The API slice reference at `ab9df83` remains accurate. The next step and deployment boundary are unchanged: the separate collision/migration API-layer slice comes next, and no core runtime workloads are implemented or deployed.

## :material-book-open-page-variant-outline: 2026-08-21: Collision/Migration Marker API-Layer Slice Implemented Locally

Implemented the local collision/migration marker API-layer slice in `src/coriolis_operator/reconcile.py` and `src/coriolis_operator/main.py`. Reconciliation now pre-reads the deterministic marker ConfigMap before server-side apply: a 404 creates normally; a fully matching managed marker (standard `app.kubernetes.io/*`/`coriolis.cloudbase.it/*` labels, appliance-name annotation, matching controller owner reference) reconciles; and a compatible legacy `0.5.2`/`0.5.3`-shaped marker (no management signature, matching controller owner reference, compatible `acceptedVersion`/`profile`) is normalized in place under the unchanged shipped marker name with its stale generation updated. Partial/conflicting standard metadata, owner mismatch, incompatible legacy data, or owner-plus-retention metadata is a `ResourceCollision`: the object is not patched, adopted, deleted, or renamed, and status keeps all six conditions with `Accepted=True`, `Progressing`/`Reconciled`/`Ready=False`, `Degraded=True`, and `Upgradeable=False/UpgradeNotSupported`; a prior `acceptedVersion` is preserved but not newly established on an initial collision. Non-404 Kubernetes read failures still propagate; SSA content type, field manager, `force=True`, marker data, naming, and patch-error behavior are unchanged; ConfigMap RBAC gains only `get`. Validation passed: 70 unit tests; `uv run ruff check .`; `uv run ruff format --check .`; `uv run mypy src`; `helm lint helm/`; `helm template coriolis-operator helm/ --include-crds`; `git diff --check`. The slice is **uncommitted, unpushed, and undeployed**; the deployed release `0.5.3` remains marker-only and unchanged and lacks these local pre-read/collision semantics. Retained-resource adoption remains an unresolved authorization safety gate and is NOT implemented; all runtime resources remain unimplemented/undeployed, and a MariaDB vertical slice remains blocked by unresolved retained-resource authorization and Secret/configuration/storage/readiness gates. The prior local commit references remain accurate: API slice `ab9df83`, metadata helper `fbab6e5`, status docs `6bf5cb5`.

## :material-book-open-page-variant-outline: 2026-08-21: Collision/Migration Marker API-Layer Slice Committed Locally

The collision/migration marker API-layer slice from the previous entry is now **committed locally at `d8df00f` on branch `dev`**, but it is **not pushed and not deployed**. The deployed release `0.5.3` remains unchanged/marker-only and lacks these local pre-read/collision semantics. Validation remains 70 unit tests plus the existing gates (`uv run ruff check .`; `uv run ruff format --check .`; `uv run mypy src`; `helm lint helm/`; `helm template coriolis-operator helm/ --include-crds`; `git diff --check`). Retained-resource adoption remains an unresolved authorization safety gate and is NOT implemented; all runtime resources remain unimplemented/undeployed, and the retained-resource/runtime boundaries are unchanged. The prior local commit references remain accurate: API slice `ab9df83`, metadata helper `fbab6e5`, status docs `6bf5cb5`.

## :material-book-open-page-variant-outline: 2026-08-21: Retained-Resource Authorization/Classification Slice Implemented Locally

Implemented the pure retained-resource authorization/classification slice in `src/coriolis_operator/reconcile.py`: `classify_retained_resource` returns `RetainedClassification.ABSENT/REUSE/COLLISION`, with `EXTERNAL_READ_ONLY_RESOURCES`. An absent resource is eligible for creation (`ABSENT`); a retained resource (PVC, state Secret, CA state) is reused automatically (`REUSE`) only when its deterministic name/namespace and every operator-controlled identity field match exactly — full appliance-name annotation, standard managed/identity labels, component label, exact retention annotation/class — with **no owner references** (owner plus retention is a collision even if an owner UID matches); unrelated extra labels/annotations are permitted; missing/partial/conflicting identity metadata is a collision and is never normalized; a matching ownerless retained object is `REUSE` with no mutation or adoption patching. The creating appliance CR UID is deliberately **not** part of the identity: retained resources survive CR deletion/recreation, so automatic exact-match reattachment works even when the CR UID changes, and any stale `coriolis.cloudbase.it/appliance-uid` annotation is ignored as unrelated. This is a namespace trust boundary: in-namespace users can forge the operator's identity metadata. External/pre-existing resources, especially the registry pull Secret `coriolis-appliance-registry`, fail closed as `COLLISION` (even absent or with forged matching metadata) and remain read-only and outside this classifier/reconciliation policy. The classifier is pure and works with mapping-shaped fakes and real `V1Secret`/`V1PersistentVolumeClaim` model objects; no runtime resource is constructed, reconciled, patched, read, or adopted, and no adoption mutations exist. Validation passed: 94 unit tests; `uv run ruff check .`; `uv run ruff format --check .`; `uv run mypy src`; `helm lint helm/`; `helm template coriolis-operator helm/ --include-crds`; `git diff --check`. The slice is **uncommitted, unpushed, and undeployed**; the deployed release `0.5.3` remains marker-only and unchanged. A MariaDB vertical slice remains blocked by unresolved Secret/configuration/storage/readiness gates. The prior local commit references remain accurate: API slice `ab9df83`, metadata helper `fbab6e5`, status docs `6bf5cb5`, collision/migration slice `d8df00f`.

## :material-book-open-page-variant-outline: 2026-08-21: Retained-Resource Authorization/Classification Slice Committed Locally

The retained-resource authorization/classification slice from the previous entry is now **committed locally at `1b73045` on branch `dev`**, but it is **not pushed and not deployed**. The deployed release `0.5.3` remains unchanged/marker-only. Validation remains 94 unit tests plus the existing gates (`uv run ruff check .`; `uv run ruff format --check .`; `uv run mypy src`; `helm lint helm/`; `helm template coriolis-operator helm/ --include-crds`; `git diff --check`). No runtime resource is constructed, reconciled, patched, read, or adopted, and no adoption mutations exist; the retained-resource/runtime boundaries are unchanged. The prior local commit references remain accurate: API slice `ab9df83`, metadata helper `fbab6e5`, status docs `6bf5cb5`, collision/migration slice `d8df00f`.

## :material-book-open-page-variant-outline: 2026-08-21: Secret/Configuration Contract Slice Documented Locally

Added a documentation-only Secret/configuration contract slice to [docs/foundational-resource-contract.md](foundational-resource-contract.md), freezing the foundational Kubernetes Secret/ConfigMap names, key layouts, and the primary `coriolis.conf` split using only known source key names; all generated names use the existing `appliance_resource_name(<CR name>, <component>)` truncation/hash rules. Source-audited from `coriolis-docker/passwords.yml.sample`, `coriolis-docker/coriolis_ansible/roles/coriolis/common/tasks/setup.yml`, `coriolis-docker/coriolis_ansible/roles/coriolis/common/templates/coriolis.conf.j2`, and `coriolis-docker/coriolis_ansible/roles/bootstrap/step-ca/tasks/setup_step_ca_container.yml`. Frozen retained, ownerless Secrets: `<appliance>-coriolis-credentials` (`coriolis_database_password`, `coriolis_keystone_password`, `temp_keypair_password`; licensing server/UI, Metal Hub, InfluxDB keys absent/deferred), `<appliance>-infrastructure-credentials` (`database_password`, `rabbitmq_password`, `keystone_admin_password` preserving local Kolla source names; no invented Barbican/Memcached password), `<appliance>-step-ca-credentials` (`init_password`; `/etc/step` CA state on retained storage, TLS/private-key layout deferred). Frozen owner-referenced, rebuildable configuration: ConfigMap `<appliance>-coriolis-config` with exactly `coriolis-api.wsgi`, `wsgi-coriolis.conf`, `vixdisklib.conf`, `api-paste.ini`, `policy.yml`, `coriolis.release` (forbidden: `coriolis.conf`, provider fragments, credentials, tokens, private keys, registry auth); Secret `<appliance>-coriolis-config-secret` with exactly `coriolis.conf` (complete because it embeds credentials; GC'd with the CR and regenerated from retained credentials, not a retained credential store). Workloads mount both together as a read-only projected volume at `/etc/coriolis` with explicit `items` paths, no credential environment variables, and no `subPath`; rotation rollout mechanics remain deferred, and sensitive values must never appear in Pod template annotations/labels/status/events/logs. Retained credential values are generated once and reused only under the exact-match retained classifier policy committed at `1b73045`; generation algorithms/lengths not evidenced are not documented. The external `coriolis-appliance-registry` Secret remains read-only and is never copied into operator-owned Secrets. The prior unresolved name/key and `coriolis.conf` split gates are closed; the contract's Unresolved Gates now list the remaining generators/builders/reconcile reads/SSA/RBAC, TLS/CA layout, optional component credentials, storage sizes/layout, probes/readiness/bootstrap, and rotation rollout mechanics. Validation passed: `uv run pytest`; `uv run ruff check .`; `uv run ruff format --check .`; `uv run mypy src`; `helm lint helm/`; `helm template coriolis-operator helm/ --include-crds`; `git diff --check`, plus a review of the modified docs for contradictory unresolved-name/key or `coriolis.conf`-split claims. This slice is **documented locally and uncommitted, unpushed, and undeployed**: no Secret/ConfigMap builders, values, RBAC, CRD, runtime resources, or reconcile behavior changed, and no credential values were included.

## :material-book-open-page-variant-outline: 2026-08-21: Secret/Configuration Contract Slice Committed Locally

The documentation-only Secret/configuration contract slice from the previous entry is now **committed locally at `8ce26ba` on branch `dev`**, but it is **not pushed and not deployed**. It remains documentation-only with no behavior change: no code, builders, values, RBAC, CRD, runtime resource, or reconcile behavior changed, and no credential values were included; the deployed release `0.5.3` remains marker-only and unchanged. Validation remains 94 existing unit tests plus the existing gates (`uv run ruff check .`; `uv run ruff format --check .`; `uv run mypy src`; `helm lint helm/`; `helm template coriolis-operator helm/ --include-crds`; `git diff --check`). The contract's Unresolved Gates remain unchanged (generators/builders/reconcile reads/SSA/RBAC, TLS/CA layout, optional component credentials, storage, probes/readiness/bootstrap, rotation rollout). The prior local commit references remain accurate: API slice `ab9df83`, metadata helper `fbab6e5`, status docs `6bf5cb5`, collision/migration slice `d8df00f`, retained-resource slice `1b73045`.

## :material-book-open-page-variant-outline: 2026-08-21: Pure Secret/ConfigMap Builder Slice Committed Locally

The five pure builders (`build_coriolis_credentials_secret`, `build_infrastructure_credentials_secret`, `build_step_ca_credentials_secret`, `build_coriolis_config_map`, and `build_coriolis_config_secret`) are **committed locally at `050f16e` on `dev`, but not pushed or deployed**. Validation passed: 116 total tests (22 new from the previous 94; 21 matched the final focused selector), Ruff lint/format, mypy, Helm lint/template, and `git diff --check`. This is manifest construction only: no credential generation, `main.py` reconciliation, Kubernetes reads/SSA, RBAC, CRD, runtime resources, status/readiness, or deployment changed; deployed `0.5.3` remains marker-only. The next gate is credential generation/value-source policy, then collision-safe Secret/ConfigMap pre-reads, SSA, minimal RBAC, and reconciliation; TLS/optional credentials/storage/probes/readiness/rotation remain deferred.

## :material-book-open-page-variant-outline: 2026-08-21: Pure Retained Credential Generation Slice Committed Locally

Implemented and validated `generate_coriolis_credentials`, `generate_infrastructure_credentials`, and `generate_step_ca_credentials` in `src/coriolis_operator/reconcile.py`, then committed the pure slice locally at `a604579` on `dev`; it is not pushed or deployed. The helpers independently generate all seven frozen credential keys with production `secrets.token_urlsafe(32)` (32 random bytes/256 bits, URL-safe opaque strings). Token-factory injection is only for deterministic tests; empty/non-string results fail value-safely. Generated values compose unchanged with the existing builders and must never appear in documentation, logs, status, events, or errors. The frozen policy is operator-generated only: no inline CR credential values and no external credential Secret source; external/pre-existing Secrets such as `coriolis-appliance-registry` remain read-only and outside this policy. Generate-once/reuse is policy only until runtime wiring: call each generator only when its retained Secret is `ABSENT`, reuse exact matching ownerless Secrets unchanged, fail closed on collisions, and defer automatic rotation. `main.py` does not call generators. Validation passed: 132 total tests (16 new from 116), focused tests, `uv run ruff check .`, `uv run ruff format --check .`, `uv run mypy src`, `helm lint helm/`, `helm template coriolis-operator helm/ --include-crds`, and `git diff --check`. No reconciliation, Kubernetes reads/writes/SSA, RBAC, CRD, runtime resources, status/readiness, chart/release, deployment, or rotation changed; deployed `0.5.3` remains marker-only. Next gates: configuration rendering inputs, retained Secret semantic validation, collision-safe pre-reads for all frozen resources before mutation, `ABSENT`-only generation, SSA, minimal Secret RBAC, and reconciliation/status failure semantics; TLS/optional credentials/storage/probes/readiness/bootstrap/rotation remain deferred.

## :material-book-open-page-variant-outline: 2026-08-21: Retained Secret Semantic Validation/Extraction Slice Committed Locally

Implemented and validated `validated_retained_secret_values` in `src/coriolis_operator/reconcile.py`, then committed the pure slice locally at `5165629` on `dev`; it is not pushed or deployed. It accepts mapping-shaped objects and Kubernetes `V1Secret` models; tolerates absent `apiVersion`/`kind` but rejects conflicting present values; requires `type: Opaque`; rejects persisted `stringData`; requires the exact frozen `data` keys with string encoded values; strictly base64-decodes then UTF-8-decodes them; and rejects empty decoded values. It returns a new decoded mapping without mutating input. Fixed/category-only errors reveal neither encoded nor decoded values. The helper validates semantics only: it does not classify metadata, read or write Kubernetes resources, generate values, apply SSA, handle collisions/status, or reconcile. Validation passed: 152 total tests (20 new from 132), focused 20 tests, `uv run ruff check .`, `uv run ruff format --check .`, `uv run mypy src`, `helm lint helm/`, `helm template coriolis-operator helm/ --include-crds`, and `git diff --check`. No `main.py`, RBAC, CRD, runtime resource, chart/release, deployment, or rotation behavior changed; deployed `0.5.3` remains marker-only. Future preflight must classify metadata first, then run semantic validation and map a semantic failure fail-closed to `COLLISION`; returned decoded values remain internal and must never be logged, statused, or evented. Next gates are non-sensitive configuration rendering inputs/templates, collision-safe pre-reads of all frozen resources before mutation, `ABSENT`-only generation, SSA, minimal Secret RBAC, and reconciliation/status failure semantics; TLS/optional credentials/storage/probes/readiness/bootstrap/rotation remain deferred.

## :material-book-open-page-variant-outline: 2026-08-21: Non-Sensitive Configuration Rendering Slice Committed Locally

Implemented and validated pure `render_coriolis_config` in `src/coriolis_operator/configuration.py`, then committed the slice locally at `97153a7` on `dev`; it is not pushed or deployed. It validates explicit bind address, port, configuration directory, VixDiskLib log directory, and accepted version; uses Jinja2 `PackageLoader` with `StrictUndefined`, disabled autoescape, and preserved trailing newlines; and renders exactly the six frozen ConfigMap files. The six verbatim Apache-2.0 upstream templates are packaged with source/license attribution, and `accepted_version` maps to legacy `default_coriolis_docker_images_tag`. It renders no `coriolis.conf`, provider fragments, credentials, or other Secret content. Validation passed: 19 focused renderer tests, 171 total tests, wheel resource inspection, and template byte comparison. No `main.py`, reconciliation, Kubernetes reads/writes, SSA, RBAC, CRD, runtime resources, workload, release/chart/image version, deployment, TLS, storage, readiness, bootstrap, or rotation changed; deployed `0.5.3` remains marker-only. The next gate is collision-safe preflight for all five frozen resources before mutation: metadata classification before retained Secret semantic validation, semantic failures fail closed to `COLLISION`, and generators only for `ABSENT`; then SSA, minimal Secret RBAC, and reconciliation/status semantics.

## :material-book-open-page-variant-outline: 2026-08-21: Pure Foundational Five-Resource Preflight Committed Locally

Implemented and validated the pure foundational five-resource preflight in `src/coriolis_operator/reconcile.py`, then committed it locally at `35eac9b` on `dev`; it is not pushed or deployed. `OwnedClassification.ABSENT/MANAGED/COLLISION`, `classify_owned_resource`, credential-safe frozen `FoundationalResourcePreflight`, and `preflight_foundational_resources` cover exactly the three retained credential Secrets plus the owner-referenced configuration ConfigMap and Secret. Metadata for all five is classified before retained Secret semantics; exact owner metadata/controller matches are `MANAGED` despite content/type drift, ownership or retention mismatch is `COLLISION`, and unrelated extra metadata is allowed. Any metadata collision stops semantic validation and generation. `REUSE` retained Secrets are semantically validated, a validation `ValueError` maps to that resource's `COLLISION`, all reusable semantics finish before generation, and generators run only for corresponding `ABSENT` retained Secrets after a collision-free preflight. Successful results carry generated/decoded credentials but hide them from `repr`; errors, logs, status, and events remain value-safe. Validation passed: 23 focused tests, 194 total tests, `uv run ruff check .`, `uv run ruff format --check .`, `uv run mypy src`, `helm lint helm/`, `helm template coriolis-operator helm/ --include-crds`, and `git diff --check`. This is pure only: no `main.py`, Kubernetes reads/writes, SSA, RBAC, CRD, runtime construction/adoption, reconciliation/status behavior, chart/release/image version, deployment, TLS/storage/readiness/bootstrap/rotation changes; deployed `0.5.3` remains marker-only. The next gate is runtime integration of collision-safe Kubernetes pre-reads for all five before mutation, minimal Secret/ConfigMap `get` and SSA RBAC as required, SSA application, and reconciliation/status collision/failure semantics; retained adoption/runtime construction remains separately deferred.

## :material-book-open-page-variant-outline: 2026-08-21: Sensitive Renderer Contract Committed Locally

Documented and committed the sensitive `coriolis.conf` renderer contract locally at `574efcf` on `dev`; it is not pushed or deployed. This is documentation-only: no renderer implementation/tests or runtime behavior changed, and deployed `0.5.3` remains marker-only. The complete rendered `coriolis.conf` remains exactly one key in the owner-referenced configuration Secret, never ConfigMap/log/status/event/metadata/documentation content. The future pure renderer source model is frozen from the immutable upstream base template and all 16 provider fragments, preserving authoritative provider lists/order/module maps; initial custom module overrides are prohibited and compression/compressor is disabled. Explicit internal dependency inputs, fixed source-audited identities/paths, credential mappings, strict value-safe validation, and a redacted interface boundary are frozen without CRD fields. All three generated credential Secrets have exactly `coriolis.cloudbase.it/retention: state-credentials`. TLS/CA material, provider connection/private data, optional credentials, dependency Services/workloads, bootstrap, storage, readiness, and rotation remain deferred. Source review covered every base-template variable and all fragments; `git diff --check` passed. The corrected next sequence is pure sensitive renderer implementation/tests, then a frozen multi-resource read/apply failure, status, atomicity, and marker-last policy, then runtime five-resource pre-reads, minimal RBAC, and SSA; runtime integration is not currently unblocked.

## :material-book-open-page-variant-outline: 2026-08-21: Pure Sensitive Renderer Implemented Locally

Implemented and validated `SensitiveCoriolisEndpoints`, `SensitiveCoriolisCredentials`, redacted one-key `SensitiveCoriolisConfig`, and `render_sensitive_coriolis_config`, committed locally at `9bb20f3` on `dev` but not pushed or deployed; deployed `0.5.3` remains marker-only. The exact one-key `coriolis.conf` output composes with the existing configuration-Secret builder and is rejected by the ConfigMap boundary; inputs are frozen/exact and unmutated, reprs are redacted, errors are fixed/category-only and value-safe, and Jinja `PackageLoader`/`StrictUndefined` with disabled autoescape and a trailing newline preserves the immutable upstream base plus all 16 provider fragments. Validation passed: 40 focused configuration tests, 215 total tests, Ruff lint/format, strict mypy, Helm lint/template, `git diff --check`, 17/17 source byte parity, and offline wheel inspection of exactly 25 expected template resources. No runtime behavior changed. The immediate gate is multi-resource read/apply failure, reconciliation status, atomicity, and marker-last semantics, followed by collision-safe pre-reads for all five resources, minimal Secret/ConfigMap RBAC, and SSA; retained adoption/runtime construction remains deferred.

## :material-book-open-page-variant-outline: 2026-08-21: Foundational Multi-Resource Failure Policy Frozen

Documented and froze the multi-resource failure/status/atomicity/race-safety/marker-last policy in [Foundational Resource Contract](foundational-resource-contract.md). The documentation-only contract requires validation; marker pre-read/classification; canonical pre-reads of the three retained credential Secrets, configuration ConfigMap, and configuration Secret; complete pure preflight, generation, rendering, and manifest preparation before mutation; canonical operations; and marker last. It defines non-transactional stop-on-first-failure behavior with no rollback, compensation, or deletion; create-on-absent with `AlreadyExists` retry/preflight; resource-version/concurrency-guarded SSA with `Conflict` retry/preflight; retained `REUSE` as no-write; and value-safe status-then-framework-retry results. The marker is only foundational completion, never readiness, transactionality, or no-drift proof; `Ready=False/RuntimeNotImplemented` remains truthful. No code, runtime behavior, Kubernetes interaction, RBAC, CRD, chart/release, or deployment changed; deployed `0.5.3` remains marker-only. `git diff --check` is the documentation-slice validation, with no code test count claimed. Next is collision-safe runtime reads and create/guarded SSA for all five resources plus the marker, minimal Secret/ConfigMap `get`/`create`/`patch` RBAC, status-then-Kopf-retry wiring, and exhaustive tests; runtime integration remains blocked until it passes. Retained adoption/runtime construction and TLS/provider private data/bootstrap/storage/readiness/rotation remain deferred.

## :material-book-open-page-variant-outline: 2026-08-21: Pure Contract/Input Migration Completed Locally

Completed an uncommitted, unpushed, and undeployed pure contract/input migration, validated by 218 unit tests. The deterministic factory derives RabbitMQ, Memcached, MariaDB, and Keystone Service names; fixes API `0.0.0.0:7667`, `/etc/coriolis`, and `/var/log/coriolis/vmware-root`; and aligns Kubernetes-derived templates to RabbitMQ plaintext `5672`, Keystone HTTP `5000`, and HTTP WSGI without internal CA/TLS directives. Step CA is removed from initial pure APIs/preflight; the foundational set is two retained credential Secrets plus an owned config ConfigMap/Secret. The slice changes the CRD ingress schema/sample and adds pure validation for development host `coriolis.app.cloudbase.wiki`, class `nginx`, cert-manager `letsencrypt` with derived `<host>-tls` Secret name, or an existing same-namespace TLS Secret without a cert-manager annotation. No `main.py`, runtime Kubernetes I/O, SSA/RBAC, actual Service/Ingress/workload resources, or release/chart/image/deployment behavior was added; deployed `0.5.3` remains marker-only. The next implementation is collision-safe marker-plus-four reads/create/guarded SSA, Secret/ConfigMap `get`/`create`/`patch` RBAC, sanitized status then Kopf retry, and exhaustive tests; Services, Ingress, and workloads follow later.

## :material-book-open-page-variant-outline: 2026-08-21: Kubernetes Networking Contract Committed Locally

Committed the pure Kubernetes networking/configuration contract locally at `e2ddb30` on `dev`; it is not pushed or deployed. Final validation passed: 218 unit tests, Ruff lint/format, strict mypy, Helm lint/template, `git diff --check`, byte parity for all 23 immutable copied upstream templates, and offline wheel inspection of all 27 packaged template resources. The commit adds no `main.py`, runtime Kubernetes I/O, SSA/RBAC, actual Service/Ingress/workload resources, release/chart/image change, or deployment behavior; deployed `0.5.3` remains marker-only. The next gate remains collision-safe marker-plus-four reads/create/guarded SSA, Secret/ConfigMap `get`/`create`/`patch` RBAC, sanitized status then Kopf retry, and exhaustive tests.

## :material-book-open-page-variant-outline: 2026-08-21: Marker-Plus-Four Foundational Runtime Gate Completed Locally

Completed the local, uncommitted, unpushed, and undeployed marker-plus-four foundational runtime gate. Reconciliation performs the marker plus four exact ordered pre-reads, treats only `404` as absent, completes metadata-first preflight/rendering before writes, creates absent retained `state-credentials` or reuses it without writing, uses resourceVersion-guarded SSA for managed resources, writes the four foundational resources in order with the marker last, and provides no rollback. Retryable failures publish sanitized `ResourceReadFailed`, `ResourceApplyFailed`, or `MarkerApplyFailed` status before `kopf.TemporaryError`; `ResourceCollision` remains stable and mutation-free, and success remains `Ready=False/RuntimeNotImplemented`. Secret and ConfigMap RBAC is exactly `get`/`create`/`patch`. Validation passed: `uv run pytest tests/unit` (243 passed); `uv run ruff check .`; `uv run ruff format --check .` (35 files already formatted); `uv run mypy src`; `helm lint helm/`; `helm template coriolis-operator helm/ --include-crds`; and `git diff --check`. No Services, Ingress resources, workloads, storage, bootstrap, probes/readiness, rotation, deployment, or version change was added; deployed `0.5.3` remains unchanged and marker-only. Services, Ingress, workloads, and remaining runtime design are deferred.

## :material-book-open-page-variant-outline: 2026-08-21: Foundational Gate Committed Locally

The marker-plus-four foundational runtime gate is committed locally at `862777d` on `dev`, with status commit `f219977`; both remain unpushed and undeployed. Deployed release `0.5.3` remains unchanged and marker-only.

## :material-book-open-page-variant-outline: 2026-08-21: Four Dependency Services Implemented Locally

Implemented the current uncommitted, unpushed, and undeployed Service slice: RabbitMQ `5672`, Memcached `11211`, MariaDB `3306`, and Keystone `5000`, in frozen order. The Services are owner-referenced ClusterIP/plaintext resources with deterministic names, standard metadata, label-safe identity/component selectors, and one named TCP port; no workloads, endpoints, Ingresses, Jobs, or additional Services were added. Validation before documentation changes passed: 252 unit tests, Ruff lint/format, mypy, and Helm lint/template; `Ready=False/RuntimeNotImplemented` remains expected.

## :material-book-open-page-variant-outline: 2026-08-22: Four Dependency Services Committed Locally

The four-Service implementation is committed locally at `797235b` on `dev`, unpushed and undeployed: RabbitMQ `5672`, Memcached `11211`, MariaDB `3306`, and Keystone `5000`. Full validation passed with 252 unit tests, Ruff lint/format, mypy, Helm lint/template, and `git diff --check`; deployed `0.5.3` remains unchanged and marker-only.

## :material-book-open-page-variant-outline: 2026-08-22: Dependency Workload Evidence Gate Documented Locally

Documented a local, unpushed, and undeployed workload evidence gate: exact approved support-image identities and source-backed dependency facts are frozen, while missing OCI, runtime, storage, and probe facts are fail-closed blockers. MariaDB is the first candidate after eligibility closes; no behavior changed. Validation passed: 252 unit tests, Ruff lint/format, mypy, Helm lint/template, and `git diff --check`.

## :material-book-open-page-variant-outline: 2026-08-22: MariaDB Runtime Evidence Advanced Locally

Anonymously inspected and pulled `cr.virtomat.io/virtomat/coriolis/mariadb-server@sha256:22cb109d23d1aa6a6acb17e54657b5b9cd753837b01345b52fc3c35cbbd9981e`, then locally proved direct restricted non-Galera `mariadbd` operation, value-safe idempotent bootstrap, authenticated TCP validation, and durable Docker-volume recreation. Rejected `kolla_start` because its required configuration and password handling violate the value boundary; its built-in checks are Galera-specific. This local, unpushed, and undeployed evidence is not a Kubernetes workload manifest contract: MariaDB remains not implementation-eligible pending workload, storage/PVC, generated-manifest, probe, resource, lifecycle/recovery, and reliable log-capture decisions. No source/runtime code, CRD, workload, PVC, Job, RBAC, probe/readiness, release/version, deployment, or cluster state changed; deployed `0.5.3` remains marker-only. Validation passed: 252 unit tests, Ruff lint/format, mypy, Helm lint/template, and `git diff --check`; no disposable MariaDB containers, networks, or volumes remain.

## :material-book-open-page-variant-outline: 2026-08-22: MariaDB Kubernetes Contract Documented Locally

Documented the development MariaDB Kubernetes contract using the approved digest, direct `mariadbd --console`, file-only credential handling, a future optional backward-compatible storage/resources API, retained ownerless RWO Filesystem PVC, owner-referenced single-replica StatefulSet, generated configuration/bootstrap resources, conservative probes, 30-second termination, and mutation-free reconciliation ordering. This supersedes the prior entry's pending-design status and closes design eligibility only for pure CRD validation/resolver and manifest-builder/preflight work; reconciliation follows only after tests. No source/runtime code, CRD, RBAC, workload, PVC, Helm artifact, deployment, or cluster behavior changed; deployed `0.5.3` remains marker-only and `Ready=False/RuntimeNotImplemented` remains truthful. Target-storage validation and production backup/restore, HA, and RPO/RTO acceptance remain open. Validation passed: 252 unit tests, Ruff lint/format, mypy, Helm lint/template, and `git diff --check`; no disposable MariaDB resources remain.

## :material-book-open-page-variant-outline: 2026-08-22: MariaDB Pure Desired State Implemented Locally

Implemented the local, unpushed, and undeployed pure MariaDB preparation gate: optional CRD storage/resources fields; an immutable, quantity-safe complete-settings resolver; value-safe direct-MariaDB configuration/bootstrap rendering; exact retained-PVC immutable-spec classification; deterministic PVC, ConfigMap, Secret, and single-replica StatefulSet manifests; and metadata-first collision preflight that returns no manifests after a collision. Credentials remain file-only, rendered Secret values are redacted from representations, and the StatefulSet fixes the approved digest, restricted security context, retained RWO Filesystem storage, ordinary runtime/tmp `emptyDir` volumes, authenticated probes, requested resources, and 30-second termination. No `main.py` integration, Kubernetes I/O, RBAC, status/readiness, runtime resource, release/version, deployment, cluster, or target-storage behavior changed; deployed `0.5.3` remains marker-only. Reconciliation and narrow RBAC are next, while target-storage validation and production backup/restore, HA, and RPO/RTO acceptance remain separate gates. Validation passed: 286 unit tests, Ruff lint/format, mypy, Helm lint/template, and `git diff --check`.

## :material-book-open-page-variant-outline: 2026-08-22: MariaDB Reconciliation Implemented Locally

Implemented the local, unpushed, and undeployed MariaDB reconciliation gate. Complete settings are resolved before Kubernetes client construction; omitted, incomplete, or invalid settings return stable mutation-free `InvalidRuntimeConfiguration` status and preserve accepted-version state. Valid reconciliation preserves the marker/foundational/four-Service read prefix, then reads the retained PVC, ConfigMap, configuration Secret, and StatefulSet in order. All collision classification, secret-safe rendering, resource-version validation, and desired-state preparation complete before writes. Writes remain foundational resources, Services, absent-PVC create or exact no-write reuse, guarded MariaDB ConfigMap/Secret/StatefulSet SSA, then marker last. Storage/resources field handlers permit correction and supported resource changes. RBAC adds only PVC `get`/`create` and StatefulSet `get`/`create`/`patch`; no PVC patch/delete, pod/log, PDB, or cluster-scope permission was added. `Ready=False/RuntimeNotImplemented` remains truthful, no resource was deployed, and deployed `0.5.3` remains marker-only. Validation passed: 316 unit tests, Ruff lint/format, mypy, Helm lint/template, and `git diff --check`. Target-storage validation and production backup/restore, HA, and RPO/RTO acceptance remain open.

## :material-book-open-page-variant-outline: 2026-08-22: Development Stack Published

MariaDB reconciliation commit `55212b0` is published on `origin/dev` and remains undeployed. Its publication also includes the preceding `dev` stack: foundational resources, the four-Service slice, MariaDB evidence/contract, and pure desired-state preparation at `5a2dfce`. The deployed operator remains `0.5.3` marker-only, so `Ready=False/RuntimeNotImplemented` remains truthful. The existing local validation suite passed before publication: 316 tests, Ruff lint/format, mypy, Helm lint/template, and `git diff --check`. Target-storage validation is next; production backup/restore, HA, and RPO/RTO remain open.

## :material-book-open-page-variant-outline: 2026-08-22: MariaDB Single-Node Storage POC

CIXpress pipeline `8nownk` completed all expected steps for source `6ba9c7e`; the resulting `0.5.5` operator image was installed in disposable namespace `coriolis-mariadb-validation-20260822` with its observed digest. Clean first boot provisioned and bound the ownerless `local-path` RWO Filesystem PVC and started MariaDB, but readiness failed because `mariadb-install-db` created anonymous `localhost` and pod-host accounts that shadowed `coriolis@%`. No credential value or raw log was inspected. The local renderer now adds `--skip-test-db`, which the approved image documents and implements by omitting `mysql_test_db.sql`; 20 focused tests and the full 316-test suite, Ruff lint/format, mypy, Helm lint/template, and diff checks pass. The fix remains uncommitted, unreleased, and not yet validated on a clean first boot.

After a controlled file-authenticated admin repair removed only the two disposable anonymous accounts, the POC proved UID/GID/fsGroup `42434` write access, authenticated probes, persisted database state, and exact retained-PVC reuse across CR deletion/recreation: PVC UID, resourceVersion, creation timestamp, bound volume, and normalized identity/spec hash remained unchanged, as did both retained credential Secret identities. A normal StatefulSet pod deletion completed in `1.684s`, and the replacement remounted the same volume on the single node with zero restarts, healthy probes, and the persisted marker intact. CSI and cross-node attach/detach were unavailable and remain unvalidated. The appliance, Helm release, namespace, retained PVC, and `Delete`-policy PV were removed; the node remained Ready and schedulable.

## :material-book-open-page-variant-outline: 2026-08-22: MariaDB Fix Released And Clean POC Passed

Published anonymous-account prevention at `3ee5d2d`. CIXpress pipeline `kpv306` completed `git-clone`, `kaniko-build`, `helm-update`, and `cleanup` successfully and produced CI-owned release commit `647c63b` with chart, app, and image version `0.5.6`. The isolated operator pulled `0.5.6` at `sha256:282991bd0f6cb76dc4f2e5049047db483afee22f725f08e5a05ed0245bf35607`.

On a fresh namespace and PVC, released `0.5.6` reached PVC Bound in 4 seconds and Pod Ready in 17 seconds without repair SQL. Admin and application file-authenticated checks passed; anonymous-account count and `test` database count were both zero. UID/GID/fsGroup `42434` write access, restricted security context, all probes, and a persisted application marker passed. CR deletion/recreation preserved the exact PVC UID, resourceVersion, creation timestamp, bound volume, normalized identity/spec hash, and retained credential Secret identities. A normal Pod deletion completed in 12 seconds; the replacement remounted the same local-path volume with zero restarts, healthy probes, zero anonymous/test accounts, and the marker intact. The CR, Helm release, namespace, PVC, and `Delete`-policy PV were removed; the node remained Ready and schedulable. CSI and cross-node attach/detach remain unvalidated, as do production backup/restore, HA, and RPO/RTO.

## :material-book-open-page-variant-outline: 2026-08-22: Memcached Reconciliation Implemented Locally

Implemented a local, uncommitted, unpushed, and undeployed Memcached Deployment using the approved exact digest and direct `/usr/bin/memcached -p 11211 -U 0` startup; the default Kolla command remains rejected. The single owner-referenced ephemeral Deployment uses UID/GID `42457`, read-only root, dropped `ALL` capabilities, no privilege escalation, `RuntimeDefault`, one `Recreate` replica, 30-second termination, no config, credentials, writable mounts, volumes, PVC, resources API, or init containers, and protocol-level startup/readiness/liveness probes. Reconciliation reads it after MariaDB, completes preflight before writes, creates absent or guarded-applies managed resources, leaves collisions mutation-free, writes after MariaDB before the marker, and adds only Deployment `get`/`create`/`patch` RBAC. `Ready=False/RuntimeNotImplemented` remains truthful. `uv run --offline pytest tests/unit` passed with 326 tests; Ruff lint/format, mypy, Helm lint/template, and `git diff --check` passed. No deployed Kubernetes Memcached POC or runtime resource, release, or chart version change is claimed: review, publication, and a released-artifact isolated POC are next, followed by RabbitMQ evidence after Memcached acceptance.

## :material-book-open-page-variant-outline: 2026-08-22: Memcached 0.5.8 Released And POC Passed

Source commit `063e438ef416599e9816a2400afcc5a5a7af9aa0` ("Implement Memcached reconciliation") was published by CIXpress pipeline `4dcpfk` (`Default`) from `2026-08-22T12:29:20+00:00` to `12:30:25+00:00`; its top-level, `git-clone`, `kaniko-build`, `helm-update`, and `cleanup` states were all `SUCCEEDED`. CI-owned release commit `cb6b055eaf5e74c99e26c1c3d662b2d749331627` set chart, app, and image version `0.5.8`.

The released-artifact POC used `cr.virtomat.io/virtomat/coriolis/operator:0.5.8` with operator imageID `sha256:9af4b018c2a7c0a23635d115d5335477b17bb81a731979bd0c93083c88461af4` in disposable single-node namespace `coriolis-memcached-validation-20260822` with CR `memcached-validation`. CR-to-Memcached-Deployment took 44s and Deployment-to-original Ready took 3s; MariaDB and Memcached reached `1/1`. The live Deployment contract passed: CR ownership; one `Recreate` replica; exact approved Memcached digest; direct command/args; port and pull Secret; UID/GID `42457`; disabled automount and service links; 30-second grace; restricted context; exact protocol probes; and no init containers, volumes/mounts, environment, configuration, credentials, PVC, or resource API. The Service selector and EndpointSlice targeted the Ready Pod on TCP `11211`; the Memcached Pod imageID matched approved digest `sha256:746b93082a4f6d07f464e93d4b14f5e30510abf17a9ae0a4af20e111408c8f1e`.

Service-DNS `version` and fixed set/get passed. Normal Pod deletion produced a Ready replacement in 3.603s with a distinct UID, zero restarts, the same digest, and a ready endpoint; the original key returned only `END`, and fresh version/set/get passed. This validates ephemerality, not persistence, HA, credentials, configuration, resource API, production readiness, or overall appliance readiness. The CR correctly remained `Ready=False/RuntimeNotImplemented` while dependencies were healthy. Cleanup normally removed the CR, Helm release, namespace, retained PVC, Delete-policy PV `pvc-b2856ccb-88ed-4d60-9878-89ef06331be8`, and copied registry Secret; the namespace/PV/release are absent, the node remains Ready and schedulable, and zero appliances remain cluster-wide. Argo now runs only healthy `coriolis-operator:0.5.8`, `1/1`, with no legacy selector overlap or appliance CR. Local validation remains 326 tests plus Ruff lint/format, mypy, Helm lint/template, and diff check. RabbitMQ evidence is next; MariaDB CSI/cross-node and production gates remain open.

## :material-book-open-page-variant-outline: 2026-08-22: RabbitMQ Reconciliation Implemented Locally

Implemented local RabbitMQ reconciliation; it is uncommitted, unpushed, undeployed, and runtime-incomplete. The approved Linux/amd64 image is `cr.virtomat.io/virtomat/coriolis/rabbitmq:2023.1-ubuntu-jammy@sha256:a595bf6f306ded2b6ad01f068ef69255df72eb73d471ba73ce9bbf0470d15d8a`, with local image ID `sha256:f9e28ef3ed172cfdda9e6c3d56c509ceaee672b516381343244ed40332a19e73`, Kolla `16.6.1`, and account UID/GID `42439` without a supplemental group. The default `dumb-init --single-child --` plus `kolla_start` path is rejected without configuration. Local disposable runs proved direct `/usr/sbin/rabbitmq-server` and `/usr/sbin/rabbitmq-diagnostics`, plaintext `0.0.0.0:5672`, console-only logging, read-only root, dropped `ALL` capabilities, no-new-privileges, and writable `/var/lib/rabbitmq`, `/run/rabbitmq`, and `/var/log/rabbitmq`.

Bootstrap is value-safe and file-only: the retained infrastructure Secret key is mounted only as a file, a random 4-byte salt is used, Rabbit SHA256 definitions are streamed with no credential/hash argv, environment, logs, or output, and mode-`0600` ephemeral definitions provision `openstack`, vhost `/`, and exact permissions. Two retained-volume launches reached Ready in 15.024s and 13.533s; sanitized running/local-alarm/listener and user/vhost/permission checks passed, state and a marker persisted/reconverged, SIGTERM exited `0` in 6.580s and 6.757s, and all disposable Docker artifacts were removed.

The implementation adds optional explicit `spec.storage.rabbitmq` and `spec.resources.rabbitmq`, a separate ownerless retained RWO Filesystem PVC, owner ConfigMap, restricted one-replica StatefulSet, existing Service/Secret references, direct script/probes/settings validation, collision-first preflight, ordered reads/writes, guarded SSA, PVC no-write reuse, no new RBAC verbs, and marker-last ordering. Writes are MariaDB, RabbitMQ, then Memcached. Full local validation passed: 355 unit tests, Ruff lint/format, mypy, Helm lint/template, and `git diff --check`. `Ready=False/RuntimeNotImplemented` remains truthful. The current Argo deployment is healthy docs-only `0.5.9` with zero appliance CRs and no POC workload; the tested Memcached artifact remains `0.5.8`. Remaining RabbitMQ gates are released-artifact single-node POC, target storage fsGroup/RWO, authenticated AMQP through Service DNS, EndpointSlice, normal replacement/same-node remount/persistence, CR recreation retained reuse/no-write, and cleanup; CSI cross-node, backup/restore, HA, RPO/RTO, and credential rotation follow. Keystone is next only after an accepted RabbitMQ POC.

## :material-book-open-page-variant-outline: 2026-08-22: RabbitMQ 0.5.11 Released And POC Accepted

Source `6a5a2b589c0dbfc2f5734f5863f9f8591c5f8c2d` was published by Default pipeline `qhvqt1` (13:59:46-14:01:07 UTC); top-level, `git-clone`, `kaniko-build`, `helm-update`, and `cleanup` succeeded. CI release `c48fd79622a8e760591333bf1ab6a0aa25d2f9d3` produced chart/app/image `0.5.10`. Its isolated clean bootstrap/authenticated-AMQP POC was not accepted: readiness used three sequential diagnostics at period `5s`/timeout `5s`, producing 37 timeout failures and Endpoint flapping despite zero restarts. Fix source `0d52c57aea1345213e519622106bb7b78236c0f1` sets readiness period `10s`/timeout `15s`; the full baseline remains 355 tests plus Ruff lint/format, mypy, Helm lint/template, and diff check. Default pipeline `4nj6f5` (14:14:34-14:15:38 UTC) also succeeded at top level and every expected step, releasing CI commit `3985a677100a55844dc07ac74a30a24e1e2b03e0`, chart/app/image `0.5.11`.

Accepted released-artifact validation used isolated namespace `coriolis-rabbitmq-validation-20260822`, operator imageID `sha256:fbc0cd338f79bf8a7ea06446f05210859ce549b387371594eee02b0ddef8a724`, the approved Rabbit digest, approved helper Coriolis API digest prefix `fce636...2705`, and single-node `local-path`. Probe-fix rollout replaced the Rabbit Pod, achieved eight consecutive 15-second Ready/endpoint samples, and preserved a durable AMQP marker. Normal Pod deletion started replacement in 4s, reached Ready in 29s with zero restarts and the same digest, then stayed stable for four samples; the marker survived. Normal CR deletion removed owned resources in 32s while retained Rabbit/Maria PVCs and credential Secrets preserved exact UIDs and unchanged resourceVersions. Recreation reached Rabbit StatefulSet in 2s and Ready in 31s, remained stable for four samples, preserved/authenticated the durable marker, and established new owner UID references.

Final clean-storage smoke created the CR at 15:20:37Z, reached Rabbit StatefulSet in 59s and Ready in 94s, and used a fresh ownerless Bound Rabbit PVC with a distinct PV. It had zero restarts, the corrected probe, no readiness timeouts, four stable 15-second samples, and a ready endpoint. Service-DNS authenticated AMQP durable publish/consume/ack and queue deletion passed. Conditions were `Accepted=True`, `Reconciled=True`, and truthfully `Ready=False`. Normal cleanup removed owned resources in 20s, namespace in 78s, and final Rabbit/Maria Delete-policy PVs in 1s; all earlier POC PVs, release, helpers, and namespace are absent. The node is Ready/schedulable, zero appliances remain cluster-wide, and shared Argo operator `0.5.11` is `1/1` with zero restarts and the same imageID. CSI/cross-node attach-detach, backup/restore, HA, RPO/RTO, credential rotation, and production storage remain open. Keystone runtime evidence is next.

## :material-book-open-page-variant-outline: 2026-08-22: Keystone Runtime Evidence Blocked At Database Gate

Local disposable inspection validated approved Keystone digest `sha256:7c57962762f5e6fdb1a109097e8f3e2e5f6218ad9c09f10a585adb67ed245cf0`, image ID `sha256:94cd15e8f645f97f65bd21a38713a13b5da44c67049de3a06436e0741f66d1ec`, Linux/amd64, Keystone `23.0.2`, unset OCI user/root default, packaged account `42425:42425` plus group `42400`, default `dumb-init --single-child --` and `kolla_start`, and no declared port or healthcheck. Unconfigured `kolla_start` is rejected because Kolla `config.json` is absent. No Keystone config, Apache site, Fernet directory, or credential-key directory is baked in. Direct WSGI and management commands plus an in-process file-password bootstrap wrapper are observed candidates only.

A restricted disposable harness then revalidated the existing exact-image MariaDB bootstrap boundary as non-root `42434:42434` with supplemental group `42400`, read-only root, dropped capabilities, and no-new-privileges. The first dedicated Keystone database validation failed before database/user authentication could be proved. Investigation stopped at the credential boundary rather than inspect raw SQL, DSNs, client files, or logs. No schema sync, Fernet/credential setup, admin bootstrap, WSGI, HTTP authentication, probes, restart, or persistence test followed. All disposable containers, networks, volumes, scratch paths, and generated credentials were removed. No tracked code, CRD, RBAC, release version, deployment, or Kubernetes resource changed. Keystone remains fail-closed and implementation-ineligible until a value-safe staged database diagnostic passes and the remaining runtime evidence closes.

## :material-book-open-page-variant-outline: 2026-08-22: Keystone Standalone Evidence And Contract Completed

Added tracked value-safe validator `scripts/validate-keystone-runtime.py` with focused unit coverage. Its staged fixed-category diagnostics resolved the earlier database failure without exposing raw material. The final exact-image run completed in `69.063s`: dedicated database/user/password/least-scope grant and repeated SQL; TCP authentication, table access, unrelated-database rejection, normal MariaDB stop and retained-data restart; `db_sync`, repeated sync, and current-schema check; non-root Fernet and credential setup/repeat with exact files `0`/`1` mode `0600`; file-only in-process admin bootstrap/repeat; and direct restricted `keystone-wsgi-public` on HTTP `5000`.

First/restarted `/v3` health took `5.628s`/`5.720s`; file-backed token authentication returned HTTP `201` with a non-empty token header in `1.480s`/`1.618s`; normal WSGI stops took `0.297s`/`0.277s`. Runtime was UID/GID `42425:42425`, supplemental group `42400`, read-only root, dropped capabilities, no-new-privileges, private networking, Secret-like read-only configuration, retained key repositories, and limited tmpfs paths. Installed source confirms each initial key file is independent URL-safe Base64 of 32 random bytes, token/receipt sharing is intentional, credential keys are separate, and read-only steady-state repositories are supported. All disposable resources and values were removed without emitting credentials, SQL values, DSNs, tokens, key material/hashes, headers, bodies, raw logs, or environments.

The frozen development contract selects a dedicated retained database credential Secret, separate retained Fernet and credential-key Secrets, stdlib-equivalent key generation, generated file-only config/auth payload, MariaDB bootstrap extension, idempotent init containers, one restricted `Recreate` Deployment, direct WSGI, authenticated startup/readiness, unauthenticated liveness, no PVC, no Job, no new RBAC verbs, and no rotation claim. This is local evidence and contract only: no operator reconciliation, CRD, Helm version, release, deployment, Kubernetes POC, HA, rotation, or production readiness changed. Keystone implementation is next.

## :material-book-open-page-variant-outline: 2026-08-22: Keystone Reconciliation Implemented Locally

Implemented the frozen Keystone contract in the local worktree. Three ownerless retained Secrets provide a dedicated database password and separate Fernet and credential repositories with metadata-first collision checks, absent-only independent generation, semantic validation, and exact no-write reuse. Generated rebuildable configuration is split between an owner ConfigMap and Secret. MariaDB bootstrap now creates and grants only the dedicated `keystone` database/user from the retained password, preserves its existing Secret key set, and uses a fixed non-sensitive pod-template annotation to roll managed Pods once.

Added one owner-referenced restricted `Recreate` Deployment using the approved digest, prepared file-only inputs, schema sync/check and idempotent bootstrap init containers, direct non-root WSGI, authenticated startup/readiness, unauthenticated liveness, no PVC, no Job, no service-account token, and no new RBAC verbs. Reconciliation pre-reads all three retained Secrets, the ConfigMap, configuration Secret, and Deployment before mutation; completes collision checks, retained generation/reuse, rendering, MariaDB rerendering, and all manifests before writes; writes absent Keystone retained state before MariaDB, then RabbitMQ, Memcached, Keystone generated resources, and the marker last. Managed resources use resourceVersion-guarded SSA; collisions are mutation-free and read/apply retries remain value-safe.

Full local validation passed: `uv run --offline pytest tests/unit -q` (397 passed), Ruff lint/format, mypy, Helm lint/template, and `git diff --check`. The implementation is uncommitted, unpushed, undeployed, and not Kubernetes POC evidence. No CRD, RBAC, Helm version, release, deployment, or cluster state changed. `Ready=False/RuntimeNotImplemented` remains truthful; publication and an isolated released-artifact POC are next.

## :material-book-open-page-variant-outline: 2026-08-23: Keystone Released And Accepted As 0.5.14

Keystone implementation source `f90cae4` was published by CIXpress pipeline `c2pajn`; all expected `git-clone`/`kaniko-build`/`helm-update`/`cleanup` steps `SUCCEEDED`, releasing `0.5.13` at CI commit `7eb2215`. Its isolated single-node POC was not accepted: the Keystone prepare init failed before db-sync/bootstrap because non-root UID/fsGroup `42425` could not chmod the root-owned emptyDir mount roots during `install -d -m 0700`; MariaDB, RabbitMQ, and Memcached stayed healthy. Fix source `087bbc2` (`Fix Keystone emptyDir preparation`) passed 398 unit tests plus Ruff lint/format, mypy, and `git diff --check`; pipeline `fhzlg6` succeeded at all four expected steps and released accepted `0.5.14` at CI commit `edd349a`. A direct local Helm OCI pull returned HTTP `401` before any disposable Helm release or CR was created, so the exact `0.5.14` chart was safely promoted by a disposable Argo Application in project `default` using the existing registry integration, an existing compatible CRD, and `skipCrds`; no credentials were exposed.

The accepted isolated single-node Keystone POC reached Keystone `Available` about 90 seconds after CR creation; prepare, db-sync, and bootstrap init containers exited `0`; `/v3` returned 200 and an authenticated token request returned 201 with a token header present on the restricted direct-WSGI one-replica `Recreate` Deployment, with ready Service/EndpointSlice and zero restarts across all workloads. A normal Pod replacement produced a distinct UID, the exact same image digest, zero restarts, init exit `0`, unchanged retained Keystone Secret UID/resourceVersion/timestamp, and passing probes/auth. CR deletion/recreation removed owned resources, reused retained Keystone Secrets and MariaDB/RabbitMQ PVCs exactly without writes under a new CR UID/ownerRefs, reached Keystone `Available` about 37 seconds later, and passed idempotent init/probes/auth. A deliberate unmanaged Keystone ConfigMap collision remained unchanged with `Reconciled=False/ResourceCollision` and zero managed writes; removing it restored healthy reconciliation.

Cleanup removed the CR, disposable Application, namespace, retained Secrets/PVCs, and Delete-policy PVs; the node was Ready/schedulable, and shared Argo operator `0.5.14` was Synced/Healthy `1/1` with zero restarts, no appliance CR, and no POC workload. This is historical Keystone `0.5.14` evidence.

## :material-book-open-page-variant-outline: 2026-08-23: Coriolis-Common 0.5.16 POC and Local v2 Correction

Source `391f94c6095f55178ba125c5eec22270284b0b92` was published by successful CIXpress pipeline `v2nv5t` as `0.5.16` at CI commit `54381936f98941473b166543274c0ea29a263e40`. Its disposable released-artifact POC used namespace `coriolis-common-validation-20260823` and Argo Application `coriolis-common-poc-20260823`. Dependencies and Services reached ready, but immutable `<appliance>-common-bootstrap-v1` failed all three attempts before dbsync. The operator correctly reported `BootstrapFailed`, wrote no marker, retained `Ready=False/RuntimeNotImplemented`, and did not mutate the failed Job. Sanitized exact-image diagnosis found oslo-config parse failures at the first provider continuation line 15 and, after a partial diagnostic correction, another provider continuation at line 27; no content or credentials are recorded.

The corrective v2 slice described here was subsequently published and accepted as `0.5.17`; its final evidence is recorded in the following entry. It retained immutable `<appliance>-common-bootstrap-v2` identity, the exact conductor digest, contiguous provider-list rendering, full generated configuration, supported dbsync overrides, create-only/collision/time-bound/RBAC/marker-last rules, and `Ready=False/RuntimeNotImplemented`.

Tracked validator `scripts/validate-coriolis-bootstrap-runtime.py` now uses the actual full sensitive renderer and emits no secrets. Dependencies, dbsync/repeat (`5.076s`, `3.761s`), actual bootstrap/repeat (`23.886s`, `20.044s`), and independent verification (`8.022s`) passed in `135.212s` with zero leftovers. Validation passes: 477 unit tests, Ruff lint/format, mypy, Helm lint/template, exact conductor parser check, Docker build, and `git diff --check`.

## :material-book-open-page-variant-outline: 2026-08-23: Coriolis-Common 0.5.17 POC Accepted

Fix source `764b9952d1e1c9bc1cbce08afddea8781f391f42` (`Fix Coriolis-common bootstrap dbsync`) was published by CIXpress pipeline `ectoq4`, started `2026-08-23T12:16:43Z` and completed `12:18:06Z`; `git-clone`, `kaniko-build`, `helm-update`, and `cleanup` all `SUCCEEDED`. CI commit `6bfc494d50949b7a5aa770c4febb7c5100b3b363` released `0.5.17`; operator imageID was `sha256:443e6e5dec8cd6e7f2040ca4fe1f5dcfcfa40ad36184cb30dc54a4be7547d8a6`. Historical `0.5.16` v1 provider-continuation parsing failed before dbsync and correctly produced `BootstrapFailed`, no marker, and no mutation.

v2 completed in place from `12:23:11Z` to `12:23:42Z` (31s), `succeeded=1`, `failed=0`, exit `0`, restarts `0`, on the exact conductor digest; v1 remained failed and untouched. Dependencies were `1/1` with one endpoint each. Output-suppressed exact-image verification passed twice in 10s, including post-recreation, for required schema tables, one unique enabled Coriolis user/default service project/password auth/admin assignment, the migration service, and exactly one RegionOne admin/internal/public endpoint each. No secrets, logs, or raw data were emitted. Resume preserved completed Job/immutable ConfigMap UID/resourceVersion/creation/completion and Job Pod identity without writes; replacement operator was ready in 4s with zero restarts.

An unmanaged immutable v2 ConfigMap collision remained exact and returned `ResourceCollision` with zero newly owned writes; five retained ownerless Secrets and MariaDB/RabbitMQ PVCs retained exact identity and ownerless retention, PVCs Bound. Removing only the conflict did not trigger reconciliation because metadata/child resources are not watched. A normal operator Pod replacement invoked Kopf resume in 6s and converged in 124s. This is an open operational follow-up, not automatic recovery. Final state was v2 only, v1 absent, dependencies healthy, Job successful, `acceptedVersion=2603.4`, `Accepted=True`, `Reconciled=True`, `Degraded=False`, and `Ready=False/RuntimeNotImplemented`.

Expected retained ownerless Secrets held CR observation to 300s after deletion; Application/namespace/both Delete-policy PV deletion took 11s/53s/9s. POC application, namespace, and PVs are absent. Shared `argocd/coriolis` `0.*.*` is `0.5.17` Synced/Healthy; shared operator `1/1` zero restarts; node Ready/schedulable. No application workload, Ingress, or `Ready=True` is claimed. Single-node local-path only; CSI/cross-node, backup/restore, HA, RPO/RTO, rotation, and production storage remain open. Next milestone: backend/application Services and workloads, then Ingress.

## :material-book-open-page-variant-outline: 2026-08-23: Coriolis API Runtime Implemented Locally

Exact approved image `cr.virtomat.io/virtomat/coriolis/coriolis-api:2603.4@sha256:fce6369f07ef777b5174d3a4f849d4eac914256a20a47ffa0cd1c98081be2705` was qualified with synthetic operator-rendered configuration without emitting values, logs, environments, or response bodies. The direct binary ran as UID/GID `42434` with read-only root, dropped capabilities, no-new-privileges, and only `/tmp`, `/var/log/coriolis`, and `/opt/coriolis/locks` writable. One master and one worker remained stable for 30 seconds; unauthenticated `/v1` returned exact `401`; the exact exec probe passed; and normal stop exited `0` in `0.848s`. The legacy Apache startup path was rejected after its baked module/startup interface failed restricted qualification.

Implemented deterministic owner-referenced `<appliance>-coriolis-api` ClusterIP Service on `7667` and a one-replica `Recreate` Deployment pinned to the exact digest and direct command. The Pod mounts the existing generated ConfigMap and sensitive configuration Secret together as one explicit read-only projected `/etc/coriolis` volume without `subPath`; no credentials enter environment, argv, metadata, status, events, logs, or documentation. The process is non-root with `RuntimeDefault`, read-only root, no privilege escalation, dropped `ALL`, disabled service links/token automount, and only the three evidenced `emptyDir` paths.

Controller integration reads and classifies both API resources before mutation, after the existing dependency/bootstrap reads. API collision is mutation-free, managed resources require resourceVersion-guarded SSA, and application occurs only after the immutable Coriolis-common Job is succeeded: Service, Deployment, then marker last. Bootstrap active/failed states create no API resource. Existing Service/Deployment RBAC is sufficient; no CRD, watch, finalizer, delete, Ingress, or readiness expansion was added. Full local validation passes: 503 unit tests, Ruff lint/format, strict mypy, Helm lint/template, container build, and `git diff --check`. This local implementation is pre-release evidence; it was subsequently released and POC-accepted as `0.5.22` (recorded above). `Ready=False/RuntimeNotImplemented` remains truthful; conductor is the next workload after API acceptance.

## :material-book-open-page-variant-outline: 2026-08-24: Transfer-Cron Controller Slice Implemented Locally

Local source adds the owner-referenced `<appliance>-coriolis-transfer-cron` one-replica `Recreate` Deployment at exact `cr.virtomat.io/virtomat/coriolis/coriolis-transfer-cron:2603.4@sha256:3a44d3b40ba92dff9217b8e7d6a7ca3e7a202efa2641c771ce9b2a3552b3ea9c`, running `/usr/local/bin/coriolis-transfer-cron --config-file=/etc/coriolis/coriolis.conf`. The Pod uses UID/GID/fsGroup `42434`, `OnRootMismatch`, read-only root, nonroot, no privilege escalation, dropped `ALL`, `RuntimeDefault`, `HOME=/tmp`, `PYTHONDONTWRITEBYTECODE=1`, read-only projected `/etc/coriolis`, and writable `/tmp` and `/var/log/coriolis`; it has no Service, `envFrom`, ports, probes, locks, PVC, init container, service account, or resources.

Reconciliation completes all reads, preflight, and resourceVersion checks before mutation. Collisions are mutation-free, managed SSA is resourceVersion-guarded, and apply failures are sanitized. After successful bootstrap, writes occur in conductor, scheduler, transfer-cron, API Service/Deployment, marker-last order. Existing generic Deployment RBAC suffices; no CRD, RBAC, Helm, or version change. Accepted reconciliation remains `Ready=False/RuntimeNotImplemented` and `Progressing=False/RuntimeNotImplemented`.

The full local gate passed: 548 unit tests, Ruff check, Ruff format check over 59 files, strict mypy over 14 source files, Helm lint/template, local operator Docker build image ID prefix `03163df34d52`, and `git diff --check`. The tracked exact-image runtime validator passed `SUMMARY runtime passed 320.708`, including exact image contract, fixed-output diagnostics after empty schedule load, stable runtime, graceful transfer-cron stop in `0.792s` and restart, RabbitMQ recovery with unchanged workload identities, and cleanup with no validator containers, volumes, or networks remaining.

This is local-only evidence: transfer-cron is not published or released and has no released-artifact Kubernetes POC. It starts after RabbitMQ and conductor; MariaDB is indirect through conductor. Nonempty schedules, due-transfer execution, Keystone trust, write workflows, persistence of in-memory cron state, HA, production storage, and overall readiness remain unqualified. Next gates are source publication through CIXpress, then an exact released-artifact isolated transfer-cron POC. Scheduler remains released and POC-accepted as `0.5.26`.

## :material-book-open-page-variant-outline: 2026-08-24: Coriolis Transfer-Cron 0.5.27 Released And POC Accepted

Implementation source `293500a0a64f6e10c776d674c141ae1ea3ba05e4` (`Implement Coriolis transfer-cron reconciliation`) passed 548 unit tests, Ruff, 59-file formatting, strict mypy over 14 source files, Helm lint/template, operator build image ID prefix `03163df34d52`, exact runtime validator `SUMMARY runtime passed 320.708`, and `git diff --check` with zero validator leftovers. CIXpress Default pipeline `ermyhd` ran `2026-08-24T19:58:15Z`-`19:59:44Z`; top-level and `git-clone` (19:58:15-19:58:26), `kaniko-build` (19:58:26-19:59:04), `helm-update` (19:59:30-19:59:36), and `cleanup` (19:59:36-19:59:44) all `SUCCEEDED`. CI commit `3f3074e68d9b786986e688649646f08bcd6fbd63` published tag/chart/app/operator `0.5.27`; released operator imageID is `sha256:7c70f0e1c54559440d02d5595b054c48c7b0d586f9f61b86aeaf717364b104cf`.

The accepted POC used context `virt-infra-dev-buc-hq`, project `default`, namespace/Application `coriolis-transfer-cron-validation-20260824`, exact chart `0.5.27` with `skipCrds`, and operator UID `04e546c6-91da-4c2a-9d14-ed1949894eb6` at zero restarts. An unmanaged ownerless zero-replica transfer Deployment (UID `daed6df8-8b2d-4fff-86fc-3d57edc58c64`, RV `13728367`, spec digest `0646ae84...d2f64`) and its ReplicaSet UID `21b1cedc-202e-4336-8d79-35fc47339591` stayed exact at t=0/+65/+130. Initial CR UID `f8699078-0ebd-4870-9cdb-fa9b688867a5`, generation/observed `1`, held absent acceptedVersion and stable `ResourceCollision`, with zero CR-owned resources, retained writes, marker, or operator restart. Deleting only the collision at `20:24:07Z` started first child/bootstrap at `20:24:25Z`; bootstrap completed `20:25:48Z`, final convergence was `20:25:52Z`, and transfer became Available `20:25:55Z` (108s), without CR edit or operator restart.

The live owner-referenced one-replica `Recreate` Deployment used transfer digest `sha256:3a44d3b...2b3ea9c`, direct command, UID/GID/fsGroup `42434`, restricted security, exact safe environment, projected config, and writable tmp/logs; it had no Service, `envFrom`, ports, probes, locks, PVC, init, service account, or resources. Pod Ready/zero restarts and fixed messaging probes twice 20 seconds apart passed. Conductor replacement UID `0ea63925-...` to `f7fe6acf-...` took 45s; transfer replacement UID `cf9c0bff-...` to `b1f81c16-...` took 7s with exact digest/zero restarts and empty post-fork schedule-load diagnostics; RabbitMQ replacement UID `39c17eb9-...` to `ab7ef648-...` took 35s while conductor/scheduler/transfer identities remained unchanged, with messaging probes passing at `20:41:18Z` and 20 seconds later.

Deleting old CR `f869...` at `20:48:54Z` left it absent in 1s and old-owned resources absent in 2s; recreated CR UID `bff7959a-37f3-4450-8593-7ea8c1ec00ac` began at `20:48:57Z`. Exactly five retained ownerless state Secrets and two Bound PVCs preserved UID/RV/creation/metadata/volume association without writes; 20 ephemeral resources moved to the new owner UID. Bootstrap completed in 51s and transfer/final convergence in 77s. Final conditions were `Accepted=True`, `Progressing=False/RuntimeNotImplemented`, `Reconciled=True`, `Ready=False/RuntimeNotImplemented`, `Degraded=False`, and `Upgradeable=False`. One local jq expression failed during transfer polling, so Kubernetes creation/Ready timestamps establish the accepted 7s result. An immediate aggregate probe after recreation was rejected; unchanged Pods passed independent diagnostics, then corrected warning-suppressed combined probes returned only `CORIOLIS_MESSAGING_OK` at `20:57:48Z` and `20:58:11Z`. This was tooling-only correction, not a workload restart/change.

Final CR deletion was `21:00:13Z`-`21:00:45Z`. Exact CR-owned GC time is unclaimed because one broad metadata query failed, but per-kind metadata-only checks proved no old owner refs before Application deletion; retained state remained until namespace deletion. Application deletion/absence was `21:05:35Z`/`21:05:41Z`; namespace deletion/absence `21:05:47Z`/`21:06:33Z`; both Delete-policy local-path PVs were absent `21:06:41Z`, with no force, finalizer/patch, or individual resource deletion. Final scope is absent; shared `argocd/coriolis` is `0.5.27` `Synced/Healthy`, operator `1/1` exact imageID/zero restarts, zero CRs, node Ready/schedulable. Evidence proves the exact live Deployment, collision recovery, empty schedule diagnostics, conductor/transfer/RabbitMQ recovery, retained no-write recreation, and normal cleanup. It does not prove nonempty schedules, cron firing/due transfers, Keystone trust, write/migration/provider workflows, in-memory cron persistence, HA, cross-node/production storage, backup/restore, or overall readiness. `Ready=False/RuntimeNotImplemented` and no transfer Service/Ingress remain mandatory; remaining workloads follow, then logical Ingress.

## :material-book-open-page-variant-outline: 2026-08-25: Minion-Manager Implemented And Qualified Locally

The exact `coriolis-minion-manager:2603.4@sha256:1ea016dd967ce249a45cf9937701a45880f3b42f8146a93d1f5eb4f1d84e1fb9` image passed immutable inspection and disposable runtime qualification under direct `/usr/local/bin/coriolis-minion-manager --config-file=/etc/coriolis/coriolis.conf`. It runs as one messaging worker with numeric UID/GID `42434`, read-only root, dropped `ALL`, no-new-privileges, read-only generated `/etc/coriolis`, writable `/tmp` and `/var/log/coriolis`, private networking, and no ports, locks, or Service. Fixed-output diagnostics and an empty minion-pool read proved RabbitMQ RPC and direct MariaDB access; 20-second stability, clean exit `0` in `1.245s`, restart, RabbitMQ recovery without recreation, and zero-leftover cleanup passed in `SUMMARY runtime passed 264.176`.

Local reconciliation adds one owner-referenced one-replica `Recreate` minion-manager Deployment after transfer-cron and before API/marker. Complete pre-reads precede mutation; collisions are mutation-free, managed repair requires resourceVersion-guarded SSA, common bootstrap success gates apply, failures are sanitized, and the marker remains last. The exact digest, direct command, restricted security, safe environment, projected configuration, and tmp/log writable paths are frozen. Existing Deployment RBAC suffices; no Service, CRD, RBAC, Helm version, probes, PVC, init container, service account, resources, or readiness expansion was added.

The full local gate passed 558 unit tests, Ruff, 61-file formatting, strict mypy over 15 source files, Helm lint/template, operator build image ID prefix `690bdd8663a8`, runtime validation, and `git diff --check`. This checkpoint records pre-release source evidence; it does not itself establish publication, a released artifact, or deployment. Minion provisioning, nonempty pools, automatic refresh, provider actions, Keystone trust, writes, HA, production storage, and overall readiness remain unqualified; `Ready=False/RuntimeNotImplemented` remains mandatory. Next gates are source publication through CIXpress, then an exact released-artifact isolated minion-manager POC.

## :material-book-open-page-variant-outline: 2026-08-25: Coriolis Minion-Manager 0.5.28 Released And POC Accepted

Source `5294df9a50e6c8a15c6cad374c66c3a9ab95b077` passed the complete publication gate: 558 unit tests, Ruff, 61-file formatting, strict mypy over 15 source files, Helm lint/template, operator build `690bdd8663a8`, `git diff --check`, and exact-image runtime rerun `SUMMARY runtime passed 255.112` with no leftovers. CIXpress Default pipeline `f3pgh0` matched the exact source and ran `05:18:39Z`-`05:19:50Z`; top-level and `git-clone`, `kaniko-build`, `helm-update`, and `cleanup` all `SUCCEEDED`. CI commit/tag `69c60fdcaec2ec86df99d5cf91520d7051000691` synchronized chart/app/operator `0.5.28`; released operator imageID is `sha256:4e1a90f2bb883f80437ca048aa6645a9e94d87b80f2f82ad1a4964993e88886b`.

The accepted isolated POC used context `virt-infra-dev-buc-hq`, project `default`, namespace/Application `coriolis-minion-manager-validation-20260825`, OCI chart `coriolis/helm/coriolis-operator:0.5.28`, and `skipCrds`. Operator UID `04bb480b-573f-4fc7-bbfc-ec08c1057fbd` ran the exact released digest with zero restarts. Ownerless zero-replica collision Deployment UID `403307cd-c06b-4b83-ac0d-83f42221bc27`, RV `13873650`, generation 1, and spec digest `070cb7df...8d74` held exact with initial CR UID `13c491c4-6ced-4e0a-8495-459615491336`, absent acceptedVersion, stable `ResourceCollision`, zero managed/retained/marker writes, and unchanged operator identity across three complete snapshots spanning 295s. Two earlier read-only scheduling attempts were not accepted because one arrived after its target and one mistyped the CR resource; neither changed the cluster.

Deleting only the collision at `05:42:21Z` created first children in 55s, completed bootstrap at `05:44:28Z`, and reached managed reconciliation/availability in 158s without CR edit or operator restart. The resulting owner-referenced one-replica `Recreate` Deployment pins exact minion digest `sha256:1ea016dd...e1fb9`, direct command, UID/GID/fsGroup `42434`, restricted security, projected `/etc/coriolis`, writable `/tmp` and `/var/log/coriolis`, and no Service, `envFrom`, ports, probes, locks, PVC, init, service account, resources, or extra mounts. A read-only verifier initially expected the wrong successful Degraded reason; source-backed `Degraded=False/NotDegraded` was confirmed without a workload change.

Fixed-output diagnostics and `get_minion_pools == []` passed before and after stability from `05:58:44Z` to `05:59:04Z`. Normal minion deletion replaced Pod UID `541a99d0-7509-44cc-99d7-01108b50dd74` with `b1d07f0d-38dd-4364-8eec-97eb4f26996f` Ready in 7s at the exact image and zero restarts. RabbitMQ deletion replaced UID `79e45f28-7ba0-4947-b185-12a7b441d410` with `31ab40c2-1cf8-447e-b37c-5f776f683d49` Ready in 31s while minion and unrelated UIDs/restarts plus RabbitMQ PVC UID/RV stayed unchanged; the fixed probe passed on the first post-recovery attempt at `06:01:50Z`.

Normal CR deletion at `06:04:33Z` removed old-UID-owned resources in 8s; recreation at `06:05:17Z` changed CR UID to `454dd96c-e204-4512-9f27-6c633d880dd3`, retained five ownerless state Secrets and two Bound PVCs at exact UID/RV, moved 24 ephemeral resources to the new owner, converged in 65s, and passed fixed RPC again. Final conditions were `Accepted=True/Accepted`, `Progressing=False/RuntimeNotImplemented`, `Reconciled=True/Reconciled`, `Ready=False/RuntimeNotImplemented`, `Degraded=False/NotDegraded`, and `Upgradeable=False/UpgradeNotSupported`.

Final CR deletion was `06:10:44Z`-`06:10:53Z`, Application deletion `06:11:46Z`-`06:11:55Z`, and namespace deletion `06:12:04Z`-`06:13:22Z`; both Delete-policy PVs were absent by `06:14:03Z`. No force, finalizer change, patch, or individual retained-resource/PV deletion occurred. Final scope is absent; shared `argocd/coriolis` remains `0.5.28` `Synced/Healthy`, shared operator UID `539701b6-ae8e-4fab-b442-0be684b120e6` is Ready at the released digest with zero restarts, and the node is Ready/schedulable. This evidence does not prove nonempty pools, provisioning or refresh, provider operations, Keystone trust, writes, HA, production storage, backup/restore, or overall readiness; `Ready=False/RuntimeNotImplemented` remains mandatory.

## :material-book-open-page-variant-outline: 2026-08-25: Deployer-Manager Implemented And Qualified Locally

Local source implements one owner-referenced one-replica `Recreate` deployer-manager Deployment at exact `cr.virtomat.io/virtomat/coriolis/coriolis-deployer-manager:2603.4@sha256:a2a7091daf8e172b96fa0b48d19ffad285d7bfaad42fc7e8cd44a688f06f36aa`, using direct `/usr/local/bin/coriolis-deployer-manager --config-file=/etc/coriolis/coriolis.conf`. `/entrypoint.sh`/Cmd metadata and restricted help/version passed. The Deployment runs as UID/GID/fsGroup `42434` with read-only root and a projected read-only config Secret; only memory `/tmp` and `/var/log/coriolis` `emptyDir` are writable. Generated `log_dir` requires the log mount: the initial full validator reported the omission as a sanitized `OSError`, and the corrected full validator passed in `256.242s`.

Reconciliation completes all reads/preflight before writes, holds collisions without mutation, uses resourceVersion-guarded SSA, applies deployer-manager after minion-manager and before API, and writes the marker last with sanitized failure status. No Service, port, probe, PVC, lock, init container, service account, resource block, `envFrom`, or other writable path is added. The runtime validator proves exact image/command/security/writable paths, fresh-empty-stack startup, stability, graceful stop/restart, and RabbitMQ recovery without recreating deployer.

The full local gate passed 568 tests in 14.40s, Ruff lint and 63-file format check, strict mypy over 16 source files, Helm lint/template, local operator Docker image `sha256:b762a5b339d04267a68f03850d9f59f785ea3eb3a5d8f5ae2fa72beb77a7b415`, `git diff --check`, and exact-token cleanup. This is not a release, deployment, or Kubernetes POC and does not prove deployer RPC, auto-deployment, Keystone trust, provider/migration/nonempty/write workflows, or overall readiness. `Ready=False/RuntimeNotImplemented` remains mandatory; publication, exact released-artifact provenance, and an isolated Kubernetes POC are next.

## :material-book-open-page-variant-outline: 2026-08-25: Coriolis Deployer-Manager 0.5.29 Released And POC Accepted

Source `4382c000f18cc323bacebc0e1df78c8b2f052d20` was published by CIXpress Default pipeline `wk1zo8` from `07:43:50Z` to `07:45:23Z`; top-level and `git-clone`, `kaniko-build`, `helm-update`, and `cleanup` all completed `SUCCEEDED`. CI release/tag `c35afc7032de19ec43f3870b6300e2f4f02370aa` synchronized chart/app/operator `0.5.29`; released operator digest is `sha256:37634ac8afd4ce7e7445aeb5f5786867cdd009d0f41b13b9baf95b1e92be4b08`.

The accepted isolated POC used context `virt-infra-dev-buc-hq`, project `default`, namespace/Application `coriolis-deployer-manager-validation-20260825`, OCI chart `coriolis/helm/coriolis-operator:0.5.29`, and `skipCrds`. A node initially observed `Ready=False` recovered to two consecutive `Ready=True` observations before any mutation and remained Ready. Ownerless zero-replica Deployment UID `866472b2-adf6-415a-8395-2421a079ab0e`, RV `13913626`, ReplicaSet UID `94388c5f-24f8-4c59-93b6-45d30716762c`, spec hash `831ed01de99bb0854032749f2ede0e613e5ad437620d5f105d9948b2c482a095`, and initial CR UID/RV `bb144cd3-a3ae-45e6-8eb3-69b00b6eb220`/`13913723` held exact across t=0/+65/+130 snapshots spanning 143s. acceptedVersion remained absent with stable `ResourceCollision`, zero CR-owner refs, deterministic managed/retained/marker writes, PVC/PV claims, or restarts. Deleting only the collision at `08:08:04Z` yielded first children in 54s, bootstrap in 135s, managed deployer in 140s, final reconcile in about 141s, and Available in 143s without CR edit or operator restart.

The live owner-referenced one-replica `Recreate` Deployment pins deployer digest `sha256:a2a7091daf8e172b96fa0b48d19ffad285d7bfaad42fc7e8cd44a688f06f36aa`, direct command, config Secret only, UID/GID/fsGroup `42434`, restricted security, writable memory `/tmp` plus `/var/log/coriolis` only, and every documented omitted surface. Initial Pod UID `e998c567-7edf-4ef4-a41d-0428be6ef464` was Ready with zero restarts; final conditions remain `Ready=False/RuntimeNotImplemented` and `Degraded=False/NotDegraded`. Initial 22s stability passed. Two normal deployer Pod deletions produced distinct Ready zero-restart exact-digest replacements (`aed98ed1-ce5b-40d6-b58e-ae309d9f6724`, then `669e570f-1101-4148-b820-531743a6e88d`) and 20s stability, but exact deployer replacement latency is deliberately unclaimed because two local monitor assertions failed after deletion. RabbitMQ replacement (`cd8cb7bf-c1eb-4640-b3c9-bc5e8e6969cd` to `9c6a86fb-9034-4139-a9ed-990067457d83`) was requested `08:28:30Z`, created `08:28:33Z`, Ready `08:29:01Z`, and zero-restart; deployer remained unchanged/zero-restart and Rabbit PVC UID/RV/volume remained `d8d80ddd-824d-4f90-b46e-6d151a0679e7`/`13916615`/`pvc-d8d80ddd-824d-4f90-b46e-6d151a0679e7`.

Normal CR lifecycle removed the old UID and created UID `ddfea8e8-baeb-4dc4-84cf-4c0e8cf04a24` at `08:39:39Z`, with final transition `08:40:48.942Z`. Five ownerless retained Secrets and two Bound PVCs preserved exact metadata/RV/volume; 25 direct ephemeral resources transferred (6 ConfigMaps, 8 Deployments, 1 Job, 3 Secrets, 5 Services, 2 StatefulSets). A duplicate local `kubectl create` returned `AlreadyExists` after the new CR existed, so exact recreation-command timing/latency is deliberately unclaimed; final state, ownership, and retention are accepted. Normal cleanup removed the CR `08:46:44Z`-`08:47:07Z`, Application `08:48:24Z`-`08:48:30Z`, namespace `08:48:37Z`-`08:49:50Z`, and both Delete-policy PVs by `08:50:09Z`, without force, grace-zero, finalizer patches, owner edits, or individual child/retained/PVC/PV deletion. Normal Argo Event records remain in `argocd` and were not individually deleted; exact CR/Application/namespace/PVs are absent. Shared `argocd/coriolis` is `0.5.29` `Synced/Healthy`; shared operator exact digest/zero-restart, no shared CR, node Ready/schedulable. This excludes deployer RPC, auto-deployment, Keystone trust, provider/migration/nonempty/write workflows, HA, production storage, backup/restore, and overall readiness. Privileged worker and remaining workloads are next.

## :material-book-open-page-variant-outline: 2026-08-25: Pre-Release Local Coriolis Worker Controller Slice

Local source implements one owner-referenced, one-replica `Recreate` worker Deployment at exact `cr.virtomat.io/virtomat/coriolis/coriolis-worker:2603.4@sha256:ff30999d6e43709411f197b1b6b80dbce1d7e5498a27f869df93a061626ab2c9`, applied after deployer-manager and before the API, using direct `/usr/local/bin/coriolis-worker --worker-process-count 1 --config-file=/etc/coriolis/coriolis.conf`, 30s grace, and `imagePullPolicy: Always`. The Pod hostname is the deterministic label-safe runtime name for stable `(host,binary,topic)` registration. The Deployment is explicitly root/privileged (`runAsUser/Group 0`, `privileged: true`, `allowPrivilegeEscalation: true`, read-only root, `Unconfined` seccomp) with automount token and service links disabled, a read-only config Secret, memory `/tmp`, `emptyDir` logs/export, and `hostPath` `/dev` RW and `/lib/modules` RO; there is no Service, port, probe, PVC, lock, init container, service account, resource block, `envFrom`, host networking, PID/IPC, mount propagation, or VDDK/Hyper-V/libvirt/logger/profiling/other mount.

Reconciliation completes collision-safe full pre-reads/preflight before writes, uses guarded SSA, gates apply on a succeeded common bootstrap, sanitizes apply failures, and writes the marker last; `Ready=False/RuntimeNotImplemented` remains mandatory. The exact image is linux/amd64 with root/unset user, `/entrypoint.sh`, an image Cmd of the direct worker/config, and no declared port, volume, or healthcheck. A hardened no-network/no-write image-contract stage uses `find_spec` without importing providers to prove all active provider module roots are present and `coriolis_provider_oracle_vm`, `coriolis_provider_opc`, `coriolis_provider_nutanix`, and `coriolis_provider_cloudstack` are absent, so the active Kubernetes provider class list excludes only oracle-vm/opc/nutanix/cloudstack while module maps/templates remain provenance; Kubernetes configuration adds `[luks] tpm2_pcrs = 7`. The registration-only validator on an internal disposable network, intentionally without host mounts/devices/provider actions, proves expected `NoWorkerServiceError`, one exact worker enabled/UP, direct worker RPC and scheduler selection, the same hashed database identity across a clean stop/restart and RabbitMQ recovery, unchanged containers, 20s stability, and complete cleanup; final `SUMMARY runtime passed 478.013`. During qualification full-stack long runs reproducibly hit exit 137 at historical 15s bounds for deployer/scheduler/worker, which now use 30s. Conductor's approximately 30s internal shutdown also raced a 30s SIGKILL boundary, so its manifest/validator bound is 45s. Mandatory exit 0 remains; final shutdown timings deployer `24.431s`, worker `22.135s`, minion `8.762s`, transfer `7.369s`, scheduler `20.518s`, conductor `30.689s`.

The full local gate passed 589 tests in 13.88s, Ruff check and 43-file format check, strict mypy over 17 source files, Helm lint/template, local operator image `d4e637d378ad`, `git diff --check`, and an independent review whose findings were corrected and requalified. This is local, uncommitted, unpublished implementation and qualification evidence, not a release, deployment, or Kubernetes POC; no source commit is cited yet and the current committed base remains `8a6a862`. Host device/module behavior is manifest-only pending an isolated POC. Excluded claims: provider/migration/task/write/Keystone-trust/VDDK/Hyper-V/libvirt/iSCSI/logger workflow, HA/storage persistence, and production readiness. Next gates are the final diff review, then explicit approval for commit/publish, CIXpress provenance, and a separately approved isolated registration-only POC/cleanup. Released deployer-manager `0.5.29` evidence remains retained.

## :material-book-open-page-variant-outline: 2026-08-26: Coriolis Worker 0.5.31 Released And POC Accepted

Source commit `d1f45c7297ffa925bd9412a26dedd50d1a95e50f` (`Implement Coriolis worker reconciliation`) was published by CIXpress Default `c9jv45` from `04:42:50Z` to `04:44:20Z`; top-level and `git-clone`, `kaniko-build`, `helm-update`, and `cleanup` all completed `SUCCEEDED`. CI release/tag `1b7835146242e945d40660cd6462486a860c7f70` synchronized chart/app/operator `0.5.31`; released operator digest `sha256:52762fa5741ca65b8b60df976f7253db72dac650f8ba1bb7b0ee03fe1dbdf7e9`, chart digest `sha256:8e2578b401a7261c0556ac65605fec6b1aa8ef88987ec6c33d08bafac9e5b2e5`.

The controller owns one owner-referenced, one-replica `Recreate` worker Deployment at exact `cr.virtomat.io/virtomat/coriolis/coriolis-worker:2603.4@sha256:ff30999d...26ab2c9`, applied after deployer-manager and before the API, using direct `/usr/local/bin/coriolis-worker --worker-process-count 1 --config-file=/etc/coriolis/coriolis.conf`, 30s grace, and `imagePullPolicy: Always`. The Pod hostname is the deterministic label-safe runtime name for stable `(host,binary,topic)` registration. It is explicitly root/privileged (`runAsUser/Group 0`, `privileged: true`, `allowPrivilegeEscalation: true`, read-only root, `Unconfined` seccomp) with automount token/service links disabled, a read-only config Secret, memory `/tmp`, `emptyDir` logs/export, and `hostPath` `/dev` RW and `/lib/modules` RO; no Service, port, probe, PVC, lock, init container, service account, resource block, `envFrom`, host networking, PID/IPC, mount propagation, or VDDK/Hyper-V/libvirt/logger/profiling/other mount. Reconciliation completes collision-safe pre-reads/preflight, guarded SSA, successful-bootstrap gating, and marker-last; `Ready=False/RuntimeNotImplemented` remains mandatory.

The accepted isolated POC used context `virt-infra-dev-buc-hq`, project `default`, namespace/Application `coriolis-worker-validation-20260826`, OCI chart `coriolis/helm/coriolis-operator:0.5.31`, and `skipCrds`; preflight node was Ready/schedulable/no pressure twice. An initial schema-valid but runtime-incomplete CR produced stable mutation-free `InvalidRuntimeConfiguration`; it was normally deleted while no child or retained resources existed, then recreated with source-backed `local-path` MariaDB 10Gi/RabbitMQ 1Gi and 250m/512Mi requests, 1 CPU/1Gi limits (no schema defaulting implied). Ownerless zero-replica collision Deployment UID `2858a477-d6e9-420f-99bf-8e61ab9fe7a9`/RV `14245705`/hash `8d6a1cb0` and RS UID `13e33901-24db-420b-83f7-fbe3140822ce`/RV `14245704`/hash `309fe1d5` plus CR UID/RV `b6082e05-b4bb-4b28-ae3a-6de46965c539`/`14247963` held exact across three snapshots spanning 204s with stable `ResourceCollision`, absent acceptedVersion, and no CR-owned/retained/marker/PVC/PV writes or operator/node change. Deleting only the collision at `05:16:01Z` produced first child `05:16:46Z`, bootstrap completion `05:18:09Z`, managed worker Ready `05:18:13Z`, and final reconcile `05:18:16Z`, without CR edit or operator restart (first observations, not guaranteed exact latency).

The live worker passed the exact contract: digest `sha256:ff30999d...26ab2c9`, direct command/count/config, one Recreate replica, appliance-specific hostname, root/privileged/Unconfined/read-only root, actual `/dev` RW and `/lib/modules` RO hostPath mounts, config Secret RO, memory tmp/emptyDir logs/export, all documented omissions, no Service/EndpointSlice, zero restarts, and truthful final conditions. A warning-suppressed fixed-output registration probe emitted exactly `coriolis-worker-ok`; direct Worker RPC plus scheduler showed exactly one enabled/UP worker; the hashed DB service identity `6c0f0800ab89c2cf30d5bc01eb22e6040a261bb9ab707681a1231ae30d18d983` stayed identical through stability, worker replacement, RabbitMQ recovery, and CR recreation. Initial 20s stability passed.

Normal worker Pod deletion requested `05:37:05Z`; replacement UID `ca6512f3-24bc-4819-88a5-f8b611ca6398` Ready `05:37:22Z`, zero restarts, exact image/node, unrelated identities unchanged. Normal RabbitMQ deletion requested `05:37:49Z`; replacement UID `ea8693bd-b493-4987-a33d-2d0da8e044bd` Ready `05:38:47Z`, zero restarts/exact image/node, worker unchanged; Rabbit PVC UID/RV/volume `48b92e12-0364-4bec-a8b8-a16ecee8e425`/`14251081`/`pvc-48b92e12-0364-4bec-a8b8-a16ecee8e425` exact.

Normal CR lifecycle deleted old UID `b6082e05-...` (requested `05:43:15Z`, resources absent `05:44:12Z`) and created new UID `4a701a89-ceec-47ac-b8c4-5c17593ec877` (`05:44:27Z`), converging all 26 direct resources by about `05:45:30Z` (6 ConfigMaps, 9 Deployments, 1 Job, 3 Secrets, 5 Services, 2 StatefulSets); five retained ownerless credential Secrets and both PVCs preserved exact UID/RV/volume with no retained owner refs; the marker and all 26 direct resources moved only to the new UID; probe/hash and 20s stability passed after recreation. Cleanup deleted the CR (`05:52:29Z`-`05:52:32Z`) with all children GC'd, Application `05:53:56Z`-`05:54:10Z`, namespace `05:54:12Z`-`05:55:35Z`, and both Delete-policy PVs by `05:55:55Z`, without force/grace-zero/finalizer/owner edits or individual child/retained/PVC/PV deletion; normal Argo Events not individually deleted. Final target CR/Application/namespace/PVs absent; shared `argocd/coriolis` `0.5.31` `Synced/Healthy`, shared operator 1/1 zero restarts, no shared CR, node Ready/schedulable/no pressure.

Accepted boundaries: released worker reconciliation, exact privileged host mounts working for the Pod/registration lifecycle, registration-only RPC/scheduler/DB identity, and collision/recovery/replacement/retention/cleanup. Excluded: provider imports/classes beyond `find_spec`, any provider/migration/task/write/Keystone-trust/VDDK/Hyper-V/libvirt/iSCSI/logger workflow, HA/cross-node/storage-persistence production claims, backup/restore, and overall readiness. `Ready=False/RuntimeNotImplemented` remains mandatory; remaining workloads and logical-origin Ingress follow. The full local gate remains 589 tests/13.88s, Ruff check and 43-file format check, strict mypy over 17 source files, Helm lint/template, local operator `d4e637d378ad`, and exact validator `SUMMARY runtime passed 478.013`, with clean review/cleanup.
